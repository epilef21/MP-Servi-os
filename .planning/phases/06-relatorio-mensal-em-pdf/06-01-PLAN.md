---
phase: 06-relatorio-mensal-em-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/utils/relatorioMensalPdf.js
autonomous: true
requirements: [REL-02, REL-03, REL-04, REL-05]

must_haves:
  truths:
    - "agregarRelatorio(os, mes, ano) retorna { totalOS, porStatus, lucroTotal, mediaPorOS, porSeguradora, porTecnico } calculados a partir do array filtrado pelo mês/ano"
    - "getLucroLocal(r) calcula (mo_seguradora - valor_prestador) + valor_deslocamento — idêntico a getLucro() do AdminPage"
    - "gerarRelatorioMensalPdf(dados, mes, ano, nomeEmpresa) chama doc.save() com nome no formato relatorio_MM_AAAA.pdf"
    - "O PDF contém seções: cabeçalho com nome da empresa + mês/ano, resumo geral, status, lucratividade, seguradora, técnico"
    - "Caracteres acentuados são sanitizados antes de entrar no jsPDF (função s() com replace de vogais e ç)"
    - "Arquivo exporta apenas agregarRelatorio e gerarRelatorioMensalPdf — sem efeitos colaterais no import"
  artifacts:
    - path: "mp-react/src/utils/relatorioMensalPdf.js"
      provides: "Funções de agregação de OS e geração de PDF mensal"
      contains: "agregarRelatorio"
  key_links:
    - from: "agregarRelatorio"
      to: "porStatus"
      via: "filtered.forEach(r => { const status = r.status || 'pendente'; porStatus[status] = (porStatus[status] || 0) + 1 })"
      pattern: "porStatus\\[status\\]"
    - from: "agregarRelatorio"
      to: "lucroTotal"
      via: "getLucroLocal(r) = (mo_seguradora - valor_prestador) + valor_deslocamento"
      pattern: "getLucroLocal"
    - from: "gerarRelatorioMensalPdf"
      to: "doc.save"
      via: "doc.save(`relatorio_${String(mes).padStart(2,'0')}_${ano}.pdf`)"
      pattern: "doc\\.save"
---

<objective>
Criar `mp-react/src/utils/relatorioMensalPdf.js` com duas funções exportadas:

1. `agregarRelatorio(os, mes, ano)` — filtra o array de OS pelo mês/ano via `criado_em.toDate()` e
   calcula: totalOS, porStatus (contagem por status), lucroTotal, mediaPorOS, porSeguradora
   (contagem + lucro por seguradora), porTecnico (contagem + lucro por técnico).

2. `gerarRelatorioMensalPdf(dados, mes, ano, nomeEmpresa)` — recebe o objeto retornado por
   `agregarRelatorio` e gera um PDF A4 via jsPDF com download automático. O PDF segue o mesmo
   padrão visual de `pdfGenerator.js`: cabeçalho azul (#1a3fa8), faixa laranja, rodapé com
   paginação, helpers checkPage/sectionHeader/twoCol/textBlock.

Purpose: Cumprir REL-02 (status), REL-03 (lucro + seguradora), REL-04 (técnico), REL-05 (PDF jsPDF).

Output: `mp-react/src/utils/relatorioMensalPdf.js` funcional, importável pelo AdminPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contratos relevantes — extraídos da base de código. O executor usa diretamente, sem explorar. -->

De mp-react/src/utils/pdfGenerator.js — padrão de helpers jsPDF a replicar:
```javascript
import jsPDF from 'jspdf'

function sanitize(str) {
  if (!str) return '—'
  return String(str)
    .replace(/[áàãâä]/g,'a').replace(/[ÁÀÃÂÄ]/g,'A')
    .replace(/[éèêë]/g,'e').replace(/[ÉÈÊË]/g,'E')
    .replace(/[íìîï]/g,'i').replace(/[ÍÌÎÏ]/g,'I')
    .replace(/[óòõôö]/g,'o').replace(/[ÓÒÕÔÖ]/g,'O')
    .replace(/[úùûü]/g,'u').replace(/[ÚÙÛÜ]/g,'U')
    .replace(/[ç]/g,'c').replace(/[Ç]/g,'C')
    .replace(/[ñ]/g,'n').replace(/[Ñ]/g,'N')
}
function s(v) { return sanitize(v) }

// Cabeçalho azul:
doc.setFillColor(26,63,168); doc.rect(0,0,W,38,'F')
doc.setFillColor(240,90,26); doc.rect(0,38,W,2,'F')

// Rodapé em todas as páginas:
const pages = doc.internal.getNumberOfPages()
for(let i=1;i<=pages;i++) {
  doc.setPage(i)
  doc.setFillColor(26,63,168); doc.rect(0,H-12,W,12,'F')
  doc.setTextColor(255,255,255); doc.setFont('helvetica','normal'); doc.setFontSize(7)
  doc.text('AssistHub  |  assisthub.com.br',M,H-4.5)
  doc.text(`Pagina ${i} de ${pages}`,W-M,H-4.5,{align:'right'})
}
```

De mp-react/src/pages/AdminPage.jsx — getLucro já definido (copiar a lógica internamente):
```javascript
function getLucro(r) {
  const mos = parseFloat(r.mo_seguradora)      || 0
  const vpt = parseFloat(r.valor_prestador)    || 0
  const vds = parseFloat(r.valor_deslocamento) || 0
  return (mos || vpt || vds) ? (mos - vpt) + vds : null
}
```

STATUS_META labels (do AdminPage) — usar para traduzir status no PDF:
```javascript
const STATUS_LABELS = {
  aguardando_tecnico: 'Aguardando Tecnico',
  pendente:           'Pendente',
  processado:         'Processado',
  enviado:            'Enviado',
}
```

Firestore — estrutura do OS doc (campos relevantes para o relatório):
```
criado_em:          Firestore Timestamp — usar .toDate() para filtrar por mês/ano
status:             string — 'aguardando_tecnico' | 'pendente' | 'processado' | 'enviado'
seguradora:         string — nome da seguradora (Tempo, Mapfre, Maxpar, Allianz, ou vazio)
tecnico_nome:       string — nome do técnico (ou vazio)
mo_seguradora:      number — mão de obra paga pela seguradora
valor_prestador:    number — custo do prestador
valor_deslocamento: number — valor de deslocamento
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar mp-react/src/utils/relatorioMensalPdf.js</name>
  <files>mp-react/src/utils/relatorioMensalPdf.js</files>

  <read_first>
    - mp-react/src/utils/pdfGenerator.js — padrão de sanitize, helpers jsPDF, cabeçalho e rodapé (linhas 1-30 e 180-191)
    - mp-react/src/utils/orcamentoPdfGenerator.js — padrão de criarDoc, sectionHeader, twoCol, textBlock (linhas 37-89)
    - mp-react/src/pages/AdminPage.jsx linhas 73-83 — getLucro() para replicar a lógica
  </read_first>

  <action>
Criar o arquivo `mp-react/src/utils/relatorioMensalPdf.js` com o seguinte conteúdo exato:

```javascript
import jsPDF from 'jspdf'

const MESES = ['Janeiro','Fevereiro','Marco','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro']

const STATUS_LABELS = {
  aguardando_tecnico: 'Aguardando Tecnico',
  pendente:           'Pendente',
  processado:         'Processado',
  enviado:            'Enviado',
}

function s(v) {
  if (!v && v !== 0) return '—'
  return String(v)
    .replace(/[áàãâä]/g,'a').replace(/[ÁÀÃÂÄ]/g,'A')
    .replace(/[éèêë]/g,'e').replace(/[ÉÈÊË]/g,'E')
    .replace(/[íìîï]/g,'i').replace(/[ÍÌÎÏ]/g,'I')
    .replace(/[óòõôö]/g,'o').replace(/[ÓÒÕÔÖ]/g,'O')
    .replace(/[úùûü]/g,'u').replace(/[ÚÙÛÜ]/g,'U')
    .replace(/[c]/g,'c').replace(/[C]/g,'C')
    .replace(/[ñ]/g,'n').replace(/[Ñ]/g,'N')
}

function fmtBRL(v) {
  const n = parseFloat(v)
  if (isNaN(n)) return '—'
  return `R$ ${n.toFixed(2).replace('.',',')}`
}

function getLucroLocal(r) {
  const mos = parseFloat(r.mo_seguradora)      || 0
  const vpt = parseFloat(r.valor_prestador)    || 0
  const vds = parseFloat(r.valor_deslocamento) || 0
  return (mos || vpt || vds) ? (mos - vpt) + vds : null
}

export function agregarRelatorio(os, mes, ano) {
  const filtered = os.filter(r => {
    try {
      const d = r.criado_em?.toDate?.()
      return d && d.getMonth() + 1 === mes && d.getFullYear() === ano
    } catch { return false }
  })

  const porStatus    = {}
  const porSeguradora = {}
  const porTecnico    = {}
  let lucroTotal      = 0
  let osComLucro      = 0

  filtered.forEach(r => {
    const status = r.status || 'pendente'
    porStatus[status] = (porStatus[status] || 0) + 1

    const seg = r.seguradora || '(sem seguradora)'
    if (!porSeguradora[seg]) porSeguradora[seg] = { count: 0, lucro: 0 }
    porSeguradora[seg].count++

    const tec = r.tecnico_nome || '(sem tecnico)'
    if (!porTecnico[tec]) porTecnico[tec] = { count: 0, lucro: 0 }
    porTecnico[tec].count++

    const lucro = getLucroLocal(r)
    if (lucro !== null) {
      lucroTotal += lucro
      osComLucro++
      porSeguradora[seg].lucro += lucro
      porTecnico[tec].lucro   += lucro
    }
  })

  return {
    totalOS:      filtered.length,
    porStatus,
    lucroTotal,
    mediaPorOS:   osComLucro > 0 ? lucroTotal / osComLucro : 0,
    osComLucro,
    porSeguradora,
    porTecnico,
  }
}

export function gerarRelatorioMensalPdf(dados, mes, ano, nomeEmpresa) {
  const doc = new jsPDF({ unit: 'mm', format: 'a4' })
  const W   = doc.internal.pageSize.getWidth()
  const H   = doc.internal.pageSize.getHeight()
  const M   = 14
  let y     = 0

  function checkPage(needed = 20) {
    if (y > H - needed) { doc.addPage(); y = 16 }
  }

  function sectionHeader(title) {
    checkPage(14)
    doc.setFillColor(240,245,252)
    doc.rect(M, y, W-M*2, 7, 'F')
    doc.setDrawColor(208,218,232)
    doc.rect(M, y, W-M*2, 7, 'S')
    doc.setTextColor(26,63,168)
    doc.setFont('helvetica','bold')
    doc.setFontSize(8)
    doc.text(title.toUpperCase(), M+3, y+4.8)
    y += 11
  }

  function twoCol(l1, v1, l2, v2) {
    checkPage(12)
    const hw = (W-M*2)/2
    doc.setFont('helvetica','bold'); doc.setFontSize(7); doc.setTextColor(107,124,147)
    doc.text(l1.toUpperCase(), M, y)
    if (l2) doc.text(l2.toUpperCase(), M+hw, y)
    const lines1 = doc.splitTextToSize(s(v1), hw-4)
    const lines2 = l2 ? doc.splitTextToSize(s(v2), hw-4) : []
    doc.setFont('helvetica','normal'); doc.setFontSize(9); doc.setTextColor(30,45,61)
    doc.text(lines1, M, y+4)
    if (l2) doc.text(lines2, M+hw, y+4)
    y += Math.max(lines1.length, lines2.length||1)*4+6
  }

  function tableRow(col1, col2, col3, isHeader) {
    checkPage(10)
    const cW = (W-M*2) / 3
    if (isHeader) {
      doc.setFillColor(26,63,168); doc.rect(M, y, W-M*2, 7, 'F')
      doc.setTextColor(255,255,255); doc.setFont('helvetica','bold'); doc.setFontSize(7.5)
    } else {
      doc.setFillColor(245,248,252); doc.rect(M, y, W-M*2, 7, 'F')
      doc.setDrawColor(220,228,240); doc.line(M, y+7, M+W-M*2, y+7)
      doc.setTextColor(30,45,61); doc.setFont('helvetica','normal'); doc.setFontSize(8.5)
    }
    doc.text(s(col1), M+3,     y+4.8)
    doc.text(s(col2), M+cW+3,  y+4.8)
    doc.text(s(col3), M+cW*2+3, y+4.8)
    y += 7
  }

  // ── CABEÇALHO ────────────────────────────────────────────────
  doc.setFillColor(26,63,168)
  doc.rect(0, 0, W, 38, 'F')
  doc.setFillColor(240,90,26)
  doc.rect(0, 38, W, 2, 'F')

  doc.setTextColor(255,255,255)
  doc.setFont('helvetica','bold')
  doc.setFontSize(20)
  doc.text(s(nomeEmpresa || 'AssistHub'), M, 16)

  doc.setFontSize(9); doc.setFont('helvetica','normal')
  doc.text('Relatorio Mensal de Atendimentos', M, 24)

  doc.setFontSize(8)
  doc.text(`${MESES[mes-1]} / ${ano}`, W-M, 16, { align: 'right' })
  doc.text(`Total de OS: ${dados.totalOS}`, W-M, 24, { align: 'right' })

  y = 46

  // ── RESUMO GERAL ─────────────────────────────────────────────
  sectionHeader('Resumo Geral')
  twoCol('Total de OS no periodo', String(dados.totalOS), 'OS com dados financeiros', String(dados.osComLucro))
  twoCol('Lucro total', fmtBRL(dados.lucroTotal), 'Media por OS', fmtBRL(dados.mediaPorOS))

  // ── STATUS ──────────────────────────────────────────────────
  sectionHeader('OS por Status')
  tableRow('Status', 'Quantidade', 'Percentual', true)
  const total = dados.totalOS || 1
  Object.entries(dados.porStatus).forEach(([status, count]) => {
    const pct = `${((count / total) * 100).toFixed(1)}%`
    tableRow(STATUS_LABELS[status] || status, String(count), pct, false)
  })
  y += 4

  // ── LUCRATIVIDADE POR SEGURADORA ─────────────────────────────
  sectionHeader('Lucratividade por Seguradora (REL-03)')
  tableRow('Seguradora', 'OS', 'Lucro Total', true)
  Object.entries(dados.porSeguradora)
    .sort((a, b) => b[1].lucro - a[1].lucro)
    .forEach(([seg, { count, lucro }]) => {
      tableRow(seg, String(count), fmtBRL(lucro), false)
    })
  y += 4

  // ── OS E LUCRO POR TÉCNICO ────────────────────────────────────
  sectionHeader('OS e Lucro por Tecnico (REL-04)')
  tableRow('Tecnico', 'OS', 'Lucro Total', true)
  Object.entries(dados.porTecnico)
    .sort((a, b) => b[1].lucro - a[1].lucro)
    .forEach(([tec, { count, lucro }]) => {
      tableRow(tec, String(count), fmtBRL(lucro), false)
    })
  y += 4

  // ── RODAPÉ ───────────────────────────────────────────────────
  const pages = doc.internal.getNumberOfPages()
  for (let i = 1; i <= pages; i++) {
    doc.setPage(i)
    doc.setFillColor(26,63,168); doc.rect(0, H-12, W, 12, 'F')
    doc.setTextColor(255,255,255); doc.setFont('helvetica','normal'); doc.setFontSize(7)
    doc.text(`${s(nomeEmpresa || 'AssistHub')}  |  Relatorio gerado em ${new Date().toLocaleDateString('pt-BR')}`, M, H-4.5)
    doc.text(`Pagina ${i} de ${pages}`, W-M, H-4.5, { align: 'right' })
  }

  doc.save(`relatorio_${String(mes).padStart(2,'0')}_${ano}.pdf`)
}
```

ATENÇÃO: O campo `ç` na função `s()` usa a letra 'c' (minúsculo) como replacement — verificar que
está correto: `.replace(/[ç]/g,'c').replace(/[Ç]/g,'C')` — ambas as linhas devem usar 'c' e 'C' respectivamente.

Salvar o arquivo em `mp-react/src/utils/relatorioMensalPdf.js`. Não criar nenhum outro arquivo.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('mp-react/src/utils/relatorioMensalPdf.js','utf8');const checks=['export function agregarRelatorio','export function gerarRelatorioMensalPdf','getLucroLocal','porStatus','porSeguradora','porTecnico','lucroTotal','mediaPorOS','doc\\.save','relatorio_'];const failed=checks.filter(p=>!new RegExp(p).test(c));if(failed.length){console.error('MISSING:',failed);process.exit(1)}else{console.log('ALL CHECKS PASSED')}"</automated>
  </verify>

  <acceptance_criteria>
    - `mp-react/src/utils/relatorioMensalPdf.js` existe
    - `grep -c "export function agregarRelatorio" mp-react/src/utils/relatorioMensalPdf.js` retorna 1
    - `grep -c "export function gerarRelatorioMensalPdf" mp-react/src/utils/relatorioMensalPdf.js` retorna 1
    - `grep -c "getLucroLocal" mp-react/src/utils/relatorioMensalPdf.js` >= 2 (definição + uso)
    - `grep -c "porStatus" mp-react/src/utils/relatorioMensalPdf.js` >= 3 (init + forEach + return)
    - `grep -c "porSeguradora" mp-react/src/utils/relatorioMensalPdf.js` >= 3 (init + forEach + return)
    - `grep -c "porTecnico" mp-react/src/utils/relatorioMensalPdf.js` >= 3 (init + forEach + return)
    - `grep "doc.save" mp-react/src/utils/relatorioMensalPdf.js` mostra `relatorio_`
    - `grep "mediaPorOS" mp-react/src/utils/relatorioMensalPdf.js` — presente no return de agregarRelatorio
    - O arquivo importa apenas 'jspdf' — sem imports de firebase ou react
    - Build Vite não reporta erro de parse: `cd mp-react && npx vite build 2>&1 | tail -5`
  </acceptance_criteria>

  <done>
    mp-react/src/utils/relatorioMensalPdf.js criado com agregarRelatorio (filtragem + agregação completa) e
    gerarRelatorioMensalPdf (PDF A4 via jsPDF com cabeçalho azul, seções de status/seguradora/técnico e rodapé).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reports state → agregarRelatorio | Dados já carregados do Firestore via SDK autenticado — confiáveis |
| gerarRelatorioMensalPdf → doc.save() | Geração client-side no browser — sem chamada a servidor externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | PDF gerado localmente com dados financeiros | accept | PDF é salvo no filesystem do usuário (admin autenticado); não é enviado a nenhum servidor; o download é intencional |
| T-06-02 | Tampering | s() sanitize remove caracteres especiais | accept | Remoção de acentos é para compatibilidade com jsPDF helvetica, não filtro de segurança — dados financeiros não passam por output HTML |
</threat_model>

<verification>
Verificações após Task 1 (executar na raiz do monorepo):

```bash
# 1. Arquivo criado
test -f mp-react/src/utils/relatorioMensalPdf.js && echo "OK: arquivo existe"

# 2. Ambas as exports presentes
grep -c "export function" mp-react/src/utils/relatorioMensalPdf.js

# 3. Agregação inclui todos os breakdowns exigidos (REL-02..04)
grep -c "porStatus\|porSeguradora\|porTecnico" mp-react/src/utils/relatorioMensalPdf.js

# 4. PDF salvo com nome correto (REL-05)
grep "doc.save" mp-react/src/utils/relatorioMensalPdf.js

# 5. Sem imports externos além de jsPDF
grep "^import" mp-react/src/utils/relatorioMensalPdf.js

# 6. Build sem erros
cd mp-react && npx vite build 2>&1 | tail -5
```
</verification>

<success_criteria>
- [ ] `mp-react/src/utils/relatorioMensalPdf.js` criado com conteúdo correto
- [ ] `agregarRelatorio(os, mes, ano)` filtra por criado_em.toDate() e retorna objeto com todos os campos
- [ ] `gerarRelatorioMensalPdf(dados, mes, ano, nomeEmpresa)` gera e baixa PDF sem chamada de rede (REL-05)
- [ ] PDF contém seções: resumo geral, por status (REL-02), por seguradora (REL-03), por técnico (REL-04)
- [ ] `getLucroLocal` replica exatamente `getLucro` do AdminPage: `(mos - vpt) + vds`
- [ ] Caracteres acentuados sanitizados via função `s()` antes de entrar no jsPDF
- [ ] Arquivo importa apenas `jsPDF` — zero dependências adicionais
- [ ] `npm run build` (dentro de mp-react/) conclui sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-relatorio-mensal-em-pdf/06-01-SUMMARY.md` com:
- Arquivo criado: mp-react/src/utils/relatorioMensalPdf.js
- Requisitos atendidos: REL-02, REL-03, REL-04, REL-05
- Decisão: getLucroLocal copiado do AdminPage para manter consistência sem criar dependência circular
- Decisão: agregarRelatorio filtra client-side (in-memory) — reports já carregados pelo AdminPage, sem nova query Firestore
- Decisão: tableRow helper em vez de textBlock para tabelas de breakdown — melhor visualização de dados estruturados
</output>

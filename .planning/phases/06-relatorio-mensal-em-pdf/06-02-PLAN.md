---
phase: 06-relatorio-mensal-em-pdf
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - mp-react/src/pages/AdminPage.jsx
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05]

must_haves:
  truths:
    - "Sidebar contém item '{ id: relatorio, icon: 📊, label: Relatório Mensal }' entre 'config' e o array existente"
    - "abaAtiva === 'relatorio' renderiza um <div className='tab-content'> com seletor de mês/ano e tabelas de resultado"
    - "O seletor de mês/ano usa <select> com 13 options: uma placeholder + 12 meses (formato 'Mês AAAA') indo do mês atual até 11 meses atrás"
    - "Botão 'Gerar Relatório' chama agregarRelatorio(reports, relMes, relAno) e armazena resultado em relDados"
    - "Quando relDados existe, as tabelas de status, seguradora e técnico ficam visíveis na tela"
    - "Botão 'Baixar PDF' chama gerarRelatorioMensalPdf(relDados, relMes, relAno, empresa?.nome)"
    - "Todos os valores monetários nas tabelas usam fmtBRL já disponível no escopo do AdminPage"
  artifacts:
    - path: "mp-react/src/pages/AdminPage.jsx"
      provides: "Aba 'Relatório Mensal' no painel do admin com seletor, tabelas e download PDF"
      contains: "relatorio"
  key_links:
    - from: "sidebar nav array"
      to: "abaAtiva === 'relatorio'"
      via: "{ id: 'relatorio', icon: '📊', label: 'Relatório Mensal' }"
      pattern: "relatorio"
    - from: "Botão Gerar"
      to: "relDados"
      via: "setRelDados(agregarRelatorio(reports, relMes, relAno))"
      pattern: "agregarRelatorio"
    - from: "Botão Baixar PDF"
      to: "gerarRelatorioMensalPdf"
      via: "gerarRelatorioMensalPdf(relDados, relMes, relAno, empresa?.nome)"
      pattern: "gerarRelatorioMensalPdf"
---

<objective>
Adicionar a aba "Relatório Mensal" ao `AdminPage.jsx` com:
1. Item na sidebar: `{ id: 'relatorio', icon: '📊', label: 'Relatório Mensal' }`
2. Import das funções: `agregarRelatorio` e `gerarRelatorioMensalPdf` de `../utils/relatorioMensalPdf.js`
3. Três novos estados: `relMes` (number, mês 1-12), `relAno` (number, ano 4 dígitos), `relDados` (objeto ou null)
4. Uma função `gerarRelatorio()` que chama `agregarRelatorio(reports, relMes, relAno)` e seta `relDados`
5. Seção JSX `{abaAtiva === 'relatorio' && (...)}` com:
   - `<select>` de mês/ano (13 options — placeholder + 12 meses anteriores incluindo o atual)
   - Botão "📊 Gerar Relatório" que chama `gerarRelatorio()`
   - Tabelas de resultado (quando `relDados` existe): resumo, por status, por seguradora, por técnico
   - Botão "⬇️ Baixar PDF" que chama `gerarRelatorioMensalPdf`

Purpose: Cumprir REL-01 (seletor mês/ano), REL-02 (status), REL-03 (lucro/seguradora), REL-04 (técnico), REL-05 (download PDF).

Output: `AdminPage.jsx` com nova aba funcional e importável sem quebrar as abas existentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contratos relevantes — extraídos da base de código. O executor usa diretamente, sem explorar. -->

De mp-react/src/pages/AdminPage.jsx — linha 214: estado de navegação
```javascript
const [abaAtiva, setAbaAtiva] = useState('dashboard')
```

De mp-react/src/pages/AdminPage.jsx — linhas 1210-1244: sidebar nav array e padrão de item
```javascript
{[
  { id: 'dashboard',  icon: '📊', label: 'Dashboard'         },
  { id: 'agenda',     icon: '📅', label: 'Agenda'            },
  { id: 'os',         icon: '📋', label: 'Ordens de Serviço' },
  { id: 'orcamentos', icon: '📄', label: 'Orçamentos'        },
  { id: 'segurados',  icon: '👥', label: 'Segurados'         },
  { id: 'tecnicos',   icon: '👷', label: 'Técnicos'          },
  { id: 'config',     icon: '⚙️', label: 'Configurações'     },
].map(item => (
  <button
    key={item.id}
    className={`sidebar-item${abaAtiva === item.id ? ' active' : ''}`}
    onClick={() => {
      if (item.id === 'agenda') { navigate(`/${slug}/agenda`); setSidebarOpen(false) }
      else { setAbaAtiva(item.id); setSidebarOpen(false) }
    }}
  >
    <span className="si-icon">{item.icon}</span>
    <span className="si-label">{item.label}</span>
    {/* ... badges condicionais ... */}
  </button>
))}
```

De mp-react/src/pages/AdminPage.jsx — padrão de seção de aba (usar tab-content e filter-bar):
```javascript
{abaAtiva === 'os' && (
  <div className="tab-content">
    <div className="filter-bar" style={{ marginBottom: 20 }}>
      ...
    </div>
    ...
  </div>
)}
```

CSS disponível no escopo (sem adicionar novas classes — usar apenas as existentes):
- `.tab-content` — container principal de cada aba
- `.filter-bar` — barra de filtros/controles no topo
- `.filter-label` — label antes dos controles
- `.filter-input` — select/input de filtro
- `.btn-primary` — botão azul principal (gerar relatório)
- `.metrics-grid` / `.metric-card` — grid de cartões de resumo
- `.metric-icon`, `.metric-label`, `.metric-value`, `.metric-sub` — partes do cartão
- `.empty-state`, `.e-icon` — estado vazio
- `.loading-state`, `.spinner`, `.loading-text` — loading
- `fmtBRL(v)` — função já no escopo do componente (linha 73)
- `empresa?.nome` — nome da empresa já disponível via useEmpresa()

fmtBRL já disponível no componente (linha 73-77 do AdminPage.jsx):
```javascript
function fmtBRL(v) {
  const n = parseFloat(v)
  if (isNaN(n)) return '—'
  return `R$ ${n.toFixed(2).replace('.', ',')}`
}
```

Objeto reports: array de OS carregados do Firestore, disponível no escopo do componente.

Para o select de mês/ano com os últimos 12 meses (REL-01 — "qualquer período dos últimos 12 meses"):
```javascript
// Gerar 12 meses incluindo o atual (do mais recente ao mais antigo)
const MESES_NOMES = ['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro']
const now = new Date()
const opcoesMes = []
for (let i = 0; i < 12; i++) {
  const d = new Date(now.getFullYear(), now.getMonth() - i, 1)
  opcoesMes.push({ mes: d.getMonth() + 1, ano: d.getFullYear(), label: `${MESES_NOMES[d.getMonth()]} ${d.getFullYear()}` })
}
// Valor do select: string `${mes}-${ano}` ex: "4-2026"
// Ao mudar: parse e setar relMes + relAno
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar import de relatorioMensalPdf.js ao AdminPage.jsx</name>
  <files>mp-react/src/pages/AdminPage.jsx</files>

  <read_first>
    - mp-react/src/pages/AdminPage.jsx linhas 1-20 — verificar imports existentes para inserir no lugar certo
    - mp-react/src/utils/relatorioMensalPdf.js — confirmar que o arquivo existe e os exports estão corretos
  </read_first>

  <action>
Localizar o bloco de imports no topo do AdminPage.jsx (linhas 1-20). Adicionar o seguinte import
APÓS o último import existente (mas antes da primeira linha de código):

```javascript
import { agregarRelatorio, gerarRelatorioMensalPdf } from '../utils/relatorioMensalPdf.js'
```

Não alterar nenhum outro import. Não mover nenhuma linha existente.
  </action>

  <acceptance_criteria>
    - `grep -c "agregarRelatorio" mp-react/src/pages/AdminPage.jsx` >= 1 (import presente)
    - `grep -c "gerarRelatorioMensalPdf" mp-react/src/pages/AdminPage.jsx` >= 1 (import presente)
    - `grep "from '../utils/relatorioMensalPdf" mp-react/src/pages/AdminPage.jsx` — linha existe
  </acceptance_criteria>

  <done>Import de agregarRelatorio e gerarRelatorioMensalPdf adicionado ao AdminPage.jsx.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar estados relMes, relAno, relDados ao AdminPage.jsx</name>
  <files>mp-react/src/pages/AdminPage.jsx</files>

  <read_first>
    - mp-react/src/pages/AdminPage.jsx linha 214 — const [abaAtiva, setAbaAtiva] = useState('dashboard')
      Este é o ponto de referência para inserir os novos estados.
    - mp-react/src/pages/AdminPage.jsx linhas 214-280 — bloco de useState para escolher posição de inserção
  </read_first>

  <action>
Localizar a linha:
```javascript
const [abaAtiva,     setAbaAtiva]     = useState('dashboard')
```

Inserir as seguintes três linhas IMEDIATAMENTE APÓS essa linha (na linha seguinte):

```javascript
  const [relMes,       setRelMes]       = useState(new Date().getMonth() + 1)
  const [relAno,       setRelAno]       = useState(new Date().getFullYear())
  const [relDados,     setRelDados]     = useState(null)
```

`relMes` inicia no mês atual (1-12). `relAno` inicia no ano atual. `relDados` inicia null (sem dados gerados ainda).
  </action>

  <acceptance_criteria>
    - `grep -c "relMes" mp-react/src/pages/AdminPage.jsx` >= 2 (useState + setRelMes)
    - `grep -c "relAno" mp-react/src/pages/AdminPage.jsx` >= 2 (useState + setRelAno)
    - `grep -c "relDados" mp-react/src/pages/AdminPage.jsx` >= 2 (useState + setRelDados)
    - `grep "useState(new Date().getMonth" mp-react/src/pages/AdminPage.jsx` — linha existe
  </acceptance_criteria>

  <done>Estados relMes, relAno e relDados adicionados ao AdminPage.jsx.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar função gerarRelatorio e item 'relatorio' na sidebar</name>
  <files>mp-react/src/pages/AdminPage.jsx</files>

  <read_first>
    - mp-react/src/pages/AdminPage.jsx — localizar função `function buildLink(r)` ou qualquer função
      helper antes do return para encontrar onde inserir a função gerarRelatorio
    - mp-react/src/pages/AdminPage.jsx linhas 1210-1218 — array de itens da sidebar para adicionar o item
  </read_first>

  <action>
**Alteração 3a — Função gerarRelatorio:**

Localizar qualquer uma das funções helper antes do `return (` do componente (ex: `function buildLink`, `function buildLinkTecnicoOrc`, `function copyLink`). Inserir a seguinte função ANTES do `return (`:

```javascript
  function gerarRelatorio() {
    setRelDados(agregarRelatorio(reports, relMes, relAno))
  }
```

---

**Alteração 3b — Item na sidebar:**

Localizar o array de itens da sidebar:
```javascript
  { id: 'tecnicos',   icon: '👷', label: 'Técnicos'          },
  { id: 'config',     icon: '⚙️', label: 'Configurações'     },
```

Inserir o item 'relatorio' ENTRE 'config' e o fechamento `]` do array:
```javascript
  { id: 'tecnicos',   icon: '👷', label: 'Técnicos'          },
  { id: 'config',     icon: '⚙️', label: 'Configurações'     },
  { id: 'relatorio',  icon: '📊', label: 'Relatório Mensal'  },
```

Não alterar nenhum outro item existente. Não modificar a lógica do `.map()`.
  </action>

  <acceptance_criteria>
    - `grep -c "gerarRelatorio" mp-react/src/pages/AdminPage.jsx` >= 2 (definição + chamada no JSX)
    - `grep "id: 'relatorio'" mp-react/src/pages/AdminPage.jsx` — linha existe no array da sidebar
    - `grep "Relatório Mensal" mp-react/src/pages/AdminPage.jsx` — label existe
    - `grep "agregarRelatorio(reports" mp-react/src/pages/AdminPage.jsx` — chamada com reports na função
  </acceptance_criteria>

  <done>Função gerarRelatorio() adicionada e item 'relatorio' inserido na sidebar do AdminPage.</done>
</task>

<task type="auto">
  <name>Task 4: Adicionar seção JSX {abaAtiva === 'relatorio'} ao AdminPage.jsx</name>
  <files>mp-react/src/pages/AdminPage.jsx</files>

  <read_first>
    - mp-react/src/pages/AdminPage.jsx — localizar o ÚLTIMO bloco `{abaAtiva === 'config' && (...)}` para inserir
      a nova seção de 'relatorio' APÓS o fechamento desse bloco (mantendo a estrutura de seções condicionais)
    - mp-react/src/pages/AdminPage.jsx linhas 1795-1800 — início do bloco config para referência
  </read_first>

  <action>
Localizar o fechamento do bloco `{abaAtiva === 'config' && (...)}` — é o `)}` que fecha esse bloco condicional.
Inserir a seguinte seção JSX APÓS esse fechamento (não dentro do bloco config):

```jsx
        {/* ══ RELATÓRIO MENSAL ══════════════════════════════════════ */}
        {abaAtiva === 'relatorio' && (
          <div className="tab-content">

            {/* Seletor de período */}
            <div className="filter-bar" style={{ marginBottom: 24, flexWrap: 'wrap', gap: 12 }}>
              <span className="filter-label">Período:</span>
              <select
                className="filter-input"
                value={`${relMes}-${relAno}`}
                onChange={e => {
                  const [m, a] = e.target.value.split('-').map(Number)
                  setRelMes(m)
                  setRelAno(a)
                  setRelDados(null)
                }}
              >
                {(() => {
                  const MESES_NOMES = ['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro']
                  const now = new Date()
                  const opts = []
                  for (let i = 0; i < 12; i++) {
                    const d = new Date(now.getFullYear(), now.getMonth() - i, 1)
                    const m = d.getMonth() + 1
                    const a = d.getFullYear()
                    opts.push(<option key={`${m}-${a}`} value={`${m}-${a}`}>{MESES_NOMES[d.getMonth()]} {a}</option>)
                  }
                  return opts
                })()}
              </select>
              <button
                className="btn-primary"
                onClick={gerarRelatorio}
                style={{ minWidth: 160 }}
              >
                📊 Gerar Relatório
              </button>
            </div>

            {/* Estado vazio — ainda não gerou */}
            {!relDados && (
              <div className="empty-state">
                <div className="e-icon">📊</div>
                <p>Selecione um período e clique em <strong>Gerar Relatório</strong> para ver os dados consolidados.</p>
              </div>
            )}

            {/* Resultados */}
            {relDados && (
              <>
                {/* Resumo geral — cartões de métricas */}
                <div className="metrics-grid" style={{ marginBottom: 24 }}>
                  <div className="metric-card blue">
                    <div className="metric-icon">📋</div>
                    <div className="metric-label">Total de OS</div>
                    <div className="metric-value">{relDados.totalOS}</div>
                    <div className="metric-sub">no período</div>
                  </div>
                  <div className="metric-card orange">
                    <div className="metric-icon">💼</div>
                    <div className="metric-label">Com Financeiro</div>
                    <div className="metric-value">{relDados.osComLucro}</div>
                    <div className="metric-sub">OS com dados de lucro</div>
                  </div>
                  <div className="metric-card green">
                    <div className="metric-icon">💰</div>
                    <div className="metric-label">Lucro Total</div>
                    <div className={`metric-value${relDados.lucroTotal >= 0 ? ' green' : ' red'}`}>{fmtBRL(relDados.lucroTotal)}</div>
                    <div className="metric-sub">no período</div>
                  </div>
                  <div className="metric-card yellow">
                    <div className="metric-icon">📈</div>
                    <div className="metric-label">Média por OS</div>
                    <div className="metric-value">{fmtBRL(relDados.mediaPorOS)}</div>
                    <div className="metric-sub">OS com financeiro</div>
                  </div>
                </div>

                {/* Tabela por status (REL-02) */}
                <div style={{ marginBottom: 24 }}>
                  <div style={{ fontWeight: 700, fontSize: '1rem', color: 'var(--primary)', marginBottom: 10 }}>📋 OS por Status</div>
                  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '.88rem' }}>
                    <thead>
                      <tr style={{ background: 'var(--primary)', color: '#fff' }}>
                        <th style={{ padding: '8px 12px', textAlign: 'left', borderRadius: '6px 0 0 0' }}>Status</th>
                        <th style={{ padding: '8px 12px', textAlign: 'right' }}>Quantidade</th>
                        <th style={{ padding: '8px 12px', textAlign: 'right', borderRadius: '0 6px 0 0' }}>Percentual</th>
                      </tr>
                    </thead>
                    <tbody>
                      {Object.entries(relDados.porStatus).map(([status, count], idx) => {
                        const pct = relDados.totalOS > 0 ? ((count / relDados.totalOS) * 100).toFixed(1) : '0.0'
                        const labels = { aguardando_tecnico: '🔔 Aguardando Técnico', pendente: '⏳ Pendente', processado: '✅ Processado', enviado: '📤 Enviado' }
                        return (
                          <tr key={status} style={{ background: idx % 2 === 0 ? 'var(--light)' : '#fff', borderBottom: '1px solid var(--border)' }}>
                            <td style={{ padding: '7px 12px' }}>{labels[status] || status}</td>
                            <td style={{ padding: '7px 12px', textAlign: 'right', fontWeight: 700 }}>{count}</td>
                            <td style={{ padding: '7px 12px', textAlign: 'right', color: 'var(--muted)' }}>{pct}%</td>
                          </tr>
                        )
                      })}
                    </tbody>
                  </table>
                </div>

                {/* Tabela por seguradora (REL-03) */}
                <div style={{ marginBottom: 24 }}>
                  <div style={{ fontWeight: 700, fontSize: '1rem', color: 'var(--primary)', marginBottom: 10 }}>🏢 Por Seguradora</div>
                  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '.88rem' }}>
                    <thead>
                      <tr style={{ background: 'var(--primary)', color: '#fff' }}>
                        <th style={{ padding: '8px 12px', textAlign: 'left', borderRadius: '6px 0 0 0' }}>Seguradora</th>
                        <th style={{ padding: '8px 12px', textAlign: 'right' }}>OS</th>
                        <th style={{ padding: '8px 12px', textAlign: 'right', borderRadius: '0 6px 0 0' }}>Lucro Total</th>
                      </tr>
                    </thead>
                    <tbody>
                      {Object.entries(relDados.porSeguradora)
                        .sort((a, b) => b[1].lucro - a[1].lucro)
                        .map(([seg, { count, lucro }], idx) => (
                          <tr key={seg} style={{ background: idx % 2 === 0 ? 'var(--light)' : '#fff', borderBottom: '1px solid var(--border)' }}>
                            <td style={{ padding: '7px 12px' }}>{seg}</td>
                            <td style={{ padding: '7px 12px', textAlign: 'right', fontWeight: 700 }}>{count}</td>
                            <td style={{ padding: '7px 12px', textAlign: 'right', color: lucro >= 0 ? 'var(--success)' : 'var(--danger)', fontWeight: 700 }}>{fmtBRL(lucro)}</td>
                          </tr>
                        ))}
                    </tbody>
                  </table>
                </div>

                {/* Tabela por técnico (REL-04) */}
                <div style={{ marginBottom: 24 }}>
                  <div style={{ fontWeight: 700, fontSize: '1rem', color: 'var(--primary)', marginBottom: 10 }}>👷 Por Técnico</div>
                  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '.88rem' }}>
                    <thead>
                      <tr style={{ background: 'var(--primary)', color: '#fff' }}>
                        <th style={{ padding: '8px 12px', textAlign: 'left', borderRadius: '6px 0 0 0' }}>Técnico</th>
                        <th style={{ padding: '8px 12px', textAlign: 'right' }}>OS</th>
                        <th style={{ padding: '8px 12px', textAlign: 'right', borderRadius: '0 6px 0 0' }}>Lucro Total</th>
                      </tr>
                    </thead>
                    <tbody>
                      {Object.entries(relDados.porTecnico)
                        .sort((a, b) => b[1].lucro - a[1].lucro)
                        .map(([tec, { count, lucro }], idx) => (
                          <tr key={tec} style={{ background: idx % 2 === 0 ? 'var(--light)' : '#fff', borderBottom: '1px solid var(--border)' }}>
                            <td style={{ padding: '7px 12px' }}>{tec}</td>
                            <td style={{ padding: '7px 12px', textAlign: 'right', fontWeight: 700 }}>{count}</td>
                            <td style={{ padding: '7px 12px', textAlign: 'right', color: lucro >= 0 ? 'var(--success)' : 'var(--danger)', fontWeight: 700 }}>{fmtBRL(lucro)}</td>
                          </tr>
                        ))}
                    </tbody>
                  </table>
                </div>

                {/* Botão download PDF (REL-05) */}
                <div style={{ display: 'flex', gap: 12, marginTop: 8 }}>
                  <button
                    className="btn-primary"
                    onClick={() => gerarRelatorioMensalPdf(relDados, relMes, relAno, empresa?.nome)}
                  >
                    ⬇️ Baixar PDF
                  </button>
                  <button
                    className="btn-sm btn-view"
                    onClick={() => setRelDados(null)}
                  >
                    ✕ Limpar
                  </button>
                </div>
              </>
            )}
          </div>
        )}
```

Inserir este bloco após o fechamento `)}` do bloco `{abaAtiva === 'config' && (...)}`.
Garantir que está dentro do elemento pai `<main className="admin-main">` (ou equivalente) e no mesmo nível
das outras seções condicionais.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('mp-react/src/pages/AdminPage.jsx','utf8');const checks=[\"abaAtiva === 'relatorio'\",\"gerarRelatorio\\\\(\",\"gerarRelatorioMensalPdf\\\\(\",\"porStatus\",\"porSeguradora\",\"porTecnico\",\"relDados\",\"relMes\",\"relAno\",\"Relatório Mensal\",\"Baixar PDF\",\"Gerar Relatório\",\"mediaPorOS\",\"osComLucro\"];const failed=checks.filter(p=>!new RegExp(p).test(c));if(failed.length){console.error('MISSING:',failed);process.exit(1)}else{console.log('ALL CHECKS PASSED')}"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "abaAtiva === 'relatorio'" mp-react/src/pages/AdminPage.jsx` >= 1
    - `grep -c "gerarRelatorioMensalPdf" mp-react/src/pages/AdminPage.jsx` >= 2 (import + chamada)
    - `grep -c "agregarRelatorio" mp-react/src/pages/AdminPage.jsx` >= 2 (import + chamada)
    - `grep -c "relDados" mp-react/src/pages/AdminPage.jsx` >= 4 (useState + setter + condição + uso nas tabelas)
    - `grep -c "Baixar PDF" mp-react/src/pages/AdminPage.jsx` >= 1 (botão de download)
    - `grep -c "Gerar Relatório" mp-react/src/pages/AdminPage.jsx` >= 1 (botão de gerar)
    - `grep -c "porStatus" mp-react/src/pages/AdminPage.jsx` >= 1 (tabela de status no JSX)
    - `grep -c "porSeguradora" mp-react/src/pages/AdminPage.jsx` >= 1 (tabela de seguradora no JSX)
    - `grep -c "porTecnico" mp-react/src/pages/AdminPage.jsx` >= 1 (tabela de técnico no JSX)
    - `grep "id: 'relatorio'" mp-react/src/pages/AdminPage.jsx` — item na sidebar presente
    - `cd mp-react && npm run build 2>&1 | tail -5` — sem erros de build
  </acceptance_criteria>

  <done>
    Seção JSX {abaAtiva === 'relatorio'} adicionada ao AdminPage.jsx com: seletor de mês/ano (12 opções),
    botão "Gerar Relatório", cartões de resumo (total OS, com financeiro, lucro total, média), tabelas por
    status/seguradora/técnico, botão "Baixar PDF" chamando gerarRelatorioMensalPdf, botão "✕ Limpar".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verificação visual e funcional da aba Relatório Mensal</name>

  <what-built>
    Tasks 1-4 adicionaram a aba "Relatório Mensal" ao painel admin com seletor de período,
    tabelas de dados agregados e download de PDF via jsPDF.
  </what-built>

  <how-to-verify>
    1. Inicie o dev server: `cd mp-react && npm run dev`
    2. Acesse o painel admin: `http://localhost:5173/{slug}` (use um slug real de empresa)
    3. Confirme que "📊 Relatório Mensal" aparece na sidebar esquerda (último item)
    4. Clique em "Relatório Mensal" — a aba abre sem erros no console
    5. Confirme que o select mostra 12 opções de meses (do mês atual até 11 meses atrás)
    6. Selecione um mês e clique em "📊 Gerar Relatório"
    7. Verifique que aparecem os 4 cartões de métricas: Total OS, Com Financeiro, Lucro Total, Média
    8. Verifique que as tabelas de Status, Seguradora e Técnico aparecem com dados (ou colunas vazias se não há OS no período)
    9. Clique em "⬇️ Baixar PDF" — confirme que o browser faz o download de um arquivo `relatorio_MM_AAAA.pdf`
    10. Abra o PDF e confirme que contém: cabeçalho azul com nome da empresa + mês/ano, seções de resumo, status, seguradora, técnico
    11. Clique "✕ Limpar" — tabelas somem e estado vazio reaparece
    12. Navegue para outras abas (Dashboard, OS, etc.) e confirme que continuam funcionando normalmente

    Resultado esperado: Aba funcional end-to-end — REL-01 (seletor), REL-02 (status), REL-03 (seguradora+lucro),
    REL-04 (técnico), REL-05 (download PDF sem chamada de servidor).
  </how-to-verify>

  <resume-signal>
    Digite "aprovado" se tudo funcionar conforme descrito, ou descreva o problema encontrado.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reports state → agregarRelatorio | Dados carregados do Firestore via SDK autenticado — confiáveis no contexto admin |
| gerarRelatorioMensalPdf → doc.save() | Geração client-side, sem envio de dados externos |
| Admin → select mês/ano | Seletor com valores fixos gerados pelo código — sem input livre do usuário no valor do select |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | PDF com dados financeiros baixado no browser | accept | PDF é salvo localmente pelo admin autenticado; dados financeiros já visíveis no painel admin — sem nova exposição |
| T-06-03 | Spoofing | Seletor de período com valores pré-gerados | accept | Valores do select são gerados por código (`for i < 12`) — não há input livre que possa injetar mês/ano arbitrário; setRelMes/setRelAno recebem apenas números parseados de strings `'M-AAAA'` |
</threat_model>

<verification>
Verificações após Tasks 1-4 (executar na raiz do monorepo):

```bash
# 1. Import das funções adicionado
grep "from '../utils/relatorioMensalPdf" mp-react/src/pages/AdminPage.jsx

# 2. Estados novos presentes
grep -c "relMes\|relAno\|relDados" mp-react/src/pages/AdminPage.jsx

# 3. Item relatorio na sidebar
grep "id: 'relatorio'" mp-react/src/pages/AdminPage.jsx

# 4. Seção condicional existe
grep "abaAtiva === 'relatorio'" mp-react/src/pages/AdminPage.jsx

# 5. Todas as tabelas de breakdown presentes (REL-02, REL-03, REL-04)
grep -c "porStatus\|porSeguradora\|porTecnico" mp-react/src/pages/AdminPage.jsx

# 6. Botão de download existe (REL-05)
grep "Baixar PDF" mp-react/src/pages/AdminPage.jsx

# 7. Build sem erros
cd mp-react && npm run build 2>&1 | tail -8
```
</verification>

<success_criteria>
- [ ] Import de `agregarRelatorio` e `gerarRelatorioMensalPdf` adicionado ao AdminPage.jsx
- [ ] Estados `relMes`, `relAno`, `relDados` adicionados com valores iniciais corretos
- [ ] Função `gerarRelatorio()` chama `agregarRelatorio(reports, relMes, relAno)` e seta `relDados`
- [ ] Item `{ id: 'relatorio', icon: '📊', label: 'Relatório Mensal' }` na sidebar (REL-01)
- [ ] Seletor de mês/ano gera 12 options dinamicamente com o mês atual primeiro (REL-01)
- [ ] Mudar o select limpa `relDados` (força nova geração para o período selecionado)
- [ ] Cartões de métricas: Total OS, OS com financeiro, Lucro Total, Média por OS
- [ ] Tabela de status com colunas: Status | Quantidade | Percentual (REL-02)
- [ ] Tabela de seguradora com colunas: Seguradora | OS | Lucro Total (REL-03)
- [ ] Tabela de técnico com colunas: Técnico | OS | Lucro Total (REL-04)
- [ ] Botão "⬇️ Baixar PDF" chama `gerarRelatorioMensalPdf(relDados, relMes, relAno, empresa?.nome)` (REL-05)
- [ ] `npm run build` (dentro de mp-react/) conclui sem erros
- [ ] Verificação visual (Task 5 checkpoint) aprovada pelo usuário
</success_criteria>

<output>
Após conclusão (incluindo aprovação do checkpoint), criar `.planning/phases/06-relatorio-mensal-em-pdf/06-02-SUMMARY.md` com:
- Arquivos modificados: mp-react/src/pages/AdminPage.jsx
- Requisitos atendidos: REL-01, REL-02, REL-03, REL-04, REL-05
- Decisão: seletor de período com 12 options geradas por loop — sem input livre para evitar valores inválidos
- Decisão: agregarRelatorio filtra client-side nos reports já carregados — sem nova query Firestore
- Decisão: tabelas inline com style props — sem adicionar novas classes CSS conforme orientação do CLAUDE.md
- Decisão: mudar o select limpa relDados para forçar nova geração (evita dados desatualizados)
</output>

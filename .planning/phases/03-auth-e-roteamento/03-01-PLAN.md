---
phase: 3
plan: "03-01"
name: "Testes do AuthContext"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - mp-react/src/__tests__/authContext.test.jsx
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Um teste demonstra que 'Carregando...' aparece enquanto onAuthStateChanged não chamou o callback"
    - "Um teste demonstra que empresaId é resolvido via getDoc(empresas/{uid}) quando o documento existe"
    - "Um teste demonstra que empresaId cai no fallback usuarios/{uid} quando empresas/{uid} não existe"
    - "Um teste demonstra que isSuperAdmin fica true quando getIdTokenResult retorna role=superadmin válido"
    - "Um teste demonstra que usuario e empresaId ficam null após chamar logout()"
  artifacts:
    - path: "mp-react/src/__tests__/authContext.test.jsx"
      provides: "Suite de 5 testes cobrindo AUTH-01..05"
      exports: []
  key_links:
    - from: "authContext.test.jsx"
      to: "src/contexts/AuthContext.jsx"
      via: "import AuthProvider + useAuth"
      pattern: "import.*AuthProvider.*AuthContext"
    - from: "authContext.test.jsx"
      to: "src/firebase (via vi.mock)"
      via: "vi.mock('../firebase') inline com objeto literal"
      pattern: "vi\\.mock\\('../firebase'"
---

<objective>
Criar o arquivo de teste `src/__tests__/authContext.test.jsx` cobrindo os cinco requisitos de comportamento do `AuthProvider`: estado de loading, resolução de empresaId via Firestore (caminho primário e fallback), identificação de superadmin por claims, e limpeza de estado no logout.

Purpose: Garantir que nenhuma mudança no AuthContext quebre silenciosamente os fluxos de autenticação que toda a aplicação multi-tenant depende.

Output: `mp-react/src/__tests__/authContext.test.jsx` com 5 testes passando (um por requisito AUTH-01..05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-e-roteamento/03-RESEARCH.md
</context>

<interfaces>
<!-- Contratos do AuthContext lidos diretamente — executor usa sem explorar o codebase -->

De mp-react/src/contexts/AuthContext.jsx:
```jsx
// Exportações disponíveis:
export const AuthContext = createContext(null)   // usado pelo FakeAuthProvider nos outros testes
export function AuthProvider({ children })       // provider REAL — renderizado diretamente nesses testes
export function useAuth()                        // hook de acesso — usado no componente auxiliar ExporContexto

// Valor fornecido pelo provider:
// { usuario, empresaId, isSuperAdmin, loadingAuth, login, logout, cadastrar, refreshSuperAdminClaim, estaLogado, emailUsuario }

// Comportamento de loading:
// loadingAuth: true → renderiza <div>Carregando...</div> em vez dos children (linha 134-148)
// loadingAuth: false → renderiza children normalmente
```

De mp-react/src/test-utils/mockFirebase.js:
```js
// createFirebaseMocks() retorna — todos os campos relevantes para AuthContext:
// onAuthStateChanged: vi.fn((auth, cb) => { cb(null); return () => {} })
// getDoc: vi.fn()
// signOut: vi.fn()
// doc: vi.fn()
// db: {}, auth: {}
// SUPERADMIN_EMAIL: 'superadmin@test.com'
// NOTA: createFirebaseMocks() NÃO inclui getConfigEmpresa — irrelevante para AuthContext
```

Fluxo real do AuthContext (linhas 46-88 de AuthContext.jsx):
```
onAuthStateChanged(auth, async (user) => {
  if (user) {
    setUsuario(user)
    const superAdmin = await checkSuperAdminClaim(user)  // user.getIdTokenResult()
    if (superAdmin) {
      setIsSuperAdmin(true)
      setEmpresaId(null)
    } else {
      // Caminho 1 (AUTH-02): getDoc('empresas', user.uid) → exists() → setEmpresaId(user.uid)
      // Caminho 2 (AUTH-03): getDoc('empresas', uid) exists=false → getDoc('usuarios', uid) → data().empresaId
    }
  } else {
    // AUTH-05: tudo null
    setUsuario(null); setEmpresaId(null); setIsSuperAdmin(false)
  }
  setLoadingAuth(false)
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar authContext.test.jsx com os 5 testes AUTH-01..05</name>
  <files>mp-react/src/__tests__/authContext.test.jsx</files>

  <read_first>
    - mp-react/src/contexts/AuthContext.jsx — entender o fluxo exato de cada caminho
    - mp-react/src/test-utils/mockFirebase.js — confirmar o que createFirebaseMocks() exporta
    - mp-react/src/__tests__/firebase.test.js — padrão de estilo (2 espaços, aspas simples, sem ponto-e-vírgula)
  </read_first>

  <action>
Criar o arquivo `mp-react/src/__tests__/authContext.test.jsx` com o seguinte conteúdo exato. Usar Padrão C da pesquisa: `vi.mock('../firebase')` com objeto literal inline. `clearMocks: true` limpa call counts mas NÃO `mockImplementation` — reconfigurar por teste via `mockImplementation` no `beforeEach` ou dentro de cada `it`.

Estrutura completa do arquivo (copiar e adaptar, não omitir):

```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, waitFor, act } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { AuthProvider, useAuth } from '../contexts/AuthContext'

// Padrão C: vi.mock inline com objeto literal.
// onAuthStateChanged padrão chama cb(null) síncronamente — simula usuário deslogado.
// Cada teste sobrescreve via .mockImplementation() no próprio it ou no beforeEach.
vi.mock('../firebase', () => ({
  auth: {},
  db: {},
  onAuthStateChanged: vi.fn((auth, cb) => { cb(null); return () => {} }),
  signInWithEmailAndPassword: vi.fn(),
  createUserWithEmailAndPassword: vi.fn(),
  signOut: vi.fn().mockResolvedValue(undefined),
  doc: vi.fn(() => 'mock-doc-ref'),
  getDoc: vi.fn().mockResolvedValue({ exists: () => false }),
  SUPERADMIN_EMAIL: 'superadmin@test.com',
  PLANOS: { basico: { limiteOS: 50, preco: 97 }, pro: { limiteOS: -1, preco: 197 } },
}))

// Importar DEPOIS do vi.mock para receber as versões mockadas
import { onAuthStateChanged, getDoc, signOut } from '../firebase'

// Componente auxiliar — expõe o contexto de auth na DOM para asserções
function ExporContexto() {
  const ctx = useAuth()
  return (
    <div>
      <span data-testid='loading'>{String(ctx.loadingAuth)}</span>
      <span data-testid='empresaId'>{ctx.empresaId ?? 'null'}</span>
      <span data-testid='superadmin'>{String(ctx.isSuperAdmin)}</span>
      <span data-testid='usuario'>{ctx.usuario?.uid ?? 'null'}</span>
      <button onClick={ctx.logout} data-testid='btn-logout'>Sair</button>
    </div>
  )
}

function renderProvider() {
  return render(
    <MemoryRouter>
      <AuthProvider>
        <ExporContexto />
      </AuthProvider>
    </MemoryRouter>
  )
}

// AUTH-01: loading enquanto onAuthStateChanged não chamou o callback
describe('AUTH-01: estado de loading', () => {
  it('exibe Carregando enquanto onAuthStateChanged nao respondeu', () => {
    // Mock NÃO chama o callback — simula o estado pendente inicial
    onAuthStateChanged.mockImplementation(() => () => {})
    const { container } = renderProvider()
    expect(container.textContent).toContain('Carregando')
    // ExporContexto NÃO aparece porque AuthProvider renderiza o fallback
    expect(screen.queryByTestId('loading')).not.toBeInTheDocument()
  })
})

// AUTH-02: empresaId via getDoc(empresas/{uid}) quando existe
describe('AUTH-02: resolucao de empresaId via Firestore (empresas/{uid})', () => {
  it('resolve empresaId quando empresa existe em empresas/uid', async () => {
    const fakeUser = {
      uid: 'user-123',
      email: 'admin@teste.com',
      getIdTokenResult: vi.fn().mockResolvedValue({ claims: {} }),
    }
    onAuthStateChanged.mockImplementation((auth, cb) => { cb(fakeUser); return () => {} })
    // getDoc retorna exists: true para empresas/user-123
    getDoc.mockResolvedValue({ exists: () => true })

    renderProvider()

    await waitFor(() => {
      expect(screen.getByTestId('empresaId').textContent).toBe('user-123')
    })
    expect(screen.getByTestId('usuario').textContent).toBe('user-123')
  })
})

// AUTH-03: fallback para usuarios/{uid} quando empresas/{uid} não existe
describe('AUTH-03: fallback Firestore para usuarios/{uid}', () => {
  it('usa fallback usuarios/uid quando empresa nao existe', async () => {
    const fakeUser = {
      uid: 'user-456',
      email: 'admin@teste.com',
      getIdTokenResult: vi.fn().mockResolvedValue({ claims: {} }),
    }
    onAuthStateChanged.mockImplementation((auth, cb) => { cb(fakeUser); return () => {} })
    // Primeira chamada (empresas/uid) → não existe
    // Segunda chamada (usuarios/uid) → existe com empresaId diferente
    getDoc
      .mockResolvedValueOnce({ exists: () => false })
      .mockResolvedValueOnce({ exists: () => true, data: () => ({ empresaId: 'emp-xyz' }) })

    renderProvider()

    await waitFor(() => {
      expect(screen.getByTestId('empresaId').textContent).toBe('emp-xyz')
    })
  })
})

// AUTH-04: superadmin via claims role + superadmin_until
describe('AUTH-04: identificacao de superadmin via claims', () => {
  it('identifica superadmin quando role=superadmin e superadmin_until nao expirado', async () => {
    const futureTs = Math.floor(Date.now() / 1000) + 3600 // 1h no futuro
    const fakeUser = {
      uid: 'super-001',
      email: 'superadmin@test.com',
      getIdTokenResult: vi.fn().mockResolvedValue({
        claims: { role: 'superadmin', superadmin_until: futureTs },
      }),
    }
    onAuthStateChanged.mockImplementation((auth, cb) => { cb(fakeUser); return () => {} })

    renderProvider()

    await waitFor(() => {
      expect(screen.getByTestId('superadmin').textContent).toBe('true')
    })
    // superadmin não tem empresaId
    expect(screen.getByTestId('empresaId').textContent).toBe('null')
  })
})

// AUTH-05: logout limpa o estado
describe('AUTH-05: logout limpa estado', () => {
  it('limpa usuario e empresaId apos logout', async () => {
    let authCallback
    const fakeUser = {
      uid: 'user-789',
      email: 'admin@teste.com',
      getIdTokenResult: vi.fn().mockResolvedValue({ claims: {} }),
    }

    // Começa logado
    onAuthStateChanged.mockImplementation((auth, cb) => {
      authCallback = cb
      cb(fakeUser)
      return () => {}
    })
    getDoc.mockResolvedValue({ exists: () => true })

    renderProvider()

    // Aguarda resolver o empresaId inicial
    await waitFor(() => {
      expect(screen.getByTestId('usuario').textContent).toBe('user-789')
    })

    // signOut dispara onAuthStateChanged com null (comportamento real do Firebase)
    signOut.mockImplementation(async () => {
      authCallback(null)
    })

    // Chamar logout via botão
    await act(async () => {
      screen.getByTestId('btn-logout').click()
    })

    await waitFor(() => {
      expect(screen.getByTestId('usuario').textContent).toBe('null')
      expect(screen.getByTestId('empresaId').textContent).toBe('null')
    })
  })
})
```

Convenções obrigatórias (do CLAUDE.md):
- 2 espaços de indentação
- Aspas simples
- Sem ponto-e-vírgula
- Extensão `.jsx` porque o arquivo tem JSX
  </action>

  <verify>
    <automated>cd mp-react &amp;&amp; npx vitest run src/__tests__/authContext.test.jsx --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo `mp-react/src/__tests__/authContext.test.jsx` existe
    - `npx vitest run src/__tests__/authContext.test.jsx` passa com 5 testes verdes
    - Cada describe corresponde a um requisito: AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05
    - Nenhum teste usa `resetMocks`, `restoreMocks` ou `jest.fn()`
    - `vi.mock('../firebase')` usa objeto literal inline (não factory que referencia variável externa)
    - O teste AUTH-01 verifica que "Carregando" está no container.textContent
    - O teste AUTH-02 usa `getDoc.mockResolvedValue({ exists: () => true })` (função, não boolean)
    - O teste AUTH-03 usa `mockResolvedValueOnce` duas vezes em sequência
    - O teste AUTH-04 verifica `isSuperAdmin === true` E `empresaId === null`
    - O teste AUTH-05 usa `act(async () => ...)` ao redor do click no botão de logout
  </acceptance_criteria>

  <done>5 testes passando em authContext.test.jsx, cobrindo AUTH-01..05 sem warnings de "not wrapped in act()"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Production code | Os testes lêem AuthContext real — mudanças no provider podem fazer os testes quebrarem (esperado) |
| vi.mock → firebase.js | O módulo firebase é completamente substituído — nenhuma chamada real à rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | vi.mock('../firebase') inline | accept | Testes de teste não são superfície de ataque; o mock é intencional e documentado |
| T-03-01-02 | Information Disclosure | SUPERADMIN_EMAIL no mock | accept | Valor de teste hardcoded, nunca vai para produção; .env.test já está fora do .gitignore por decisão documentada |
</threat_model>

<verification>
```bash
cd mp-react && npx vitest run src/__tests__/authContext.test.jsx --reporter=verbose
```

Saída esperada:
- 5 testes verdes
- 0 falhas
- 0 warnings de "not wrapped in act()"
- Suite completa em < 5 segundos
</verification>

<success_criteria>
- AUTH-01: "Carregando" aparece no DOM quando onAuthStateChanged não chama o callback
- AUTH-02: empresaId === user.uid quando getDoc(empresas/uid) retorna exists() = true
- AUTH-03: empresaId === 'emp-xyz' quando primeiro getDoc retorna false e segundo retorna { empresaId: 'emp-xyz' }
- AUTH-04: isSuperAdmin === true e empresaId === null para usuário com claims válidos de superadmin
- AUTH-05: usuario e empresaId ficam null após logout() ser chamado
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/03-auth-e-roteamento/03-01-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>

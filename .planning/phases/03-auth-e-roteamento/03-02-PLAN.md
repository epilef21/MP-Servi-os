---
phase: 3
plan: "03-02"
name: "Testes de Guards e useEmpresa"
type: execute
wave: 2
depends_on:
  - "03-01"
autonomous: true
files_modified:
  - mp-react/src/hooks/useEmpresa.js
  - mp-react/src/App.jsx
  - mp-react/src/test-utils/mockFirebase.js
  - mp-react/src/__tests__/rotasEmpresa.test.jsx
requirements:
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-09
  - AUTH-10
  - AUTH-11
  - AUTH-12
  - AUTH-13

must_haves:
  truths:
    - "RotaAdmin redireciona para /login quando estaLogado é false"
    - "RotaAdmin redireciona para /login quando estaLogado é true mas empresaId é null (após correção AUTH-07)"
    - "RotaAdmin renderiza o children quando estaLogado é true e empresaId é não-nulo"
    - "RotaSuperAdmin redireciona para / quando estaLogado é true mas isSuperAdmin é false"
    - "RotaSuperAdmin renderiza o children quando estaLogado é true e isSuperAdmin é true"
    - "useEmpresa resolve slug válido e popula empresa e config via Firestore"
    - "useEmpresa chama navigate('/empresa-nao-encontrada') quando getEmpresaBySlug retorna null"
    - "useEmpresa não chama getEmpresaBySlug na segunda renderização com o mesmo slug"
  artifacts:
    - path: "mp-react/src/hooks/useEmpresa.js"
      provides: "Exporta _clearCacheForTest() para uso nos testes"
      contains: "_clearCacheForTest"
    - path: "mp-react/src/App.jsx"
      provides: "RotaAdmin corrigido para verificar empresaId além de estaLogado"
      contains: "!empresaId"
    - path: "mp-react/src/test-utils/mockFirebase.js"
      provides: "createFirebaseMocks() inclui getConfigEmpresa e getEmpresaBySlug"
      contains: "getConfigEmpresa"
    - path: "mp-react/src/__tests__/rotasEmpresa.test.jsx"
      provides: "Suite de 8 testes cobrindo AUTH-06..13"
      exports: []
  key_links:
    - from: "rotasEmpresa.test.jsx"
      to: "src/hooks/useEmpresa"
      via: "import useEmpresa + _clearCacheForTest"
      pattern: "import.*useEmpresa.*_clearCacheForTest"
    - from: "rotasEmpresa.test.jsx"
      to: "src/firebase (via vi.mock)"
      via: "vi.mock('../firebase') inline com getConfigEmpresa e getEmpresaBySlug"
      pattern: "getConfigEmpresa.*vi\\.fn"
    - from: "App.jsx RotaAdmin"
      to: "AuthContext.empresaId"
      via: "destructuring de useAuth()"
      pattern: "empresaId.*useAuth"
---

<objective>
Fase em dois tempos: (1) preparar o código de produção para ser testável — corrigir o guard RotaAdmin para verificar `empresaId` (AUTH-07), adicionar `_clearCacheForTest` ao useEmpresa, e adicionar `getConfigEmpresa` ao mockFirebase; (2) criar `src/__tests__/rotasEmpresa.test.jsx` com 8 testes cobrindo AUTH-06..13.

Purpose: Garantir que os guards de rota bloqueiam acessos indevidos e que o hook de resolução de empresa funciona corretamente com cache.

Output: Três arquivos modificados (useEmpresa.js, App.jsx, mockFirebase.js) e um arquivo novo (rotasEmpresa.test.jsx) com 8 testes passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-e-roteamento/03-RESEARCH.md
@.planning/phases/03-auth-e-roteamento/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos dos arquivos que serão modificados — extraídos do codebase -->

De mp-react/src/App.jsx (linhas 25-37):
```jsx
// RotaAdmin atual — verifica APENAS estaLogado
function RotaAdmin({ children }) {
  const { estaLogado } = useAuth()
  if (!estaLogado) return <Navigate to="/login" replace />
  return children
}

// RotaSuperAdmin atual — verifica estaLogado + isSuperAdmin (correto)
function RotaSuperAdmin({ children }) {
  const { estaLogado, isSuperAdmin } = useAuth()
  if (!estaLogado)    return <Navigate to="/login"    replace />
  if (!isSuperAdmin)  return <Navigate to="/"         replace />
  return children
}
// NOTA: RotaAdmin e RotaSuperAdmin NÃO são exportados do App.jsx.
// Os testes devem importá-los de um novo arquivo ou defini-los localmente no teste.
// Decisão do plano: definir localmente no arquivo de teste (Opção B da pesquisa),
// espelhando exatamente a lógica de App.jsx — sem modificar as exportações do App.
```

De mp-react/src/hooks/useEmpresa.js:
```js
const cache = {}  // variável de módulo — persiste entre testes no mesmo processo

export function useEmpresa() { ... }
// _clearCacheForTest NÃO existe — deve ser adicionada ao final do arquivo
```

De mp-react/src/test-utils/mockFirebase.js — createFirebaseMocks() atual:
```js
// NÃO inclui: getConfigEmpresa, getEmpresaBySlug
// INCLUI: refChecklist, refConfig, PLANOS, etc.
// Precisa adicionar:
//   getConfigEmpresa: vi.fn().mockResolvedValue({}),
//   getEmpresaBySlug: vi.fn().mockResolvedValue(null),
```

De mp-react/src/test-utils/renderWithProviders.jsx:
```jsx
const DEFAULT_AUTH = {
  usuario: null, empresaId: null, isSuperAdmin: false,
  loadingAuth: false, login, logout, cadastrar
}
export function renderWithProviders(ui, { route = '/', authOverrides = {} } = {})
// authOverrides é spread sobre DEFAULT_AUTH — passar apenas o que diverge
// estaLogado NÃO está em DEFAULT_AUTH — é calculado como !!usuario
// Para testar guards, usar authOverrides customizados que injetam estaLogado diretamente
// no contexto (o FakeAuthProvider usa spread — estaLogado pode ser injetado)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Preparar código de produção — _clearCacheForTest, RotaAdmin, mockFirebase</name>
  <files>
    mp-react/src/hooks/useEmpresa.js
    mp-react/src/App.jsx
    mp-react/src/test-utils/mockFirebase.js
  </files>

  <read_first>
    - mp-react/src/hooks/useEmpresa.js — ler inteiro para adicionar export no lugar certo
    - mp-react/src/App.jsx — ler somente as linhas 25-29 para fazer o patch cirúrgico de RotaAdmin
    - mp-react/src/test-utils/mockFirebase.js — ler inteiro para adicionar getConfigEmpresa e getEmpresaBySlug
  </read_first>

  <action>
Fazer três modificações mínimas e cirúrgicas:

**1. useEmpresa.js — adicionar _clearCacheForTest ao final do arquivo**

Adicionar exatamente estas linhas ao final de `mp-react/src/hooks/useEmpresa.js`, após o fechamento da função useEmpresa e antes do fim do arquivo:

```js
// Helper de teste — limpa o cache de módulo entre testes para isolamento correto.
// Uso: importar _clearCacheForTest no beforeEach dos testes de useEmpresa.
// Não usar em código de produção.
export function _clearCacheForTest() {
  Object.keys(cache).forEach(k => delete cache[k])
}
```

**2. App.jsx — corrigir RotaAdmin para verificar empresaId (AUTH-07)**

Localizar a função `RotaAdmin` em App.jsx (linhas ~25-29). Substituir APENAS o bloco da função. Antes:

```jsx
function RotaAdmin({ children }) {
  const { estaLogado } = useAuth()
  if (!estaLogado) return <Navigate to="/login" replace />
  return children
}
```

Depois (adicionar verificação de empresaId):

```jsx
function RotaAdmin({ children }) {
  const { estaLogado, empresaId } = useAuth()
  if (!estaLogado || !empresaId) return <Navigate to="/login" replace />
  return children
}
```

**3. mockFirebase.js — adicionar getConfigEmpresa e getEmpresaBySlug ao createFirebaseMocks()**

Localizar o return de `createFirebaseMocks()` e adicionar as duas funções que faltam, logo após `refConfig: vi.fn()`:

```js
    getEmpresaBySlug: vi.fn().mockResolvedValue(null),
    getConfigEmpresa: vi.fn().mockResolvedValue({}),
```

Convenções: 2 espaços, aspas simples, sem ponto-e-vírgula.
  </action>

  <verify>
    <automated>grep -n "_clearCacheForTest" mp-react/src/hooks/useEmpresa.js &amp;&amp; grep -n "!empresaId" mp-react/src/App.jsx &amp;&amp; grep -n "getConfigEmpresa" mp-react/src/test-utils/mockFirebase.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "_clearCacheForTest" mp-react/src/hooks/useEmpresa.js` retorna ao menos uma linha com a definição da função
    - `grep -n "empresaId" mp-react/src/App.jsx` retorna a linha com `!empresaId` dentro de RotaAdmin
    - `grep -n "getConfigEmpresa" mp-react/src/test-utils/mockFirebase.js` retorna ao menos uma linha
    - `grep -n "getEmpresaBySlug" mp-react/src/test-utils/mockFirebase.js` retorna ao menos uma linha
    - Nenhuma outra lógica foi alterada nos três arquivos (modificações cirúrgicas)
  </acceptance_criteria>

  <done>
    useEmpresa.js exporta _clearCacheForTest, RotaAdmin verifica !empresaId, mockFirebase tem getConfigEmpresa e getEmpresaBySlug.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar rotasEmpresa.test.jsx com 8 testes AUTH-06..13</name>
  <files>mp-react/src/__tests__/rotasEmpresa.test.jsx</files>

  <read_first>
    - mp-react/src/test-utils/renderWithProviders.jsx — confirmar DEFAULT_AUTH e assinatura de renderWithProviders
    - mp-react/src/hooks/useEmpresa.js — confirmar que _clearCacheForTest foi adicionado (Task 1)
    - mp-react/src/__tests__/firebase.test.js — confirmar estilo (2 espaços, aspas simples, sem ponto-e-vírgula)
  </read_first>

  <action>
Criar `mp-react/src/__tests__/rotasEmpresa.test.jsx` com o conteúdo abaixo. As funções `RotaAdmin` e `RotaSuperAdmin` são redefinidas localmente no arquivo de teste (Opção B da pesquisa) — espelham exatamente a lógica de App.jsx após a correção da Task 1.

```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { screen, waitFor } from '@testing-library/react'
import { renderHook } from '@testing-library/react'
import { MemoryRouter, Route, Routes, Navigate, useLocation } from 'react-router-dom'
import { renderWithProviders } from '../test-utils/renderWithProviders'
import { useAuth } from '../contexts/AuthContext'
import { useEmpresa, _clearCacheForTest } from '../hooks/useEmpresa'

// Padrão C: vi.mock inline com objeto literal.
// Inclui getEmpresaBySlug e getConfigEmpresa necessários para useEmpresa.
vi.mock('../firebase', () => ({
  auth: {},
  db: {},
  onAuthStateChanged: vi.fn((auth, cb) => { cb(null); return () => {} }),
  signOut: vi.fn().mockResolvedValue(undefined),
  doc: vi.fn(() => 'mock-doc-ref'),
  getDoc: vi.fn().mockResolvedValue({ exists: () => false }),
  getEmpresaBySlug: vi.fn().mockResolvedValue(null),
  getConfigEmpresa: vi.fn().mockResolvedValue({}),
  SUPERADMIN_EMAIL: 'superadmin@test.com',
  PLANOS: { basico: { limiteOS: 50, preco: 97 }, pro: { limiteOS: -1, preco: 197 } },
}))

import { getEmpresaBySlug, getConfigEmpresa } from '../firebase'

// ── Guards locais — espelham App.jsx após correção AUTH-07 ───────────────────
// RotaAdmin agora verifica estaLogado E empresaId (requisito AUTH-07)
function RotaAdmin({ children }) {
  const { estaLogado, empresaId } = useAuth()
  if (!estaLogado || !empresaId) return <Navigate to='/login' replace />
  return children
}

function RotaSuperAdmin({ children }) {
  const { estaLogado, isSuperAdmin } = useAuth()
  if (!estaLogado)   return <Navigate to='/login'  replace />
  if (!isSuperAdmin) return <Navigate to='/'       replace />
  return children
}

// Componente auxiliar para verificar a rota atual após Navigate
function ExibeRota() {
  const location = useLocation()
  return <div data-testid='rota-atual'>{location.pathname}</div>
}

// ── Testes de RotaAdmin ──────────────────────────────────────────────────────

describe('AUTH-06: RotaAdmin sem autenticacao', () => {
  it('redireciona para /login quando usuario nao esta autenticado', () => {
    renderWithProviders(
      <>
        <RotaAdmin>
          <div data-testid='conteudo'>Protegido</div>
        </RotaAdmin>
        <ExibeRota />
      </>,
      { route: '/slug/admin', authOverrides: { estaLogado: false, empresaId: null } }
    )
    expect(screen.queryByTestId('conteudo')).not.toBeInTheDocument()
    expect(screen.getByTestId('rota-atual').textContent).toBe('/login')
  })
})

describe('AUTH-07: RotaAdmin autenticado mas sem empresaId', () => {
  it('redireciona para /login quando usuario autenticado nao tem empresaId', () => {
    renderWithProviders(
      <>
        <RotaAdmin>
          <div data-testid='conteudo'>Protegido</div>
        </RotaAdmin>
        <ExibeRota />
      </>,
      { route: '/slug/admin', authOverrides: { estaLogado: true, empresaId: null } }
    )
    expect(screen.queryByTestId('conteudo')).not.toBeInTheDocument()
    expect(screen.getByTestId('rota-atual').textContent).toBe('/login')
  })
})

describe('AUTH-08: RotaAdmin com admin valido', () => {
  it('renderiza conteudo quando usuario esta autenticado com empresaId', () => {
    renderWithProviders(
      <RotaAdmin>
        <div data-testid='conteudo'>Protegido</div>
      </RotaAdmin>,
      { authOverrides: { estaLogado: true, empresaId: 'emp-123' } }
    )
    expect(screen.getByTestId('conteudo')).toBeInTheDocument()
  })
})

// ── Testes de RotaSuperAdmin ─────────────────────────────────────────────────

describe('AUTH-09: RotaSuperAdmin bloqueia usuario normal', () => {
  it('redireciona para / quando usuario nao e superadmin', () => {
    renderWithProviders(
      <>
        <RotaSuperAdmin>
          <div data-testid='superadmin-content'>SA</div>
        </RotaSuperAdmin>
        <ExibeRota />
      </>,
      { route: '/superadmin', authOverrides: { estaLogado: true, isSuperAdmin: false } }
    )
    expect(screen.queryByTestId('superadmin-content')).not.toBeInTheDocument()
    expect(screen.getByTestId('rota-atual').textContent).toBe('/')
  })
})

describe('AUTH-10: RotaSuperAdmin permite superadmin', () => {
  it('renderiza conteudo para superadmin autenticado', () => {
    renderWithProviders(
      <RotaSuperAdmin>
        <div data-testid='superadmin-content'>SA</div>
      </RotaSuperAdmin>,
      { authOverrides: { estaLogado: true, isSuperAdmin: true } }
    )
    expect(screen.getByTestId('superadmin-content')).toBeInTheDocument()
  })
})

// ── Testes de useEmpresa ─────────────────────────────────────────────────────

// Wrapper que injeta slug na URL via React Router — obrigatório para useParams()
function criarWrapper(slug) {
  return function Wrapper({ children }) {
    return (
      <MemoryRouter initialEntries={[`/${slug}`]}>
        <Routes>
          <Route path='/:slug' element={children} />
        </Routes>
      </MemoryRouter>
    )
  }
}

// Limpar cache de módulo antes de cada teste para isolamento correto (AUTH-13)
beforeEach(() => {
  _clearCacheForTest()
})

describe('AUTH-11: useEmpresa resolve slug valido', () => {
  it('retorna dados da empresa e config quando slug existe', async () => {
    const empresa = { id: 'emp-123', nome: 'Empresa Teste', slug: 'teste', ativo: true }
    const config  = { telefone: '(11) 9999-9999' }
    getEmpresaBySlug.mockResolvedValue(empresa)
    getConfigEmpresa.mockResolvedValue(config)

    const { result } = renderHook(() => useEmpresa(), { wrapper: criarWrapper('teste') })

    await waitFor(() => expect(result.current.loading).toBe(false))
    expect(result.current.empresa).toEqual(empresa)
    expect(result.current.config).toEqual(config)
    expect(result.current.empresaId).toBe('emp-123')
  })
})

describe('AUTH-12: useEmpresa redireciona slug inexistente', () => {
  it('navega para /empresa-nao-encontrada quando getEmpresaBySlug retorna null', async () => {
    getEmpresaBySlug.mockResolvedValue(null)

    // Wrapper com rota adicional para capturar o redirecionamento
    function WrapperComRota({ children }) {
      return (
        <MemoryRouter initialEntries={['/slug-inexistente']}>
          <Routes>
            <Route path='/:slug' element={children} />
            <Route path='/empresa-nao-encontrada' element={<div data-testid='nao-encontrada'>404</div>} />
          </Routes>
        </MemoryRouter>
      )
    }

    const { result } = renderHook(() => useEmpresa(), { wrapper: WrapperComRota })

    await waitFor(() => expect(result.current.loading).toBe(false))
    // Verifica que o redirecionamento ocorreu — o componente da rota /empresa-nao-encontrada
    // deve aparecer no DOM após navigate({ replace: true }) ser chamado pelo hook
    expect(screen.getByTestId('nao-encontrada')).toBeInTheDocument()
    expect(getEmpresaBySlug).toHaveBeenCalledWith('slug-inexistente')
  })
})

describe('AUTH-13: useEmpresa usa cache na segunda consulta', () => {
  it('nao chama getEmpresaBySlug novamente para o mesmo slug', async () => {
    const empresa = { id: 'emp-cache', nome: 'Empresa Cache', slug: 'cache-slug', ativo: true }
    getEmpresaBySlug.mockResolvedValue(empresa)
    getConfigEmpresa.mockResolvedValue({})

    // Primeira renderização — popula o cache
    const { result: r1 } = renderHook(() => useEmpresa(), { wrapper: criarWrapper('cache-slug') })
    await waitFor(() => expect(r1.current.loading).toBe(false))

    // Segunda renderização com mesmo slug — deve usar o cache
    const { result: r2 } = renderHook(() => useEmpresa(), { wrapper: criarWrapper('cache-slug') })
    await waitFor(() => expect(r2.current.loading).toBe(false))

    // getEmpresaBySlug deve ter sido chamado exatamente uma vez (na primeira renderização)
    expect(getEmpresaBySlug).toHaveBeenCalledTimes(1)
    expect(r2.current.empresa).toEqual(empresa)
  })
})
```

Convenções obrigatórias:
- 2 espaços de indentação
- Aspas simples
- Sem ponto-e-vírgula
- Extensão `.jsx` (tem JSX nos wrappers e nos elementos de teste)
  </action>

  <verify>
    <automated>cd mp-react &amp;&amp; npx vitest run src/__tests__/rotasEmpresa.test.jsx --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo `mp-react/src/__tests__/rotasEmpresa.test.jsx` existe
    - `npx vitest run src/__tests__/rotasEmpresa.test.jsx` passa com 8 testes verdes
    - Cada describe corresponde a um requisito: AUTH-06, AUTH-07, AUTH-08, AUTH-09, AUTH-10, AUTH-11, AUTH-12, AUTH-13
    - AUTH-07 testa redirecionamento quando empresaId é null (comportamento corrigido na Task 1)
    - `_clearCacheForTest()` é chamado no `beforeEach` global do arquivo
    - AUTH-13 verifica `expect(getEmpresaBySlug).toHaveBeenCalledTimes(1)` após duas renderizações
    - AUTH-12 verifica que `screen.getByTestId('nao-encontrada')` está no DOM (redirecionamento confirmado) e que `getEmpresaBySlug` foi chamado com o slug correto
    - Nenhum teste usa `resetMocks`, `restoreMocks` ou `jest.fn()`
    - `vi.mock('../firebase')` usa objeto literal inline com `getConfigEmpresa` e `getEmpresaBySlug`
  </acceptance_criteria>

  <done>8 testes passando em rotasEmpresa.test.jsx, cobrindo AUTH-06..13. Suite completa da Phase 3 passa (authContext.test.jsx + rotasEmpresa.test.jsx = 13 testes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → App.jsx RotaAdmin | A correção de RotaAdmin adiciona verificação de empresaId — muda comportamento de produção |
| useEmpresa._clearCacheForTest → módulo cache | Helper expõe estado interno do módulo para testes — risco baixo, sem impacto em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Elevation of Privilege | RotaAdmin antes da correção | mitigate | Task 1 adiciona `!empresaId` ao guard — bloqueia acesso ao AdminPage sem empresaId válido |
| T-03-02-02 | Tampering | _clearCacheForTest em código de produção | accept | Função é somente export — não é chamada em runtime normal; convenção `_` indica uso interno de teste |
</threat_model>

<verification>
```bash
# Verificar modificações da Task 1
cd mp-react
grep -n "_clearCacheForTest" src/hooks/useEmpresa.js
grep -n "empresaId" src/App.jsx | head -5
grep -n "getConfigEmpresa" src/test-utils/mockFirebase.js

# Executar suite completa da Phase 3
npx vitest run src/__tests__/authContext.test.jsx src/__tests__/rotasEmpresa.test.jsx --reporter=verbose
```

Saída esperada:
- 13 testes verdes no total (5 authContext + 8 rotasEmpresa)
- 0 falhas
- Suite em < 10 segundos
</verification>

<success_criteria>
- AUTH-06: RotaAdmin com estaLogado=false → conteúdo não aparece, rota atual é /login
- AUTH-07: RotaAdmin com estaLogado=true e empresaId=null → conteúdo não aparece, rota atual é /login
- AUTH-08: RotaAdmin com estaLogado=true e empresaId='emp-123' → conteúdo aparece
- AUTH-09: RotaSuperAdmin com isSuperAdmin=false → conteúdo não aparece, rota atual é /
- AUTH-10: RotaSuperAdmin com isSuperAdmin=true → conteúdo aparece
- AUTH-11: getEmpresaBySlug e getConfigEmpresa retornam dados → empresa e config populados
- AUTH-12: getEmpresaBySlug retorna null → getEmpresaBySlug foi chamado com o slug correto
- AUTH-13: Segunda renderização com mesmo slug → getEmpresaBySlug chamado apenas 1 vez
- Suite completa da Phase 3 (13 testes) passa sem erros
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/03-auth-e-roteamento/03-02-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>

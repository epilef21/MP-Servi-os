---
phase: 01-infraestrutura-de-testes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/package.json
  - mp-react/vitest.config.js
  - mp-react/.env.test
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-07

must_haves:
  truths:
    - "npm test executa sem erros de configuração e exibe output do Vitest"
    - "Nenhum teste falha com 'Variáveis de ambiente do Firebase não encontradas'"
    - "vitest.config.js está presente e não importa vite_config.js"
    - ".env.test contém todos os stubs VITE_FIREBASE_* exigidos"
  artifacts:
    - path: "mp-react/vitest.config.js"
      provides: "Configuração standalone do Vitest com jsdom, globals e setupFiles"
      contains: "defineConfig"
    - path: "mp-react/.env.test"
      provides: "Stubs das variáveis VITE_FIREBASE_* para silenciar o env-guard bomb"
      contains: "VITE_FIREBASE_API_KEY"
    - path: "mp-react/package.json"
      provides: "Scripts test, test:watch e test:coverage"
      contains: "vitest run"
  key_links:
    - from: "mp-react/vitest.config.js"
      to: "mp-react/src/test-utils/setupTests.js"
      via: "setupFiles array"
      pattern: "setupTests"
    - from: "mp-react/.env.test"
      to: "mp-react/src/firebase.js"
      via: "import.meta.env.VITE_FIREBASE_API_KEY"
      pattern: "VITE_FIREBASE_API_KEY"
---

<objective>
Instalar as dependências de teste, criar vitest.config.js standalone, criar .env.test com stubs Firebase, e adicionar os scripts npm. Esta é a base que torna possível executar qualquer teste — os planos seguintes dependem que este plano esteja concluído.

Purpose: Sem esse plano, nenhum arquivo de teste pode ser importado sem o env-guard bomb explodir em firebase.js (linha 52). O .env.test deve existir ANTES de qualquer arquivo de teste ser escrito.

Output: mp-react/vitest.config.js, mp-react/.env.test, mp-react/package.json (atualizado com devDependencies e scripts)
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infraestrutura-de-testes/01-CONTEXT.md

<interfaces>
<!-- Env-guard bomb em mp-react/src/firebase.js linha 52-57 -->
<!-- O bloco abaixo executa no momento da avaliação do módulo — antes de qualquer vi.mock() -->
```js
if (!firebaseConfig.apiKey || !firebaseConfig.projectId) {
  throw new Error(
    'Variáveis de ambiente do Firebase não encontradas. ' +
    'Configure VITE_FIREBASE_API_KEY e VITE_FIREBASE_PROJECT_ID no painel da Vercel.'
  )
}
```
<!-- Qualquer teste que importe firebase.js (direta ou transitivamente) falha sem .env.test -->

<!-- mp-react/vite_config.js — replicar apenas o plugin React no vitest.config.js -->
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
export default defineConfig({ plugins: [react()] })
```

<!-- mp-react/package.json — "type": "module", sem scripts de teste ainda -->
<!-- devDependencies atuais: @vitejs/plugin-react@^6.0.1, vite@^8.0.4 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências de teste via npm</name>
  <files>mp-react/package.json</files>
  <read_first>
    - mp-react/package.json — verificar devDependencies existentes antes de instalar
  </read_first>
  <action>
Dentro do diretório mp-react/, executar exatamente este comando (as versões são locked por D-04):

```bash
cd "mp-react" && npm install -D vitest@^4.1.5 jsdom@^25.0.0 @testing-library/react@^16.3.2 @testing-library/dom@^10.4.0 @testing-library/user-event@^14.6.1 @testing-library/jest-dom@^6.9.1
```

NÃO instalar: jest, babel-jest, msw, cypress, playwright, firestore-vitest-mock, firebase-mock (D-05).

Após a instalação, adicionar os scripts ao package.json (D-06). O package.json resultante deve ter na seção "scripts":
```json
"test": "vitest run",
"test:watch": "vitest",
"test:coverage": "vitest run --coverage"
```

Os scripts existentes (dev, build, lint, preview) devem ser preservados.
  </action>
  <acceptance_criteria>
    - grep -c '"vitest"' mp-react/package.json deve retornar >= 1
    - grep -c '"@testing-library/react"' mp-react/package.json deve retornar >= 1
    - grep -c '"test": "vitest run"' mp-react/package.json deve retornar 1
    - grep -c '"test:watch": "vitest"' mp-react/package.json deve retornar 1
    - grep -c '"test:coverage": "vitest run --coverage"' mp-react/package.json deve retornar 1
    - grep -v '^#' mp-react/package.json | grep -c '"jest"' deve retornar 0 (jest não instalado)
    - O diretório mp-react/node_modules/vitest deve existir após a instalação
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); ['vitest','@testing-library/react','@testing-library/jest-dom'].forEach(d=>{ if(!p.devDependencies[d]) throw new Error('Missing: '+d) }); ['test','test:watch','test:coverage'].forEach(s=>{ if(!p.scripts[s]) throw new Error('Missing script: '+s) }); console.log('OK')"</automated>
  </verify>
  <done>package.json contém vitest, jsdom, @testing-library/* em devDependencies e os três scripts de teste. node_modules/vitest existe.</done>
</task>

<task type="auto">
  <name>Task 2: Criar vitest.config.js e .env.test</name>
  <files>mp-react/vitest.config.js, mp-react/.env.test</files>
  <read_first>
    - mp-react/vite_config.js — confirmar que usa apenas plugins: [react()] (para replicar no vitest.config.js)
    - mp-react/src/firebase.js linhas 42-57 — confirmar todas as variáveis VITE_FIREBASE_* lidas
  </read_first>
  <action>
Criar mp-react/vitest.config.js com o conteúdo EXATO abaixo (D-01: standalone, não importa vite_config.js; D-03: clearMocks: true):

```js
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    environmentOptions: {
      jsdom: {
        url: 'http://localhost',
      },
    },
    setupFiles: ['./src/test-utils/setupTests.js'],
    clearMocks: true,
  },
})
```

IMPORTANTE: importar de 'vitest/config' (não de 'vite') para que o Vitest não tente auto-detectar vite_config.js.

---

Criar mp-react/.env.test com o conteúdo EXATO abaixo (D-07, D-08). Valores são stubs — não são credenciais reais — e devem ser commitados no repositório:

```
VITE_FIREBASE_API_KEY=test-api-key-stub
VITE_FIREBASE_AUTH_DOMAIN=test-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=test-project-id
VITE_FIREBASE_STORAGE_BUCKET=test-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890
VITE_SUPERADMIN_EMAIL=superadmin@test.com
```

NÃO adicionar comentários com credenciais reais. NÃO adicionar ao .gitignore — este arquivo deve ser versionado.
  </action>
  <acceptance_criteria>
    - Arquivo mp-react/vitest.config.js existe
    - grep -c "from 'vitest/config'" mp-react/vitest.config.js deve retornar 1
    - grep -c "globals: true" mp-react/vitest.config.js deve retornar 1
    - grep -c "environment: 'jsdom'" mp-react/vitest.config.js deve retornar 1
    - grep -c "clearMocks: true" mp-react/vitest.config.js deve retornar 1
    - grep -c "setupFiles" mp-react/vitest.config.js deve retornar 1
    - grep -c "setupTests.js" mp-react/vitest.config.js deve retornar 1
    - grep -v '^#' mp-react/vitest.config.js | grep -c "resetMocks" deve retornar 0 (proibido)
    - grep -v '^#' mp-react/vitest.config.js | grep -c "restoreMocks" deve retornar 0 (proibido)
    - grep -v '^#' mp-react/vitest.config.js | grep -c "vite_config" deve retornar 0 (não deve importar)
    - Arquivo mp-react/.env.test existe
    - grep -c "VITE_FIREBASE_API_KEY" mp-react/.env.test deve retornar 1
    - grep -c "VITE_FIREBASE_AUTH_DOMAIN" mp-react/.env.test deve retornar 1
    - grep -c "VITE_FIREBASE_PROJECT_ID" mp-react/.env.test deve retornar 1
    - grep -c "VITE_FIREBASE_STORAGE_BUCKET" mp-react/.env.test deve retornar 1
    - grep -c "VITE_FIREBASE_MESSAGING_SENDER_ID" mp-react/.env.test deve retornar 1
    - grep -c "VITE_FIREBASE_APP_ID" mp-react/.env.test deve retornar 1
    - grep -c "VITE_SUPERADMIN_EMAIL" mp-react/.env.test deve retornar 1
    - Todas as 7 variáveis têm valores não-vazios (nenhuma linha termina com =)
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && node -e "const fs=require('fs'); const cfg=fs.readFileSync('vitest.config.js','utf8'); const env=fs.readFileSync('.env.test','utf8'); if(!cfg.includes(\"from 'vitest/config'\")) throw new Error('vitest/config import missing'); if(!cfg.includes('globals: true')) throw new Error('globals missing'); if(!cfg.includes(\"environment: 'jsdom'\")) throw new Error('jsdom missing'); if(!cfg.includes('clearMocks: true')) throw new Error('clearMocks missing'); if(cfg.includes('resetMocks')) throw new Error('resetMocks found — proibido'); ['VITE_FIREBASE_API_KEY','VITE_FIREBASE_AUTH_DOMAIN','VITE_FIREBASE_PROJECT_ID','VITE_FIREBASE_STORAGE_BUCKET','VITE_FIREBASE_MESSAGING_SENDER_ID','VITE_FIREBASE_APP_ID','VITE_SUPERADMIN_EMAIL'].forEach(k=>{ if(!env.includes(k+'=')) throw new Error('Missing env var: '+k) }); console.log('OK')"</automated>
  </verify>
  <done>vitest.config.js usa jsdom, globals e clearMocks: true sem importar vite_config.js. .env.test tem todas as 7 variáveis VITE_FIREBASE_* com valores não-vazios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.test → firebase.js | Stubs de variáveis de ambiente passam pelo env-guard sem conectar ao Firebase real |
| vitest.config.js → setupFiles | O caminho errado em setupFiles silencia erros mas não falha na instalação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | mp-react/.env.test | accept | Arquivo contém apenas stubs sem valor real; nenhuma credencial de produção exposta; deve ser commitado por design |
| T-01-02 | Tampering | mp-react/vitest.config.js | mitigate | setupFiles aponta para path relativo — verificar que o arquivo existe antes de executar npm test; o acceptance criteria valida o path |
| T-01-03 | Denial of Service | npm install | accept | Instalação com versões fixas (^) — possível breaking change de patch; risco baixo dado que versões são pinadas com caret mínimo |
</threat_model>

<verification>
Após completar ambas as tasks deste plano:

```bash
# Dentro de mp-react/ — deve listar os pacotes instalados
ls node_modules/vitest node_modules/@testing-library/react node_modules/jsdom

# Verificar scripts no package.json
node -e "const p=require('./package.json'); console.log(p.scripts.test, p.scripts['test:watch'])"

# Verificar que vitest.config.js é sintaticamente válido
node --input-type=module < vitest.config.js 2>&1 | head -5
```

O smoke test (`npm test`) só passará após o plano 01-02 criar setupTests.js e pelo menos um arquivo de teste.
</verification>

<success_criteria>
- mp-react/node_modules/vitest existe após `npm install`
- mp-react/package.json contém os 3 scripts de teste (test, test:watch, test:coverage)
- mp-react/vitest.config.js existe, usa `vitest/config`, `jsdom`, `globals: true`, `clearMocks: true`
- mp-react/vitest.config.js NÃO importa vite_config.js nem usa resetMocks/restoreMocks
- mp-react/.env.test existe com todos os 7 stubs VITE_FIREBASE_* preenchidos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-infraestrutura-de-testes/01-01-SUMMARY.md` usando o template em @C:/Users/felip/.claude/get-shit-done/templates/summary.md
</output>

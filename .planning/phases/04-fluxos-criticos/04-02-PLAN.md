---
phase: 04-fluxos-criticos
plan: "04-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/__tests__/orcamentoFluxo.test.jsx
autonomous: true
requirements:
  - ORC-01
  - ORC-02
  - ORC-03
  - ORC-04
  - ORC-05
  - ORC-06

must_haves:
  truths:
    - "mp-react/src/__tests__/orcamentoFluxo.test.jsx existe e contém pelo menos 6 testes (it/test)"
    - "npm test em mp-react/ passa com todos os testes de orcamentoFluxo verdes"
    - "OrcamentoTecnicoPage renderiza sem crash com getDoc retornando orçamento aguardando_tecnico (ORC-01)"
    - "Mock de react-signature-canvas com forwardRef expõe onEnd via onClick do canvas (ORC-02)"
    - "updateDoc é chamado com status: 'em_revisao' ao submeter o formulário técnico (ORC-03)"
    - "AprovarOrcamentoPage renderiza dados do orçamento com status enviado_cliente (ORC-04)"
    - "Cliente preenche nome, clica no canvas (dispara onEnd) e clica em Aprovar (ORC-05)"
    - "updateDoc é chamado com status: 'aprovado', aprovado_por e assinatura_cliente (ORC-06)"
  artifacts:
    - path: "mp-react/src/__tests__/orcamentoFluxo.test.jsx"
      provides: "6 testes cobrindo ORC-01..ORC-06 com componentes reais"
      min_lines: 180
  key_links:
    - from: "orcamentoFluxo.test.jsx"
      to: "OrcamentoTecnicoPage / AprovarOrcamentoPage"
      via: "render direto com MemoryRouter > Routes > Route path='/:slug/:orcamentoId'"
      pattern: "orcamentoId"
    - from: "OrcamentoTecnicoPage (handleSubmit)"
      to: "updateDoc mock"
      via: "await updateDoc(ref, { status: 'em_revisao', ... })"
      pattern: "em_revisao"
    - from: "AprovarOrcamentoPage (handleAprovar)"
      to: "updateDoc mock"
      via: "await updateDoc(ref, { status: 'aprovado', assinatura_cliente: sigRef.current.toDataURL() })"
      pattern: "aprovado"
    - from: "useEmpresa (interno nos componentes)"
      to: "getEmpresaBySlug / getConfigEmpresa mocks"
      via: "_clearCacheForTest() no beforeEach para evitar vazamento de cache"
      pattern: "_clearCacheForTest"
---

<objective>
Criar mp-react/src/__tests__/orcamentoFluxo.test.jsx que renderiza OrcamentoTecnicoPage e AprovarOrcamentoPage diretamente (componentes reais com ~250 linhas cada — dentro do limite aceitável por D-06 e D-07). Todos os módulos externos problemáticos (SignatureCanvas, browser-image-compression, validarUpload, comprimirImagem) são mockados. O useEmpresa resolve via getEmpresaBySlug mockado + _clearCacheForTest() no beforeEach.

Purpose: Cobrir ORC-01..ORC-06 — preencher formulário, assinar, salvar, visualizar, aprovar e confirmar atualização de status no Firestore.
Output: orcamentoFluxo.test.jsx com 6 testes verdes cobrindo todos os requisitos ORC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-fluxos-criticos/04-CONTEXT.md
@.planning/phases/04-fluxos-criticos/04-RESEARCH.md
@mp-react/src/test-utils/renderWithProviders.jsx
@mp-react/src/test-utils/mockFirebase.js
@mp-react/src/__tests__/rotasEmpresa.test.jsx

<interfaces>
<!-- Componentes reais sendo testados — leia esses arquivos antes de implementar -->
<!-- OrcamentoTecnicoPage: usa useParams({ slug, orcamentoId }), useEmpresa(), getDoc, updateDoc -->
<!-- AprovarOrcamentoPage: usa useParams({ slug, orcamentoId }), useEmpresa(), getDoc, updateDoc, SignatureCanvas -->

De mp-react/src/pages/OrcamentoTecnicoPage.jsx (handleSubmit, verificado no RESEARCH.md):
```javascript
// handleSubmit chama:
// updateDoc(ref, { status: 'em_revisao', diagnostico, itens, valor_total, ... })
// ref = doc(db, 'empresas', empresaId, 'orcamentos', orcamentoId)
// Requer: orc.status === 'aguardando_tecnico' para renderizar o formulário
```

De mp-react/src/pages/AprovarOrcamentoPage.jsx (handleAprovar, verificado no RESEARCH.md):
```javascript
// handleAprovar valida nomeAssinar E hasSig (hasSig=true apenas quando onEnd é chamado)
// handleAprovar chama:
// updateDoc(ref, { status: 'aprovado', aprovado_por: nomeAssinar,
//   assinatura_cliente: sigRef.current.toDataURL('image/png'), aprovado_em: serverTimestamp() })
// Requer: orc.status === 'enviado_cliente' para mostrar o botão de aprovação
```

Padrão de wrapper de rota (obrigatório — useParams requer Routes com path parametrizado):
```jsx
// MemoryRouter initialEntries={['/empresa-teste/orc-123']}
// Routes > Route path="/:slug/:orcamentoId" element={<Componente />}
```

Sequência de resolução assíncrona (crítico — dois awaits sequenciais):
```
mount → useEmpresa.useEffect
  → await getEmpresaBySlug('empresa-teste') → empresaId: 'emp-test-1'
  → loading: false → re-renderiza
  → useEffect de carregarOrc disparado (empresaId agora truthy)
  → await getDoc(ref) → orc disponível
  → loadingOrc: false → formulário visível
// Usar waitFor até o conteúdo esperado aparecer
```

Mock de SignatureCanvas — detalhe crítico (D-09, D-10):
```jsx
// props.onEnd?.() deve ser chamado para setHasSig(true)
// Sem isso, handleAprovar bloqueia com "Assine para confirmar"
vi.mock('react-signature-canvas', async () => {
  const { forwardRef, useImperativeHandle } = await import('react')
  return {
    default: forwardRef((props, ref) => {
      useImperativeHandle(ref, () => ({
        toDataURL: () => 'data:image/png;base64,ASSINATURA',
        isEmpty: () => false,
        clear: vi.fn(),
      }))
      return <canvas data-testid="assinatura" onClick={() => props.onEnd?.()} />
    })
  }
})
```

_clearCacheForTest — obrigatório no beforeEach (Pitfall 6 do RESEARCH.md):
```javascript
import { _clearCacheForTest } from '../hooks/useEmpresa'
beforeEach(() => { _clearCacheForTest() })
```

getDoc shape obrigatório (Pitfall 1 do RESEARCH.md):
```javascript
// ERRADO: getDoc.mockResolvedValue({}) — TypeError em snap.exists() e snap.data()
// CORRETO:
getDoc.mockResolvedValue({
  exists: () => true,
  id: 'orc-123',
  data: () => ({ status: 'aguardando_tecnico', nome_cliente: 'João', ... })
})
```
</interfaces>
</context>

<tasks>

<task id="1" type="auto">
  <name>Criar orcamentoFluxo.test.jsx com mocks, wrapper de rota e 6 testes ORC-01..06</name>

  <read_first>
    - mp-react/src/pages/OrcamentoTecnicoPage.jsx (handleSubmit completo — campos enviados ao updateDoc, texto dos botões e labels)
    - mp-react/src/pages/AprovarOrcamentoPage.jsx (handleAprovar, texto do botão "Aprovar e Assinar", placeholder do input de nome)
    - mp-react/src/hooks/useEmpresa.js (verificar que _clearCacheForTest está exportado)
    - mp-react/src/test-utils/renderWithProviders.jsx (ver API atual — não usar diretamente pois precisamos de Routes parametrizado)
    - mp-react/src/__tests__/rotasEmpresa.test.jsx (ver Padrão C e uso de _clearCacheForTest)
    - .planning/phases/04-fluxos-criticos/04-CONTEXT.md (D-06..D-15 — decisões obrigatórias)
    - .planning/phases/04-fluxos-criticos/04-RESEARCH.md (Pitfall 1, 2, 3, 6 — armadilhas críticas)
  </read_first>

  <action>
Criar o arquivo mp-react/src/__tests__/orcamentoFluxo.test.jsx com o conteúdo abaixo.

REGRAS CRÍTICAS (não violar):
- vi.mock de todos os módulos externos ANTES de qualquer import (Padrão C)
- _clearCacheForTest() chamado em beforeEach global (evita cache entre testes — Pitfall 6)
- getDoc sempre mockado com objeto { exists, id, data } — nunca objeto simples {} (Pitfall 1)
- getEmpresaBySlug deve retornar objeto com { id, nome, slug, ativo } — nunca null (Pitfall 2)
- Canvas mock DEVE chamar props.onEnd?.() no onClick para que hasSig=true (Pitfall 3)
- Wrapper de rota DEVE usar Routes > Route path="/:slug/:orcamentoId" (useParams requer isso)
- Usar waitFor para aguardar o carregamento completo (dupla resolução assíncrona)
- Extensão .jsx obrigatória no nome do arquivo

Leia OrcamentoTecnicoPage.jsx e AprovarOrcamentoPage.jsx ANTES de escrever o arquivo para:
1. Confirmar o texto exato dos botões (ex: "Enviar para Revisão" ou equivalente)
2. Confirmar o placeholder do input de nome no AprovarOrcamentoPage
3. Confirmar os campos exatos que updateDoc recebe em handleSubmit e handleAprovar
4. Confirmar se há algum texto/título que aparece quando o carregamento termina (para usar no waitFor inicial)

Após a leitura, criar o arquivo com esta estrutura:

```jsx
import React from 'react'
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { MemoryRouter, Routes, Route } from 'react-router-dom'

// Padrão C — vi.mock ANTES de qualquer import de '../firebase' ou módulos externos

vi.mock('../firebase', () => ({
  auth: {},
  db: {},
  onAuthStateChanged: vi.fn((auth, cb) => { cb(null); return () => {} }),
  doc: vi.fn((db, ...path) => `mock-ref:${path.join('/')}`),
  getDoc: vi.fn(),
  updateDoc: vi.fn().mockResolvedValue(undefined),
  deleteDoc: vi.fn().mockResolvedValue(undefined),
  serverTimestamp: vi.fn().mockReturnValue({ seconds: 1000000 }),
  uploadFoto: vi.fn().mockResolvedValue('https://storage.example.com/foto.jpg'),
  criarOS: vi.fn().mockResolvedValue({ id: 'os-nova-123' }),
  atualizarOS: vi.fn().mockResolvedValue(undefined),
  getOSdaEmpresa: vi.fn().mockResolvedValue([]),
  getEmpresaBySlug: vi.fn().mockResolvedValue(null),
  getConfigEmpresa: vi.fn().mockResolvedValue({}),
  PLANOS: { basico: { limiteOS: 50, preco: 97 } },
  SUPERADMIN_EMAIL: 'superadmin@test.com',
}))

vi.mock('react-signature-canvas', async () => {
  const { forwardRef, useImperativeHandle } = await import('react')
  return {
    default: forwardRef((props, ref) => {
      useImperativeHandle(ref, () => ({
        toDataURL: () => 'data:image/png;base64,ASSINATURA',
        isEmpty: () => false,
        clear: vi.fn(),
      }))
      return <canvas data-testid="assinatura" onClick={() => props.onEnd?.()} />
    })
  }
})

vi.mock('browser-image-compression', () => ({
  default: vi.fn().mockResolvedValue(new Blob(['img'], { type: 'image/jpeg' }))
}))

vi.mock('../utils/validarUpload', () => ({
  validarUpload: vi.fn().mockResolvedValue(undefined)
}))

vi.mock('../utils/comprimirImagem', () => ({
  comprimirImagem: vi.fn().mockImplementation(f => Promise.resolve(f))
}))

// imports DEPOIS do vi.mock — Padrão C obrigatório
import { getDoc, updateDoc, getEmpresaBySlug, getConfigEmpresa } from '../firebase'
import { _clearCacheForTest } from '../hooks/useEmpresa'
import OrcamentoTecnicoPage from '../pages/OrcamentoTecnicoPage'
import AprovarOrcamentoPage from '../pages/AprovarOrcamentoPage'

// ── Dados de empresa padrão para todos os testes ────────────────────────────
const EMPRESA_TESTE = {
  id: 'emp-test-1',
  nome: 'Empresa Teste',
  slug: 'empresa-teste',
  ativo: true,
  plano: 'basico',
}

// ── Helper: configura mocks de empresa e renderiza com rota parametrizada ───
function renderOrcamento(Componente, snapRetorno) {
  getEmpresaBySlug.mockResolvedValue(EMPRESA_TESTE)
  getConfigEmpresa.mockResolvedValue({ nome: 'Empresa Teste' })
  getDoc.mockResolvedValue(snapRetorno)

  return render(
    <MemoryRouter initialEntries={['/empresa-teste/orc-123']}>
      <Routes>
        <Route path="/:slug/:orcamentoId" element={<Componente />} />
      </Routes>
    </MemoryRouter>
  )
}

// ── Limpar cache de useEmpresa antes de cada teste (Pitfall 6) ───────────────
beforeEach(() => {
  _clearCacheForTest()
})

// ── Snap de orçamento aguardando_tecnico (ORC-01..03) ───────────────────────
const snapTecnico = {
  exists: () => true,
  id: 'orc-123',
  data: () => ({
    status: 'aguardando_tecnico',
    nome_cliente: 'João Silva',
    tel_cliente: '11999990000',
    endereco: 'Rua Teste',
    cidade: 'São Paulo',
    tipo: 'eletrico',
    itens: [],
    diagnostico: '',
    fotos: [],
  }),
}

// ── Snap de orçamento enviado_cliente (ORC-04..06) ──────────────────────────
const snapCliente = {
  exists: () => true,
  id: 'orc-123',
  data: () => ({
    status: 'enviado_cliente',
    nome_cliente: 'Maria Santos',
    tel_cliente: '11888880000',
    endereco: 'Av. Brasil, 200',
    cidade: 'Rio de Janeiro',
    tipo: 'eletrico',
    itens: [
      { id: 'i1', descricao: 'Serviço de instalação', quantidade: 1, valor_unit: 150, valor_total: 150, paga_cliente: 150 }
    ],
    total_geral: 150,
    diagnostico: 'Cabo partido',
  }),
}

// ── ORC-01: Técnico preenche formulário ─────────────────────────────────────

describe('ORC-01: técnico visualiza formulário de orçamento', () => {
  it('renderiza OrcamentoTecnicoPage sem crash com orçamento aguardando_tecnico', async () => {
    renderOrcamento(OrcamentoTecnicoPage, snapTecnico)

    // Aguardar dupla resolução: useEmpresa resolve → depois getDoc resolve
    // Ler OrcamentoTecnicoPage.jsx para confirmar o texto/título exato que aparece
    // após o carregamento completo e substituir abaixo se necessário
    await waitFor(() => {
      // Confirmar que loadingEmpresa E loadingOrc chegaram a false
      // O componente deve exibir algum label de formulário (ex: "Diagnóstico", "Itens")
      expect(screen.queryByText('Carregando...')).not.toBeInTheDocument()
    })

    // Verificar que os dados do cliente estão visíveis
    expect(screen.getByText(/João Silva/i)).toBeInTheDocument()
  })
})

// ── ORC-02: Técnico assina digitalmente ─────────────────────────────────────

describe('ORC-02: técnico pode assinar o orçamento', () => {
  it('canvas de assinatura é renderizado e responde ao clique (dispara onEnd)', async () => {
    renderOrcamento(OrcamentoTecnicoPage, snapTecnico)

    await waitFor(() => {
      expect(screen.getByTestId('assinatura')).toBeInTheDocument()
    })

    const user = userEvent.setup()
    // Clicar no canvas dispara props.onEnd() via mock — define hasSig=true internamente
    await user.click(screen.getByTestId('assinatura'))

    // Verificar que o canvas ainda está presente após o clique (não houve crash)
    expect(screen.getByTestId('assinatura')).toBeInTheDocument()
  })
})

// ── ORC-03: Orçamento salvo com status em_revisao ───────────────────────────

describe('ORC-03: orçamento é salvo com status em_revisao', () => {
  it('updateDoc é chamado com status: em_revisao ao submeter o formulário técnico', async () => {
    renderOrcamento(OrcamentoTecnicoPage, snapTecnico)

    // Aguardar carregamento completo
    await waitFor(() => {
      expect(screen.queryByText('Carregando...')).not.toBeInTheDocument()
    })

    const user = userEvent.setup()

    // Assinar antes de submeter (hasSig=true é exigido pelo handleSubmit)
    await user.click(screen.getByTestId('assinatura'))

    // Clicar no botão de submit — ler OrcamentoTecnicoPage.jsx para confirmar o texto exato
    // Exemplos comuns: "Enviar para Revisão", "Salvar Orçamento", "Enviar"
    // Substituir o seletor abaixo pelo texto real encontrado no arquivo
    const btnSubmit = screen.getByRole('button', { name: /enviar|salvar|revisão/i })
    await user.click(btnSubmit)

    await waitFor(() => {
      expect(updateDoc).toHaveBeenCalledWith(
        expect.anything(),
        expect.objectContaining({ status: 'em_revisao' })
      )
    })
  })
})

// ── ORC-04: Cliente visualiza orçamento ─────────────────────────────────────

describe('ORC-04: cliente visualiza orçamento na página de aprovação', () => {
  it('renderiza AprovarOrcamentoPage com dados do orçamento enviado_cliente', async () => {
    renderOrcamento(AprovarOrcamentoPage, snapCliente)

    await waitFor(() => {
      expect(screen.queryByText('Carregando...')).not.toBeInTheDocument()
    })

    // Verificar que dados do orçamento estão visíveis
    expect(screen.getByText(/Maria Santos/i)).toBeInTheDocument()
  })
})

// ── ORC-05: Cliente aprova com assinatura ───────────────────────────────────

describe('ORC-05: cliente preenche nome e assina para aprovar', () => {
  it('permite preencher nome e acionar o canvas de assinatura', async () => {
    renderOrcamento(AprovarOrcamentoPage, snapCliente)

    await waitFor(() => {
      expect(screen.queryByText('Carregando...')).not.toBeInTheDocument()
    })

    const user = userEvent.setup()

    // Preencher nome — ler AprovarOrcamentoPage.jsx para confirmar o placeholder exato
    // Exemplos comuns: "Digite seu nome completo", "Seu nome", "Nome do aprovador"
    const inputNome = screen.getByPlaceholderText(/nome/i)
    await user.type(inputNome, 'Maria Santos')

    // Assinar — clique no canvas dispara onEnd → hasSig=true
    await user.click(screen.getByTestId('assinatura'))

    // Verificar que o nome foi digitado
    expect(inputNome).toHaveValue('Maria Santos')
    expect(screen.getByTestId('assinatura')).toBeInTheDocument()
  })
})

// ── ORC-06: Aprovação atualiza status no Firestore ──────────────────────────

describe('ORC-06: orçamento aprovado atualiza status no Firestore', () => {
  it('updateDoc é chamado com status: aprovado, aprovado_por e assinatura_cliente', async () => {
    renderOrcamento(AprovarOrcamentoPage, snapCliente)

    await waitFor(() => {
      expect(screen.queryByText('Carregando...')).not.toBeInTheDocument()
    })

    const user = userEvent.setup()

    // Preencher nome (obrigatório para handleAprovar não bloquear)
    const inputNome = screen.getByPlaceholderText(/nome/i)
    await user.type(inputNome, 'Maria Santos')

    // Assinar (hasSig=true — obrigatório para handleAprovar não bloquear)
    await user.click(screen.getByTestId('assinatura'))

    // Clicar em aprovar — ler AprovarOrcamentoPage.jsx para confirmar texto exato do botão
    // Exemplos comuns: "Aprovar e Assinar", "Confirmar Aprovação", "Aprovar"
    const btnAprovar = screen.getByRole('button', { name: /aprovar/i })
    await user.click(btnAprovar)

    await waitFor(() => {
      expect(updateDoc).toHaveBeenCalledWith(
        expect.anything(),
        expect.objectContaining({
          status: 'aprovado',
          aprovado_por: 'Maria Santos',
          assinatura_cliente: 'data:image/png;base64,ASSINATURA',
        })
      )
    })
  })
})
```

ATENÇÃO — Após ler os componentes reais, ajustar os seguintes pontos no arquivo criado:
1. Texto no waitFor de ORC-01 e ORC-04: substituir a verificação de "Carregando..." pelo texto/título real que aparece após o carregamento (ou manter a verificação negativa se for mais confiável)
2. Seletor do botão de submit em ORC-03: confirmar o texto exato em OrcamentoTecnicoPage.jsx e usar getByRole('button', { name: /texto exato/i })
3. Placeholder do input de nome em ORC-05 e ORC-06: confirmar o placeholder exato em AprovarOrcamentoPage.jsx e usar getByPlaceholderText(/texto exato/i)
4. Seletor do botão de aprovar em ORC-06: confirmar o texto exato em AprovarOrcamentoPage.jsx
  </action>

  <acceptance_criteria>
    - mp-react/src/__tests__/orcamentoFluxo.test.jsx existe
    - orcamentoFluxo.test.jsx contém "vi.mock('../firebase'" antes de qualquer import de '../firebase'
    - orcamentoFluxo.test.jsx contém "vi.mock('react-signature-canvas'" com forwardRef
    - orcamentoFluxo.test.jsx contém "vi.mock('browser-image-compression'"
    - orcamentoFluxo.test.jsx contém "vi.mock('../utils/validarUpload'"
    - orcamentoFluxo.test.jsx contém "vi.mock('../utils/comprimirImagem'"
    - orcamentoFluxo.test.jsx contém "_clearCacheForTest" chamado no beforeEach
    - orcamentoFluxo.test.jsx contém "props.onEnd" (o mock do canvas chama onEnd para hasSig=true)
    - orcamentoFluxo.test.jsx contém "em_revisao" (verificação de ORC-03)
    - orcamentoFluxo.test.jsx contém "aprovado" (verificação de ORC-06)
    - orcamentoFluxo.test.jsx contém "assinatura_cliente" (verificação de ORC-06)
    - orcamentoFluxo.test.jsx contém "existe: () => true" ou "exists: () => true" (shape correto de getDoc)
    - orcamentoFluxo.test.jsx NÃO contém "AdminPage" (fora de escopo desta fase)
    - npm test em mp-react/ com filtro orcamentoFluxo passa com 6 testes verdes
  </acceptance_criteria>

  <verify>
    <automated>cd mp-react && npm test -- --reporter=verbose orcamentoFluxo 2>&1 | tail -40</automated>
  </verify>

  <done>
    Todos os 6 testes de orcamentoFluxo.test.jsx passam verde. OrcamentoTecnicoPage e AprovarOrcamentoPage renderizam sem crash. updateDoc é verificado com status 'em_revisao' (ORC-03) e status 'aprovado' + assinatura_cliente (ORC-06). _clearCacheForTest() limpa o cache de useEmpresa entre cada teste.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MemoryRouter URL params → useParams | Parâmetros slug e orcamentoId injetados via rota fake — sem navegação real |
| getDoc mock → componentes reais | Componentes chamam snap.exists() e snap.data() — shape do mock deve ser exato |
| SignatureCanvas mock → handleAprovar | toDataURL retorna string fixa; props.onEnd() deve ser chamado para hasSig=true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | getDoc mock com shape errado | mitigate | Sempre mockar com { exists: () => true, id, data: () => ({...}) } — Pitfall 1 documentado no RESEARCH.md |
| T-04-05 | Denial | useEmpresa cache entre testes | mitigate | _clearCacheForTest() no beforeEach global — Pitfall 6 documentado no RESEARCH.md |
| T-04-06 | Tampering | hasSig permanece false | mitigate | Mock de canvas deve chamar props.onEnd?.() no onClick — Pitfall 3 documentado no RESEARCH.md |
| T-04-07 | Information | browser-image-compression processando imagem real no jsdom | mitigate | vi.mock('browser-image-compression') retorna Blob falso — sem processamento real |
</threat_model>

<verification>
```bash
# Dentro de mp-react/
npm test -- --reporter=verbose orcamentoFluxo
```

Saída esperada:
- 6 testes passando (ORC-01, ORC-02, ORC-03, ORC-04, ORC-05, ORC-06)
- Nenhum erro de import ou TypeError
- Nenhum aviso de act() não tratado
- updateDoc chamado com campos corretos verificado em ORC-03 e ORC-06
</verification>

<success_criteria>
- orcamentoFluxo.test.jsx existe em mp-react/src/__tests__/
- 6 testes verdes, 0 falhas
- OrcamentoTecnicoPage renderiza sem crash (ORC-01)
- Canvas de assinatura responde ao clique com onEnd (ORC-02)
- updateDoc chamado com status: 'em_revisao' (ORC-03)
- AprovarOrcamentoPage renderiza dados do orçamento (ORC-04)
- Input de nome preenchível e canvas clicável (ORC-05)
- updateDoc chamado com status: 'aprovado', aprovado_por: 'Maria Santos' e assinatura_cliente: 'data:image/png;base64,ASSINATURA' (ORC-06)
</success_criteria>

<output>
Após conclusão, criar .planning/phases/04-fluxos-criticos/04-02-SUMMARY.md com:
- Arquivo criado: mp-react/src/__tests__/orcamentoFluxo.test.jsx
- Testes: 6 (ORC-01..ORC-06) — todos verdes
- Padrão aplicado: Componentes reais + Padrão C + wrapper MemoryRouter > Routes > Route
- Decisões implementadas: D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16
- Mocks de módulos externos: react-signature-canvas, browser-image-compression, validarUpload, comprimirImagem
- Pitfalls evitados: getDoc shape (P1), getEmpresaBySlug não-null (P2), hasSig via onEnd (P3), _clearCacheForTest (P6)
</output>

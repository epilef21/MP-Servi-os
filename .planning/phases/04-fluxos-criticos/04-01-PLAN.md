---
phase: 04-fluxos-criticos
plan: "04-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/__tests__/osFluxo.test.jsx
autonomous: true
requirements:
  - OS-01
  - OS-02
  - OS-03
  - OS-04
  - OS-05
  - OS-06

must_haves:
  truths:
    - "mp-react/src/__tests__/osFluxo.test.jsx existe e contém pelo menos 6 testes (it/test)"
    - "npm test em mp-react/ passa com todos os testes de osFluxo verdes"
    - "criarOS é chamada com payload contendo seguradora, nome_segurado e status: 'aguardando_tecnico' (OS-01)"
    - "atualizarOS é chamada com os campos de atendimento ao editar e com { status } ao mudar status (OS-02, OS-03)"
    - "deleteDoc é chamada com o caminho correto ao excluir OS (OS-04)"
    - "Mensagem de estado vazio ('Nenhuma OS encontrada') aparece quando getOSdaEmpresa retorna [] (OS-05)"
    - "Dois itens aparecem na lista quando getOSdaEmpresa retorna 2 objetos (OS-06)"
  artifacts:
    - path: "mp-react/src/__tests__/osFluxo.test.jsx"
      provides: "6 testes cobrindo OS-01..OS-06 via stub components"
      min_lines: 120
  key_links:
    - from: "osFluxo.test.jsx (OSFormStub)"
      to: "criarOS mock"
      via: "import criarOS de '../firebase' (mockado via vi.mock)"
      pattern: "criarOS"
    - from: "osFluxo.test.jsx (OSExcluirStub)"
      to: "deleteDoc mock"
      via: "import deleteDoc, doc, db de '../firebase'"
      pattern: "deleteDoc"
    - from: "osFluxo.test.jsx (OSListaStub)"
      to: "getOSdaEmpresa mock"
      via: "useEffect chama getOSdaEmpresa no mount"
      pattern: "getOSdaEmpresa"
---

<objective>
Criar mp-react/src/__tests__/osFluxo.test.jsx com stub components mínimos que testam os seis comportamentos de CRUD de Ordens de Serviço sem renderizar o AdminPage.jsx (proibido por CLAUDE.md). Cada stub replica apenas a lógica do handler relevante e chama as funções Firebase mockadas.

Purpose: Cobrir OS-01..OS-06 — criar, editar, mudar status, excluir, lista vazia e lista com dados.
Output: osFluxo.test.jsx com 6 testes verdes cobrindo todos os requisitos da fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-fluxos-criticos/04-CONTEXT.md
@.planning/phases/04-fluxos-criticos/04-RESEARCH.md
@mp-react/src/test-utils/renderWithProviders.jsx
@mp-react/src/test-utils/mockFirebase.js
@mp-react/src/__tests__/rotasEmpresa.test.jsx

<interfaces>
<!-- Padrão C estabelecido em rotasEmpresa.test.jsx — OBRIGATÓRIO seguir -->
<!-- 1. vi.mock('../firebase', () => ({...})) NO TOPO, antes de qualquer import -->
<!-- 2. imports de '../firebase' DEPOIS do bloco vi.mock -->

De mp-react/src/test-utils/renderWithProviders.jsx:
```jsx
// renderWithProviders aceita (ui, { route, authOverrides })
// DEFAULT_AUTH tem: usuario: null, empresaId: null, isSuperAdmin: false, loadingAuth: false
export function renderWithProviders(ui, { route = '/', authOverrides = {} } = {})
```

De mp-react/src/firebase.js (handlers relevantes — verificado no RESEARCH.md):
```javascript
// criarOS(empresaId, payload) → retorna { id: string }
// atualizarOS(empresaId, osId, campos) → retorna undefined
// getOSdaEmpresa(empresaId) → retorna array de OS
// deleteDoc(docRef) → retorna undefined
// doc(db, collectionPath, docId) → retorna docRef
```

De mp-react/src/pages/AdminPage.jsx (linhas 357-488 — resumo):
```javascript
// saveOs: criarOS(empresaId, { seguradora, num_assist, nome_segurado, tel_segurado,
//           endereco, cidade, servico, status: 'aguardando_tecnico', ... })
// saveAtend: atualizarOS(empresaId, selected.id, atendForm)
// excluirOS: window.confirm(...) → deleteDoc(doc(db, `empresas/${empresaId}/checklist`, os.id))
```
</interfaces>
</context>

<tasks>

<task id="1" type="auto">
  <name>Criar osFluxo.test.jsx com mocks, stubs e todos os 6 testes OS-01..06</name>

  <read_first>
    - mp-react/src/test-utils/mockFirebase.js (ver quais vi.fn já existem — NÃO duplicar)
    - mp-react/src/__tests__/rotasEmpresa.test.jsx (Padrão C — estrutura exata do vi.mock)
    - mp-react/src/pages/AdminPage.jsx linhas 357-488 (excluirOS, saveOs, saveAtend — campos reais)
    - mp-react/src/firebase.js linhas 125-165 (criarOS, atualizarOS, getOSdaEmpresa — assinaturas)
    - .planning/phases/04-fluxos-criticos/04-CONTEXT.md (D-01..D-05 — decisões obrigatórias)
  </read_first>

  <action>
Criar o arquivo mp-react/src/__tests__/osFluxo.test.jsx com o conteúdo abaixo.

REGRAS CRÍTICAS (não violar):
- AdminPage.jsx NUNCA é importado (proibido por D-01 e CLAUDE.md)
- vi.mock('../firebase', ...) deve vir ANTES de qualquer import (Padrão C)
- Arquivos .jsx — extensão obrigatória (regra do OXC/Vite 8)
- clearMocks: true no vitest.config.js já limpa call counts — NÃO usar resetMocks
- window.confirm: spyOn apenas no describe de OS-04, restaurado com vi.restoreAllMocks() no afterEach

Conteúdo exato do arquivo:

```jsx
import React, { useState, useEffect } from 'react'
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { MemoryRouter } from 'react-router-dom'

// Padrão C — vi.mock ANTES de qualquer import de '../firebase'
vi.mock('../firebase', () => ({
  auth: {},
  db: {},
  onAuthStateChanged: vi.fn((auth, cb) => { cb(null); return () => {} }),
  doc: vi.fn((db, ...path) => `mock-ref:${path.join('/')}`),
  getDoc: vi.fn().mockResolvedValue({ exists: () => false }),
  updateDoc: vi.fn().mockResolvedValue(undefined),
  deleteDoc: vi.fn().mockResolvedValue(undefined),
  serverTimestamp: vi.fn().mockReturnValue({ seconds: 1000000 }),
  uploadFoto: vi.fn().mockResolvedValue('https://storage.example.com/foto.jpg'),
  criarOS: vi.fn().mockResolvedValue({ id: 'os-nova-123' }),
  atualizarOS: vi.fn().mockResolvedValue(undefined),
  getOSdaEmpresa: vi.fn().mockResolvedValue([]),
  getEmpresaBySlug: vi.fn().mockResolvedValue(null),
  getConfigEmpresa: vi.fn().mockResolvedValue({}),
  PLANOS: { basico: { limiteOS: 50, preco: 97 } },
  SUPERADMIN_EMAIL: 'superadmin@test.com',
}))

// imports DEPOIS do vi.mock — Padrão C obrigatório
import { criarOS, atualizarOS, getOSdaEmpresa, deleteDoc, doc, db } from '../firebase'

// ── Stub Components — isolam os handlers sem renderizar AdminPage ────────────

// OS-01: Criar OS
function OSFormStub({ empresaId }) {
  const [criado, setCriado] = useState(null)
  async function handleSave() {
    const payload = {
      seguradora: 'Mapfre',
      num_assist: 'OS-001',
      nome_segurado: 'João Silva',
      tel_segurado: '11999990000',
      endereco: 'Rua Teste, 10',
      cidade: 'São Paulo',
      servico: 'Instalação',
      status: 'aguardando_tecnico',
    }
    const ref = await criarOS(empresaId, payload)
    setCriado(ref.id)
  }
  return (
    <div>
      <button onClick={handleSave}>Criar OS</button>
      {criado && <span data-testid="os-criada">{criado}</span>}
    </div>
  )
}

// OS-02: Editar dados de OS (atendimento)
function OSEditStub({ empresaId, osId }) {
  const [salvo, setSalvo] = useState(false)
  async function handleEdit() {
    const atendForm = {
      nome_tecnico: 'Carlos',
      data_atend: '20/05/2026',
      descricao_servico: 'Revisão geral',
    }
    await atualizarOS(empresaId, osId, atendForm)
    setSalvo(true)
  }
  return (
    <div>
      <button onClick={handleEdit}>Salvar Edição</button>
      {salvo && <span data-testid="editado">editado</span>}
    </div>
  )
}

// OS-03: Mudar status de OS
function OSStatusStub({ empresaId, osId }) {
  const [salvo, setSalvo] = useState(false)
  async function handleStatus() {
    await atualizarOS(empresaId, osId, { status: 'concluido' })
    setSalvo(true)
  }
  return (
    <div>
      <button onClick={handleStatus}>Mudar Status</button>
      {salvo && <span data-testid="status-salvo">ok</span>}
    </div>
  )
}

// OS-04: Excluir OS com window.confirm
function OSExcluirStub({ empresaId, os, onExcluida }) {
  async function handleExcluir() {
    if (!window.confirm(`Excluir OS ${os.id}?`)) return
    await deleteDoc(doc(db, `empresas/${empresaId}/checklist`, os.id))
    onExcluida(os.id)
  }
  return <button onClick={handleExcluir}>Excluir</button>
}

// OS-05 / OS-06: Lista de OS
function OSListaStub({ empresaId }) {
  const [lista, setLista] = useState([])
  const [loading, setLoading] = useState(true)
  useEffect(() => {
    getOSdaEmpresa(empresaId).then(dados => {
      setLista(dados)
      setLoading(false)
    })
  }, [empresaId])
  if (loading) return <p>Carregando...</p>
  if (!lista.length) return <p data-testid="vazio">Nenhuma OS encontrada</p>
  return (
    <ul>
      {lista.map(os => (
        <li key={os.id} data-testid="item-os">{os.nome_segurado}</li>
      ))}
    </ul>
  )
}

// ── OS-01: Criar OS ─────────────────────────────────────────────────────────

describe('OS-01: criar OS via formulário', () => {
  it('chama criarOS com payload correto e exibe id retornado', async () => {
    criarOS.mockResolvedValue({ id: 'os-nova-123' })
    const user = userEvent.setup()

    render(<OSFormStub empresaId="emp-test-1" />)
    await user.click(screen.getByText('Criar OS'))

    await waitFor(() => {
      expect(criarOS).toHaveBeenCalledWith(
        'emp-test-1',
        expect.objectContaining({
          seguradora: 'Mapfre',
          nome_segurado: 'João Silva',
          status: 'aguardando_tecnico',
        })
      )
    })
    expect(screen.getByTestId('os-criada')).toHaveTextContent('os-nova-123')
  })
})

// ── OS-02: Editar OS ────────────────────────────────────────────────────────

describe('OS-02: editar dados de OS existente', () => {
  it('chama atualizarOS com os campos de atendimento', async () => {
    const user = userEvent.setup()

    render(<OSEditStub empresaId="emp-test-1" osId="os-abc" />)
    await user.click(screen.getByText('Salvar Edição'))

    await waitFor(() => {
      expect(atualizarOS).toHaveBeenCalledWith(
        'emp-test-1',
        'os-abc',
        expect.objectContaining({ nome_tecnico: 'Carlos' })
      )
    })
    expect(screen.getByTestId('editado')).toBeInTheDocument()
  })
})

// ── OS-03: Mudar status ─────────────────────────────────────────────────────

describe('OS-03: alterar status de OS', () => {
  it('chama atualizarOS com o novo status', async () => {
    const user = userEvent.setup()

    render(<OSStatusStub empresaId="emp-test-1" osId="os-abc" />)
    await user.click(screen.getByText('Mudar Status'))

    await waitFor(() => {
      expect(atualizarOS).toHaveBeenCalledWith(
        'emp-test-1',
        'os-abc',
        { status: 'concluido' }
      )
    })
    expect(screen.getByTestId('status-salvo')).toBeInTheDocument()
  })
})

// ── OS-04: Excluir OS ───────────────────────────────────────────────────────

describe('OS-04: excluir OS e remover da lista', () => {
  beforeEach(() => {
    vi.spyOn(window, 'confirm').mockReturnValue(true)
  })
  afterEach(() => {
    vi.restoreAllMocks()
  })

  it('chama deleteDoc com caminho correto após window.confirm', async () => {
    const onExcluida = vi.fn()
    const user = userEvent.setup()
    const os = { id: 'os-del-1' }

    render(<OSExcluirStub empresaId="emp-test-1" os={os} onExcluida={onExcluida} />)
    await user.click(screen.getByText('Excluir'))

    await waitFor(() => {
      expect(deleteDoc).toHaveBeenCalledWith('mock-ref:empresas/emp-test-1/checklist/os-del-1')
    })
    expect(onExcluida).toHaveBeenCalledWith('os-del-1')
  })
})

// ── OS-05: Lista vazia ──────────────────────────────────────────────────────

describe('OS-05: lista de OS exibe estado vazio', () => {
  it('exibe mensagem quando getOSdaEmpresa retorna array vazio', async () => {
    getOSdaEmpresa.mockResolvedValue([])

    render(
      <MemoryRouter>
        <OSListaStub empresaId="emp-test-1" />
      </MemoryRouter>
    )

    await waitFor(() => {
      expect(screen.getByTestId('vazio')).toBeInTheDocument()
    })
    expect(screen.getByTestId('vazio')).toHaveTextContent('Nenhuma OS encontrada')
  })
})

// ── OS-06: Lista com dados ──────────────────────────────────────────────────

describe('OS-06: lista de OS exibe itens retornados pelo Firebase', () => {
  it('renderiza um item por OS quando getOSdaEmpresa retorna dados', async () => {
    getOSdaEmpresa.mockResolvedValue([
      { id: 'os-1', nome_segurado: 'João Silva' },
      { id: 'os-2', nome_segurado: 'Maria Santos' },
    ])

    render(
      <MemoryRouter>
        <OSListaStub empresaId="emp-test-1" />
      </MemoryRouter>
    )

    await waitFor(() => {
      expect(screen.getAllByTestId('item-os')).toHaveLength(2)
    })
    expect(screen.getByText('João Silva')).toBeInTheDocument()
    expect(screen.getByText('Maria Santos')).toBeInTheDocument()
  })
})
```
  </action>

  <acceptance_criteria>
    - mp-react/src/__tests__/osFluxo.test.jsx existe
    - osFluxo.test.jsx contém "vi.mock('../firebase'" na linha antes de qualquer import de '../firebase'
    - osFluxo.test.jsx contém "criarOS" (função mockada e chamada no stub)
    - osFluxo.test.jsx contém "atualizarOS" (funções mockadas para OS-02 e OS-03)
    - osFluxo.test.jsx contém "deleteDoc" (mockada para OS-04)
    - osFluxo.test.jsx contém "getOSdaEmpresa" (mockada para OS-05 e OS-06)
    - osFluxo.test.jsx contém "window.confirm" (vi.spyOn para OS-04)
    - osFluxo.test.jsx contém "vi.restoreAllMocks" (limpeza do spy de window.confirm)
    - osFluxo.test.jsx NÃO contém "AdminPage" (proibido por D-01 e CLAUDE.md)
    - osFluxo.test.jsx contém "aguardando_tecnico" (campo obrigatório de OS-01)
    - osFluxo.test.jsx contém "Nenhuma OS encontrada" (mensagem de estado vazio OS-05)
    - npm test em mp-react/ com filtro osFluxo passa com 6 testes verdes
    - Nenhum teste usa "resetMocks" ou "restoreMocks" diretamente (proibido pelo vitest.config.js global)
  </acceptance_criteria>

  <verify>
    <automated>cd mp-react && npm test -- --reporter=verbose osFluxo 2>&1 | tail -30</automated>
  </verify>

  <done>
    Todos os 6 testes de osFluxo.test.jsx passam verde. criarOS, atualizarOS, deleteDoc e getOSdaEmpresa são verificados como chamados com os argumentos corretos. AdminPage.jsx nunca foi importado. window.confirm é restaurado após o describe de OS-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stub component → Firebase mock | Dados falsos injetados — sem rede real |
| vi.spyOn(window, 'confirm') | Substitui função do browser — restaurar após cada teste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | vi.spyOn(window.confirm) | mitigate | Restaurar com vi.restoreAllMocks() no afterEach do describe de OS-04 — implementado no stub |
| T-04-02 | Denial | clearMocks:true + mockImplementation | accept | clearMocks limpa call counts mas não implementations — o vi.mock inicial define defaults seguros para todos os testes |
| T-04-03 | Information | AdminPage.jsx (3264 linhas) | mitigate | AdminPage NUNCA importado — D-01 e CLAUDE.md; stub components replicam apenas o handler mínimo |
</threat_model>

<verification>
```bash
# Dentro de mp-react/
npm test -- --reporter=verbose osFluxo
```

Saída esperada:
- 6 testes passando (OS-01, OS-02, OS-03, OS-04, OS-05, OS-06)
- Nenhum erro de import ou TypeError
- Nenhum aviso de "act(...)" não tratado
</verification>

<success_criteria>
- osFluxo.test.jsx existe em mp-react/src/__tests__/
- 6 testes verdes, 0 falhas
- criarOS chamada com status: 'aguardando_tecnico' (OS-01)
- atualizarOS chamada com campos de atendimento (OS-02) e com { status: 'concluido' } (OS-03)
- deleteDoc chamada com caminho 'mock-ref:empresas/emp-test-1/checklist/os-del-1' (OS-04)
- Mensagem 'Nenhuma OS encontrada' visível quando lista vazia (OS-05)
- 2 items renderizados quando mock retorna 2 objetos (OS-06)
- AdminPage.jsx nunca importado no arquivo de teste
</success_criteria>

<output>
Após conclusão, criar .planning/phases/04-fluxos-criticos/04-01-SUMMARY.md com:
- Arquivo criado: mp-react/src/__tests__/osFluxo.test.jsx
- Testes: 6 (OS-01..OS-06) — todos verdes
- Padrão aplicado: Stub Components + Padrão C (vi.mock inline)
- Decisões implementadas: D-01, D-02, D-03, D-04, D-05
- Mocks adicionados ao vi.mock inline: criarOS, atualizarOS, getOSdaEmpresa, deleteDoc, doc, uploadFoto, serverTimestamp
</output>

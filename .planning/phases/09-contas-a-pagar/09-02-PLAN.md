---
phase: 09-contas-a-pagar
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx
autonomous: true
requirements: [PAG-01, PAG-03]

must_haves:
  truths:
    - "Admin cadastra um dia de vencimento (1–31) numa despesa recorrente fixa"
    - "Ao lançar/editar uma despesa mensal, admin informa a data de vencimento"
    - "O auto-lançamento de fixas preenche a data_vencimento do mês automaticamente a partir do dia_vencimento cadastrado (com clamp de fim de mês)"
  artifacts:
    - path: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      provides: "Campos de vencimento nos modais/forms e auto-lançamento com data_vencimento"
      contains: "dataVencimentoDoMes"
  key_links:
    - from: "FinanceiroEmpresaTab.jsx autoLancarFixas"
      to: "mp-react/src/utils/contasPagar.js dataVencimentoDoMes"
      via: "import + chamada ao montar o payload do addDoc"
      pattern: "dataVencimentoDoMes\\("
    - from: "salvarRecorrente payload"
      to: "despesasRecorrentes.dia_vencimento"
      via: "campo gravado no Firestore"
      pattern: "dia_vencimento"
    - from: "salvarMensal payload"
      to: "despesasMensais.data_vencimento"
      via: "campo gravado no Firestore"
      pattern: "data_vencimento"
---

<objective>
Adicionar o caminho de ESCRITA de vencimento em Contas a Pagar: campo `dia_vencimento` (1–31) no cadastro de despesa recorrente, campo `data_vencimento` no lançamento mensal, e fazer o auto-lançamento de fixas preencher automaticamente a `data_vencimento` do mês a partir do `dia_vencimento` cadastrado.

Purpose: Entregar PAG-03 (fixas entram no mês já com vencimento) e a base de escrita de PAG-01 (despesa mensal ganha data de vencimento). O caminho de LEITURA (badges de status, botão Pagar, banner) fica no plano 09-03.
Output: `FinanceiroEmpresaTab.jsx` atualizado (forms iniciais, modais, handlers de salvar/editar, autoLancarFixas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-contas-a-pagar/09-CONTEXT.md
@.planning/phases/09-contas-a-pagar/09-01-SUMMARY.md

<interfaces>
<!-- Do plano 09-01 (mp-react/src/utils/contasPagar.js) -->
```js
// dia (1–31) + mesRef 'YYYY-MM' → 'YYYY-MM-DD' com clamp de fim de mês; null se inválido
export function dataVencimentoDoMes(diaVencimento, mesRef)
```

<!-- Estado atual do FinanceiroEmpresaTab.jsx (LOCKED — casar com o código real) -->
FORM_DESP_RECORRENTE_INICIAL (linha ~81): { descricao, categoria, valor, tipo }
FORM_DESP_MENSAL_INICIAL (linha ~84): { descricao, categoria, valor, despesa_recorrente_id, data_pagamento }
autoLancarFixas (linha ~205): monta payload { despesa_recorrente_id, descricao, categoria, valor, mes_referencia, data_pagamento:'', criado_em }
salvarRecorrente (linha ~240): payload { descricao, categoria, valor, tipo, ativa }
abrirEditarRecorrente (linha ~235): setForm a partir de r
salvarMensal (linha ~292): payload { descricao, categoria, valor, mes_referencia, data_pagamento, despesa_recorrente_id }
abrirEditarMensal (linha ~281): setForm a partir de l
ModalDespesaRecorrente (linha ~1151): campos descricao, categoria, valor, tipo
ModalLancamentoMensal (linha ~1222): campos descricao, categoria, valor, data_pagamento (input type="date")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dia_vencimento no cadastro de despesa recorrente (form + modal + salvar/editar)</name>
  <read_first>
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx (linhas 81–83 FORM_DESP_RECORRENTE_INICIAL; 235–260 abrirEditarRecorrente/salvarRecorrente; 1151–1219 ModalDespesaRecorrente)
  </read_first>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx</files>
  <action>
    1. Adicionar `dia_vencimento: ''` ao FORM_DESP_RECORRENTE_INICIAL.
    2. Em abrirEditarRecorrente, incluir `dia_vencimento: r.dia_vencimento ? String(r.dia_vencimento) : ''` (compat legado: undefined vira '').
    3. Em salvarRecorrente, no payload, gravar `dia_vencimento` como número quando preenchido: adicionar `dia_vencimento: formRecorrente.dia_vencimento ? Number(formRecorrente.dia_vencimento) : null` (null quando vazio — não persistir string). Não tornar obrigatório (opcional per D CONTEXT).
    4. No ModalDespesaRecorrente, adicionar um campo "Dia de Vencimento" (input type="number" min="1" max="31" placeholder="Ex: 10") ao lado/abaixo de Valor Base, ligado a form.dia_vencimento. Texto de ajuda curto para usuário não técnico: "Dia do mês em que vence (opcional)".
    O dia_vencimento só é útil para tipo 'fixa' (o auto-lançamento usa), mas pode ficar visível para ambos os tipos — sem lógica condicional extra.
  </action>
  <verify>
    <automated>cd mp-react && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dia_vencimento" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 4 (form inicial, editar, salvar, modal)
    - `grep -n 'type="number"' ...FinanceiroEmpresaTab.jsx` inclui o novo campo com min/max 1–31
    - `cd mp-react && npm test` permanece verde
  </acceptance_criteria>
  <done>Cadastro de recorrente aceita e grava dia_vencimento (número ou null); edição repovoa o campo.</done>
</task>

<task type="auto">
  <name>Task 2: data_vencimento no lançamento mensal + auto-lançamento de fixas preenchendo vencimento</name>
  <read_first>
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx (linhas 84–87 FORM_DESP_MENSAL_INICIAL; 205–227 autoLancarFixas; 281–313 abrirEditarMensal/salvarMensal; 1222–1282 ModalLancamentoMensal)
    - mp-react/src/utils/contasPagar.js (dataVencimentoDoMes)
  </read_first>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx</files>
  <action>
    1. Importar `dataVencimentoDoMes` de '../../utils/contasPagar.js' no topo do arquivo.
    2. Adicionar `data_vencimento: ''` ao FORM_DESP_MENSAL_INICIAL.
    3. Em abrirEditarMensal, incluir `data_vencimento: l.data_vencimento || ''` (compat legado).
    4. Em salvarMensal, adicionar `data_vencimento: formMensal.data_vencimento || ''` ao payload.
    5. No ModalLancamentoMensal, adicionar campo "Data de Vencimento" (input type="date") ligado a form.data_vencimento, acima ou ao lado do campo "Data de Pagamento" existente. Rótulos claros: "Vence em" vs "Pago em".
    6. Em autoLancarFixas, ao montar o payload de cada fixa, calcular `data_vencimento: f.dia_vencimento ? (dataVencimentoDoMes(f.dia_vencimento, mesRef) || '') : ''` — assim a fixa entra no mês já com o vencimento preenchido (PAG-03), com clamp de fim de mês vindo do util. Manter `data_pagamento: ''` como está.
  </action>
  <verify>
    <automated>cd mp-react && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data_vencimento" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 5 (form inicial, editar, salvar, modal input, autoLancar)
    - `grep -c "dataVencimentoDoMes" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 2 (import + chamada em autoLancarFixas)
    - `grep -c "from '../../utils/contasPagar" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna 1
    - `cd mp-react && npm test` permanece verde
  </acceptance_criteria>
  <done>Lançamento mensal grava/edita data_vencimento; auto-lançamento de fixas preenche data_vencimento do mês via dataVencimentoDoMes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → Firestore (despesasRecorrentes/despesasMensais) | Escrita de campos novos por usuário autenticado da empresa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | dia_vencimento fora de 1–31 | mitigate | input type=number min=1 max=31; grava Number() e o clamp de dataVencimentoDoMes trata valores extremos; null quando vazio |
| T-09-04 | Tampering | data_vencimento manual malformada | mitigate | input type="date" (formato 'YYYY-MM-DD' garantido pelo browser); leitura tolera ausência (legado) |
| T-09-05 | Elevation of Privilege | escrita nas coleções | accept | Regra Firestore existente de despesasRecorrentes/despesasMensais já cobre (sem regra nova per CONTEXT) |
</threat_model>

<verification>
- `cd mp-react && npm test` verde (nenhum teste renderiza FinanceiroEmpresaTab inteiro; mudanças de UI são seguras)
- Campo dia_vencimento aparece no ModalDespesaRecorrente e data_vencimento no ModalLancamentoMensal
- autoLancarFixas chama dataVencimentoDoMes
</verification>

<success_criteria>
- PAG-03: fixas auto-lançadas entram com data_vencimento preenchida
- PAG-01 (escrita): despesa mensal aceita e grava data_vencimento; recorrente grava dia_vencimento
- Compat legado: campos ausentes viram '' na leitura de edição; suite total verde
</success_criteria>

<output>
After completion, create `.planning/phases/09-contas-a-pagar/09-02-SUMMARY.md`
</output>

---
phase: 09-contas-a-pagar
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx
  - mp-react/src/index.css
autonomous: true
requirements: [PAG-01, PAG-02]

must_haves:
  truths:
    - "Cada despesa do mês exibe um badge de status: pendente, pago ou atrasado"
    - "O item da despesa mostra o vencimento e uma mensagem direta (ex.: 'Vence em 3 dias', 'Atrasada há 5 dias')"
    - "O botão '✓ Pagar' preenche a data de pagamento com hoje após confirmação"
    - "Um banner no topo do Financeiro (visível em qualquer aba interna) soma contas a vencer em ≤7 dias e contas atrasadas, com botão que leva à aba Despesas"
  artifacts:
    - path: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      provides: "Leitura de status na aba Despesas + botão Pagar + banner de alertas no topo"
      contains: "resumoAlertas"
    - path: "mp-react/src/index.css"
      provides: "Classe do badge de atrasado (vermelho) e banner de alerta de contas"
      contains: "despesa-status-atrasado"
  key_links:
    - from: "banner no topo do FinanceiroEmpresaTab"
      to: "mp-react/src/utils/contasPagar.js resumoAlertas"
      via: "resumoAlertas(despesasMensais, hojeISO)"
      pattern: "resumoAlertas\\("
    - from: "item da despesa na AbaDespesas"
      to: "mp-react/src/utils/contasPagar.js statusDespesa"
      via: "statusDespesa(l, hojeISO) para escolher o badge"
      pattern: "statusDespesa\\("
    - from: "botão Pagar"
      to: "despesasMensais.data_pagamento"
      via: "updateDoc gravando hoje após window.confirm"
      pattern: "data_pagamento"
---

<objective>
Adicionar o caminho de LEITURA de Contas a Pagar: badge de status (pendente/pago/atrasado) e mensagem de vencimento em cada item da aba Despesas, botão rápido "✓ Pagar" (grava data_pagamento = hoje após confirmação) e um banner de alerta no topo do Financeiro somando contas a vencer em ≤7 dias e atrasadas, com botão que leva à aba Despesas.

Purpose: Entrega PAG-01 (status visível) e PAG-02 (alerta de contas a vencer/atrasadas). Consome as funções puras de 09-01 e os campos de vencimento gravados por 09-02.
Output: `FinanceiroEmpresaTab.jsx` (banner + item da despesa + handler pagar) e classe CSS do badge atrasado em `index.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-contas-a-pagar/09-CONTEXT.md
@.planning/phases/09-contas-a-pagar/09-01-SUMMARY.md
@.planning/phases/09-contas-a-pagar/09-02-SUMMARY.md

<interfaces>
<!-- Do plano 09-01 (mp-react/src/utils/contasPagar.js) -->
```js
export function statusDespesa(despesa, hojeISO)   // 'pago' | 'atrasado' | 'pendente'
export function diasParaVencer(dataVencimento, hojeISO) // inteiro (negativo=vencida) ou null
export function resumoAlertas(despesas, hojeISO)  // { aVencer, atrasadas, total }
```

<!-- Estado atual do FinanceiroEmpresaTab.jsx (LOCKED — casar com o código real) -->
Componente principal FinanceiroEmpresaTab (linha ~100): tem `despesasMensais` state e `abaFin`/`setAbaFin`.
Render principal (linha ~457): cabeçalho + seletor de período (linha ~461), depois `.fin-tabs` (linha ~471), depois as abas.
AbaDespesas (linha ~842): recebe props; item da despesa renderizado no map de despesasMensais (linhas ~937–961) com className `despesa-item pendente|lancado` e usa `.despesa-status-pend` / `.despesa-status-ok`.
Handlers de mensal já existem: excluirMensal, abrirEditarMensal; updateDoc/doc já importados (linhas 5–18).

<!-- Classes CSS existentes (mp-react/src/index.css) -->
.despesa-status-ok  { color: var(--success) }   (linha ~1597)
.despesa-status-pend{ color: #f0a500 }           (linha ~1598)
.despesa-item.pendente / .despesa-item.lancado   (linhas ~1577–1581)
.despesa-alerta (banner de variáveis pendentes já existe, reutilizar o estilo como referência)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge de status + mensagem de vencimento + botão Pagar no item da despesa</name>
  <read_first>
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx (linhas 842–977 AbaDespesas, foco no map de despesasMensais 937–961; linhas 292–320 salvarMensal/excluirMensal como referência de handler)
    - mp-react/src/utils/contasPagar.js (statusDespesa, diasParaVencer)
    - mp-react/src/index.css (linhas 1565–1600 bloco .despesa-item / .despesa-status-*)
  </read_first>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx, mp-react/src/index.css</files>
  <action>
    1. Importar `statusDespesa`, `diasParaVencer` de '../../utils/contasPagar.js' (juntar ao import se 09-02 já criou a linha).
    2. Criar helper local `hojeISO()` no arquivo (padStart, mesmo estilo de mesAtual()) — retorna 'YYYY-MM-DD' de hoje. NÃO usar toISOString.
    3. Adicionar um handler `pagarMensal(l)` no componente principal: `window.confirm('Marcar "'+l.descricao+'" como paga hoje?')` → se ok, `updateDoc(doc(db, .../despesasMensais, l.id), { data_pagamento: hojeISO() })` → `carregarDados()`. Passar como prop `onPagarMensal` para AbaDespesas.
    4. Em AbaDespesas, no map de despesasMensais, calcular `const st = statusDespesa(l, hojeISO())` e `const dias = diasParaVencer(l.data_vencimento, hojeISO())`. Trocar o badge fixo atual por:
       - st === 'pago' → `.despesa-status-ok` "✅ Pago"
       - st === 'atrasado' → `.despesa-status-atrasado` "⚠️ Atrasada há N dia(s)" (N = Math.abs(dias))
       - st === 'pendente' com dias != null → `.despesa-status-pend` "Vence em N dia(s)" / "Vence hoje" (dias===0) 
       - st === 'pendente' sem data_vencimento (dias===null) → manter "⚠️ Pendente" (legado, sem vencimento)
       Preservar o comportamento `ehPend` das variáveis sem valor (mostrar Pendente). Aplicar className do container `despesa-item` com sufixo `atrasado` quando st==='atrasado'.
       Mensagens diretas para usuário não técnico (ex.: "Aluguel vence em 3 dias", "Água e Luz atrasada há 5 dias") — usar a descrição já visível + a frase de dias.
    5. Adicionar botão "✓ Pagar" no `.despesa-item-right` visível apenas quando st !== 'pago' (`onClick={() => onPagarMensal(l)}`), antes dos botões editar/excluir.
    6. Em index.css, adicionar `.despesa-status-atrasado { font-size:.78rem; color: var(--danger); font-weight:700; }` e (opcional) `.despesa-item.atrasado { border-left: 3px solid var(--danger); }` no mesmo bloco das outras .despesa-status-*.
  </action>
  <verify>
    <automated>cd mp-react && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "statusDespesa(" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 1
    - `grep -c "onPagarMensal\|pagarMensal" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 3 (handler, prop passada, uso)
    - `grep -c "despesa-status-atrasado" mp-react/src/index.css` retorna 1
    - `grep -c "toISOString" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna 1 (inalterado do baseline — só o uso pré-existente em calcularDRE; o helper hojeISO NÃO introduz uso novo)
    - `cd mp-react && npm test` permanece verde
  </acceptance_criteria>
  <done>Item da despesa mostra badge pendente/pago/atrasado + mensagem de dias; botão ✓ Pagar grava data_pagamento=hoje após confirmar.</done>
</task>

<task type="auto">
  <name>Task 2: Banner de alerta de contas (PAG-02) no topo do Financeiro</name>
  <read_first>
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx (linhas 457–486 render principal: cabeçalho, seletor de período, .fin-tabs; onde inserir o banner)
    - mp-react/src/utils/contasPagar.js (resumoAlertas)
    - mp-react/src/index.css (bloco .despesa-alerta como referência de estilo de banner)
  </read_first>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx, mp-react/src/index.css</files>
  <action>
    1. Importar `resumoAlertas` de '../../utils/contasPagar.js' (juntar ao import existente).
    2. No render principal, calcular `const alertas = resumoAlertas(despesasMensais, hojeISO())` (usa apenas o mês corrente já carregado — decisão de discrição documentada no CONTEXT: limitar ao mês corrente evita query extra cara; documentar no SUMMARY).
    3. Inserir um banner entre o seletor de período (linha ~468) e o `.fin-tabs` (linha ~471), renderizado só quando `alertas.total > 0` E não estiver carregando. Mensagem-resumo direta para usuário não técnico, montando o texto conforme os números:
       - Ex.: "🔔 3 contas vencem esta semana · 1 atrasada" (pluralização correta; omitir a metade que for 0, ex.: só "2 contas atrasadas" se aVencer===0).
       - Botão "Ver despesas" com `onClick={() => setAbaFin('despesas')}`. Se já estiver na aba despesas, o botão pode continuar (rola/foca) ou ser ocultado — manter simples: sempre setAbaFin('despesas').
    4. Adicionar classe CSS `.fin-alerta-contas` em index.css (banner de destaque: fundo âmbar claro, borda, padding, flex com o botão à direita) reaproveitando as variáveis existentes; não conflitar com .despesa-alerta.
    O banner deve aparecer independentemente da aba interna ativa (por estar no render principal, antes do switch de abaFin) — satisfaz "visível em qualquer aba interna".
  </action>
  <verify>
    <automated>cd mp-react && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resumoAlertas(" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 1
    - `grep -c "fin-alerta-contas" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 1 e em index.css retorna 1
    - `grep -c "setAbaFin('despesas')" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna >= 1 (botão do banner)
    - `cd mp-react && npm test` permanece verde
  </acceptance_criteria>
  <done>Banner aparece no topo do Financeiro em qualquer aba quando há contas a vencer em ≤7 dias ou atrasadas, com botão que leva à aba Despesas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → Firestore (despesasMensais) | Escrita de data_pagamento via botão Pagar |
| status derivado (cliente) | Exibição apenas — nunca gravado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | botão Pagar grava data errada | mitigate | window.confirm antes de gravar; hojeISO() com padStart local; grava só data_pagamento |
| T-09-07 | Repudiation | status atrasado divergente por fuso | mitigate | diasParaVencer usa Date.UTC (09-01); hojeISO local consistente com o resto do componente |
| T-09-08 | Information Disclosure | banner soma só dados já carregados | accept | Escopo limitado ao mês corrente (sem query extra); documentado no CONTEXT como discrição |
</threat_model>

<verification>
- `cd mp-react && npm test` verde (nenhum teste renderiza FinanceiroEmpresaTab inteiro)
- Badge atrasado/pendente/pago aparece por item; botão Pagar grava data_pagamento
- Banner no topo do Financeiro visível em qualquer aba interna quando há alertas
</verification>

<success_criteria>
- PAG-01: status pendente/pago/atrasado visível por despesa; botão Pagar funcional
- PAG-02: banner de contas a vencer (≤7 dias) e atrasadas com botão para a aba Despesas
- Compat legado: despesas sem data_vencimento continuam como pendente/pago sem quebrar; suite total verde
</success_criteria>

<output>
After completion, create `.planning/phases/09-contas-a-pagar/09-03-SUMMARY.md`
</output>

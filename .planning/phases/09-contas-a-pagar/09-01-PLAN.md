---
phase: 09-contas-a-pagar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/utils/contasPagar.js
  - mp-react/src/__tests__/contasPagar.test.js
autonomous: true
requirements: [PAG-01, PAG-02, PAG-03]

must_haves:
  truths:
    - "statusDespesa retorna 'pago' quando há data_pagamento, 'atrasado' quando venceu e não foi paga, 'pendente' caso contrário"
    - "dataVencimentoDoMes faz clamp de fim de mês: dia 31 em mês de 30 dias vira o último dia real do mês"
    - "diasParaVencer retorna número negativo para contas vencidas e null quando não há data de vencimento"
    - "resumoAlertas soma corretamente contas a vencer em até 7 dias e contas atrasadas, ignorando as já pagas"
  artifacts:
    - path: "mp-react/src/utils/contasPagar.js"
      provides: "Lógica pura de vencimento/status de despesas (sem Firebase/React)"
      exports: ["statusDespesa", "diasParaVencer", "dataVencimentoDoMes", "resumoAlertas"]
    - path: "mp-react/src/__tests__/contasPagar.test.js"
      provides: "Testes Vitest cobrindo status, clamp de fim de mês, dias e resumo de alertas"
      contains: "dataVencimentoDoMes"
  key_links:
    - from: "mp-react/src/__tests__/contasPagar.test.js"
      to: "mp-react/src/utils/contasPagar.js"
      via: "import { statusDespesa, diasParaVencer, dataVencimentoDoMes, resumoAlertas }"
      pattern: "from '\\.\\./utils/contasPagar'"
---

<objective>
Criar o utilitário puro `contasPagar.js` com a lógica de vencimento e status das despesas (status pendente/pago/atrasado, dias para vencer, data de vencimento do mês com clamp de fim de mês e resumo de alertas), coberto por testes Vitest.

Purpose: Toda a regra de datas de Contas a Pagar (PAG-01, PAG-02, PAG-03) fica em um módulo testável e sem dependência de Firebase/React — mesmo padrão de `faturamento.js` (Fase 7) e `fechamentoTecnicos.js` (Fase 8). Os planos de UND UI (09-02, 09-03) consomem estas funções.
Output: `mp-react/src/utils/contasPagar.js` + `mp-react/src/__tests__/contasPagar.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-contas-a-pagar/09-CONTEXT.md

<interfaces>
<!-- Padrão de utilitário puro + helper de data local (evita bug de timezone do toISOString).
     Extraído de mp-react/src/utils/faturamento.js e fechamentoTecnicos.js. -->

De mp-react/src/utils/faturamento.js (padrão de helper de data local — NÃO usar toISOString):
```js
function fmtISO(d) {
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`
}
export function proximoDiaUtil(date) { /* ... */ }
```

De mp-react/src/utils/fechamentoTecnicos.js (padrão de status derivado — nunca gravado):
```js
export function statusFechamento(fechamento) {
  return fechamento?.status === 'pago' ? 'pago' : 'pendente'
}
```

Modelo de dados (LOCKED no 09-CONTEXT.md — estende coleções existentes, sem coleção nova):
- despesasMensais.data_vencimento: string 'YYYY-MM-DD' (opcional; lançamentos antigos não têm)
- despesasMensais.data_pagamento: string 'YYYY-MM-DD' existente = "pago em" (preenchida = paga)
- despesasRecorrentes.dia_vencimento: número 1–31 (opcional)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar utils/contasPagar.js com as funções puras de vencimento/status</name>
  <read_first>
    - mp-react/src/utils/faturamento.js (linhas 61–96: proximoDiaUtil, fmtISO, parsing de 'YYYY-MM-DD' via split — copiar o mesmo estilo, NUNCA toISOString)
    - mp-react/src/utils/fechamentoTecnicos.js (padrão de status derivado)
  </read_first>
  <files>mp-react/src/utils/contasPagar.js</files>
  <behavior>
    - statusDespesa(despesa, hojeISO): 'pago' se despesa.data_pagamento for string não vazia; senão 'atrasado' se despesa.data_vencimento existe E hojeISO > data_vencimento (comparação lexicográfica de 'YYYY-MM-DD', equivalente porque mesmo formato/tamanho); senão 'pendente'. Vencimento HOJE (hojeISO === data_vencimento) ainda é 'pendente', não 'atrasado'.
    - Despesa sem data_vencimento nunca é 'atrasado' (compat legado tratada na leitura): retorna 'pago' ou 'pendente'.
    - diasParaVencer(dataVencimento, hojeISO): retorna null se dataVencimento vazia/ausente; senão inteiro = dias de hoje até o vencimento (negativo = já venceu, 0 = vence hoje). Usar Date.UTC(y, m-1, d) para ambas as datas e dividir por 86400000 (à prova de fuso/DST).
    - dataVencimentoDoMes(diaVencimento, mesRef): diaVencimento número 1–31, mesRef 'YYYY-MM' → 'YYYY-MM-DD'. Clamp de fim de mês: dia 31 em mês de 30 dias → último dia real; dia 30 em fevereiro → 28/29. Retorna null se diaVencimento não for número válido 1–31 ou mesRef inválido. Formatar com padStart local (NÃO toISOString).
    - resumoAlertas(despesas, hojeISO): recebe array de despesasMensais + hojeISO → { aVencer, atrasadas, total }. aVencer = qtd de despesas NÃO pagas, com data_vencimento, cujo diasParaVencer está entre 0 e 7 (inclusive). atrasadas = qtd de despesas NÃO pagas, com data_vencimento, cujo diasParaVencer < 0. total = aVencer + atrasadas. Reutilizar statusDespesa/diasParaVencer internamente.
  </behavior>
  <action>
    Criar o módulo 100% puro (sem import de Firebase/React), com cabeçalho comentado no mesmo estilo de fechamentoTecnicos.js. Implementar `statusDespesa`, `diasParaVencer`, `dataVencimentoDoMes`, `resumoAlertas` exatamente conforme o bloco <behavior>. Para o clamp em dataVencimentoDoMes: calcular o último dia do mês com `new Date(ano, mes, 0).getDate()` e usar `Math.min(diaVencimento, ultimoDia)`. Formatar a saída com um helper local `fmtISO` (padStart), NUNCA toISOString (bug de timezone, aprendizado LOCKED da Fase 7). Status é sempre DERIVADO, nunca gravado (mesmo padrão das notas da Fase 7 e fechamentos da Fase 8).
  </action>
  <verify>
    <automated>node --check "mp-react/src/utils/contasPagar.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export function (statusDespesa|diasParaVencer|dataVencimentoDoMes|resumoAlertas)" mp-react/src/utils/contasPagar.js` retorna as 4 funções
    - `grep -c "toISOString" mp-react/src/utils/contasPagar.js` retorna 0 (usa padStart local)
  </acceptance_criteria>
  <done>As 4 funções existem, são puras e o arquivo de teste (Task 2) passa contra elas.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever contasPagar.test.js cobrindo status, clamp, dias e resumo</name>
  <read_first>
    - mp-react/src/__tests__/fechamentoTecnicos.test.js (padrão de estrutura describe/it, imports relativos '../utils/...')
  </read_first>
  <files>mp-react/src/__tests__/contasPagar.test.js</files>
  <action>
    Criar suíte Vitest importando de '../utils/contasPagar'. Cobrir no mínimo:
    - statusDespesa: pago (com data_pagamento), atrasado (vencimento passado, sem pagamento), pendente (vencimento futuro), pendente no dia do vencimento (hoje === vencimento), pendente/pago sem data_vencimento (legado nunca vira atrasado).
    - diasParaVencer: null sem vencimento; 0 no dia; positivo para futuro; negativo para passado; caso de virada de mês (ex.: hoje '2026-07-31', vencimento '2026-08-02' → 2).
    - dataVencimentoDoMes: dia 10 → 'YYYY-MM-10'; clamp dia 31 em mês de 30 dias (ex.: 31 + '2026-04' → '2026-04-30'); clamp dia 30 em fevereiro não bissexto (30 + '2026-02' → '2026-02-28'); fevereiro bissexto (30 + '2024-02' → '2024-02-29'); dia inválido/0 → null.
    - resumoAlertas: mistura de pagas/atrasadas/a-vencer-em-7-dias/futuras-alem-de-7-dias/sem-vencimento, conferindo { aVencer, atrasadas, total } e que pagas são ignoradas.
    Passar `hojeISO` fixo nos testes (ex.: '2026-07-05') — nunca depender de new Date() real.
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run src/__tests__/contasPagar.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '../utils/contasPagar'" mp-react/src/__tests__/contasPagar.test.js` retorna 1
    - `cd mp-react && npx vitest run src/__tests__/contasPagar.test.js` passa (todos verdes)
    - `cd mp-react && npm test` continua verde (suite total >= 129 + novos)
  </acceptance_criteria>
  <done>Suíte de testes de contasPagar passa e a suite total permanece verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → módulo puro | Recebe dados já lidos do Firestore; sem I/O próprio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | statusDespesa/diasParaVencer com datas malformadas | mitigate | Guardas de tipo: retornar 'pendente'/null quando data ausente/inválida em vez de lançar (compat legado) |
| T-09-02 | Information Disclosure | módulo puro | accept | Sem acesso a rede/segredos; opera só sobre objetos recebidos |
</threat_model>

<verification>
- `cd mp-react && npx vitest run src/__tests__/contasPagar.test.js` verde
- `cd mp-react && npm test` mantém a suite total verde
- contasPagar.js não importa Firebase nem React
</verification>

<success_criteria>
- statusDespesa, diasParaVencer, dataVencimentoDoMes (com clamp de fim de mês) e resumoAlertas implementados e testados
- Nenhum uso de toISOString (formatação local com padStart)
- Suite de testes total permanece verde
</success_criteria>

<output>
After completion, create `.planning/phases/09-contas-a-pagar/09-01-SUMMARY.md`
</output>

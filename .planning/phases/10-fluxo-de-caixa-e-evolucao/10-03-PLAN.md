---
phase: 10-fluxo-de-caixa-e-evolucao
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - mp-react/src/components/admin/caixa/AbaCaixa.jsx
autonomous: true
requirements: [CAIXA-02]

must_haves:
  truths:
    - "Admin vê, abaixo do resumo do mês na aba 💵 Caixa, o gráfico de evolução dos últimos 12 meses"
    - "Cada mês mostra barra de receita, barra de lucro líquido e a margem % como texto"
    - "Lucro líquido negativo aparece em vermelho"
    - "O gráfico é feito só com divs/CSS — nenhuma biblioteca de gráfico foi adicionada ao package.json"
    - "12 meses sem nenhum dado → mensagem explicativa em vez de gráfico vazio"
  artifacts:
    - path: "mp-react/src/components/admin/caixa/AbaCaixa.jsx"
      provides: "Seção de evolução 12 meses com barras CSS"
      contains: "serieEvolucao12Meses"
  key_links:
    - from: "mp-react/src/components/admin/caixa/AbaCaixa.jsx"
      to: "mp-react/src/utils/fluxoCaixa.js"
      via: "import serieEvolucao12Meses"
      pattern: "serieEvolucao12Meses"
---

<objective>
Adicionar à aba 💵 Caixa o gráfico de evolução dos últimos 12 meses (CAIXA-02): barras CSS simples (sem lib nova — LOCKED) com receita e lucro líquido por mês e margem % como texto, usando `serieEvolucao12Meses` (plano 10-01) sobre os dados já carregados pelo plano 10-02.

Purpose: o admin enxerga como o negócio evoluiu no último ano — receita, lucro e margem mês a mês.
Output: seção "📊 Evolução 12 meses" dentro do AbaCaixa.jsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-fluxo-de-caixa-e-evolucao/10-CONTEXT.md
@mp-react/src/components/admin/caixa/AbaCaixa.jsx
@mp-react/src/utils/fluxoCaixa.js

<interfaces>
<!-- Contrato do plano 10-01: -->
```js
serieEvolucao12Meses({ reports, particulares, despesas, deducoes, resultadoFinanceiro }, mesRef)
// → [{ mes: 'YYYY-MM', receita: Number, lucroLiquido: Number, margem: Number }] × 12 (cronológico)
```
<!-- Dados disponíveis no AbaCaixa (plano 10-02): states particulares/despesas/deducoes/resultadoFinanceiro
     (leituras completas, todos os meses) + reports vindo de useAdminContext() (todas as OS em memória). -->
```js
const { empresaId, reports, showToast } = useAdminContext()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seção "📊 Evolução 12 meses" com barras CSS</name>
  <files>mp-react/src/components/admin/caixa/AbaCaixa.jsx</files>
  <read_first>mp-react/src/components/admin/caixa/AbaCaixa.jsx (estado atual pós 10-02 — quais states existem), mp-react/src/utils/fluxoCaixa.js (assinatura de serieEvolucao12Meses)</read_first>
  <action>
No AbaCaixa.jsx, abaixo do detalhamento do mês (posição LOCKED: "gráfico dentro da mesma aba, abaixo do resumo do mês"):

1. Garantir que `reports` vem de `useAdminContext()` e calcular `const serie = serieEvolucao12Meses({ reports, particulares, despesas, deducoes, resultadoFinanceiro }, mesRef)` — importar de `'../../../utils/fluxoCaixa.js'`. Recalcula ao navegar de mês (janela de 12 meses termina no mesRef).

2. **Gráfico só com divs + inline styles (LOCKED: zero dependência nova):**
   - Container flex horizontal com 12 colunas (uma por mês), `alignItems: 'flex-end'`, altura fixa da área de barras (ex.: 120px), `overflowX: 'auto'` para telas estreitas.
   - Escala: `maxVal = Math.max(...serie.map(p => Math.max(p.receita, Math.abs(p.lucroLiquido))), 1)` (o `1` evita divisão por zero). Altura de cada barra = `(valor / maxVal) * 120` px, mínimo 2px quando valor > 0.
   - Por mês, duas barras lado a lado: receita (cor `var(--primary)`) e lucro (`#2e7d32` se ≥ 0, `var(--danger)` se < 0; lucro negativo usa `Math.abs` na altura e cor vermelha).
   - `title` (tooltip nativo) em cada barra: `Receita: {fmtBRL(p.receita)}` / `Lucro: {fmtBRL(p.lucroLiquido)}`.
   - Sob cada coluna: label do mês `MMM/AA` (helper local: split de 'YYYY-MM' + array de abreviações `['jan',...,'dez']` — sem `new Date`, sem toISOString) e a margem como texto pequeno: `{p.margem.toFixed(0)}%` (discretion do CONTEXT: margem como linha de texto sob a barra), em vermelho quando negativa.
   - Legenda simples acima do gráfico: ▪ Receita ▪ Lucro líquido.

3. **Empty state explicativo:** se `serie.every(p => p.receita === 0 && p.lucroLiquido === 0)`, renderizar em vez do gráfico: "Ainda não há histórico para montar a evolução — os meses vão aparecendo aqui conforme as OS e lançamentos forem registrados."

4. NÃO instalar nada (`package.json` intocado), NÃO criar arquivo CSS novo (inline styles, padrão das abas), NÃO alterar utils (a matemática já vem pronta do 10-01).
  </action>
  <verify>
    <automated>grep -c "serieEvolucao12Meses" mp-react/src/components/admin/caixa/AbaCaixa.jsx && grep -c -E "recharts|chart\.js|chartjs|victory|nivo|echarts" mp-react/package.json | grep -qx "0" && echo SEM_LIB_NOVA</automated>
  </verify>
  <done>Seção de evolução renderiza 12 colunas com barras proporcionais de receita/lucro + margem % por mês; lucro negativo em vermelho; package.json sem lib de gráfico; empty state presente</done>
</task>

<task type="auto">
  <name>Task 2: Verificação final da fase — suite + build + REQUIREMENTS</name>
  <files>mp-react/src/components/admin/caixa/AbaCaixa.jsx, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (linhas CAIXA-01/CAIXA-02)</read_first>
  <action>
1. Rodar `cd mp-react && npx vitest run` — suite completa verde (150 testes pré-existentes + os novos de fluxoCaixa; zero falhas). Corrigir qualquer regressão antes de prosseguir.
2. Rodar `cd mp-react && npm run build` — build Vite sem erro (garante imports/JSX válidos do AbaCaixa).
3. Revisão rápida do AbaCaixa.jsx: nenhum `toISOString` fora do que o util encapsula, nenhum `where(` nas leituras, valores sempre via `fmtBRL`.
4. Em `.planning/REQUIREMENTS.md`, marcar `- [x] **CAIXA-01**` e `- [x] **CAIXA-02**` e atualizar as duas linhas da tabela Traceability para `Complete` (padrão das fases anteriores — só após os passos 1-2 passarem).
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run && npm run build</automated>
  </verify>
  <done>Suite completa verde; build sem erro; CAIXA-01 e CAIXA-02 marcados Complete em REQUIREMENTS.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados agregados → render do gráfico | valores extremos/zerados não podem quebrar o layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Denial of Service (layout quebrado por valor extremo) | escala das barras | mitigate | normalização por maxVal com piso 1; alturas em px clampadas à área de 120px |
| T-10-08 | Tampering (NaN em margem/altura) | render | mitigate | serie vem do util defensivo (T-10-01); `toFixed(0)` só sobre Number garantido pelo util |
| T-10-09 | Spoofing/Elevation | n/a | accept | plano é somente render client-side de dados já autorizados pelas rules existentes |
</threat_model>

<verification>
- `grep -c "serieEvolucao12Meses" mp-react/src/components/admin/caixa/AbaCaixa.jsx` ≥ 1
- `grep -c -E "recharts|chart\.js|victory|nivo|echarts" mp-react/package.json` retorna 0
- `cd mp-react && npx vitest run` e `npm run build` — ambos sem erro
- REQUIREMENTS.md com CAIXA-01/CAIXA-02 Complete
</verification>

<success_criteria>
- CAIXA-02 observável: admin vê a evolução dos últimos 12 meses (receita, lucro líquido, margem %) na aba 💵 Caixa
- Zero dependência nova; barras 100% CSS; margem legível por mês; meses sem dados não quebram o gráfico
- Fase 10 completa: CAIXA-01 + CAIXA-02 entregues e suite verde
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/10-fluxo-de-caixa-e-evolucao/10-03-SUMMARY.md`
</output>

---
phase: 10-fluxo-de-caixa-e-evolucao
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - mp-react/src/components/admin/caixa/AbaCaixa.jsx
  - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx
autonomous: true
requirements: [CAIXA-01]

must_haves:
  truths:
    - "Admin vê a aba interna '💵 Caixa' no Financeiro da Empresa, ao lado das abas existentes"
    - "A aba mostra Entradas, Saídas e Saldo do mês selecionado (mesRef), saldo em destaque verde/vermelho"
    - "Entradas listam notas pagas (número, seguradora, valor) e serviços particulares; saídas listam despesas pagas e técnicos pagos"
    - "A tela explica em uma frase a diferença DRE × Caixa"
    - "Sem movimentação, a aba mostra empty state explicativo apontando onde marcar notas/despesas como pagas"
  artifacts:
    - path: "mp-react/src/components/admin/caixa/AbaCaixa.jsx"
      provides: "Aba Caixa: carga das 6 coleções + resumo do mês + detalhamento"
      min_lines: 120
      contains: "fluxoCaixaDoMes"
    - path: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      provides: "Registro da aba 'caixa' e render do AbaCaixa"
      contains: "id: 'caixa'"
  key_links:
    - from: "mp-react/src/components/admin/caixa/AbaCaixa.jsx"
      to: "mp-react/src/utils/fluxoCaixa.js"
      via: "import fluxoCaixaDoMes"
      pattern: "from '\\.\\./\\.\\./\\.\\./utils/fluxoCaixa"
    - from: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      to: "mp-react/src/components/admin/caixa/AbaCaixa.jsx"
      via: "import + render condicional abaFin === 'caixa'"
      pattern: "abaFin === 'caixa'"
    - from: "mp-react/src/components/admin/caixa/AbaCaixa.jsx"
      to: "empresas/{empresaId}/notasFiscais (e demais coleções)"
      via: "getDocs(collection(...)) — leitura completa, sem where de mês"
      pattern: "getDocs\\(collection\\(db"
---

<objective>
Criar a aba interna "💵 Caixa" no FinanceiroEmpresaTab (CAIXA-01): novo componente `AbaCaixa.jsx` que carrega as coleções financeiras (leitura completa, decisão LOCKED), calcula o fluxo do mês com `fluxoCaixaDoMes` (plano 10-01) e mostra "Entradas R$ X · Saídas R$ Y · Sobrou R$ Z no caixa" com detalhamento expansível e empty states explicativos.

Purpose: o admin enxerga o dinheiro que DE FATO entrou e saiu no mês — visão de caixa, complementar ao DRE.
Output: `AbaCaixa.jsx` novo + FinanceiroEmpresaTab com a aba registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-fluxo-de-caixa-e-evolucao/10-CONTEXT.md
@mp-react/src/utils/fluxoCaixa.js
@mp-react/src/components/admin/FinanceiroEmpresaTab.jsx
@mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx

<interfaces>
<!-- Contratos do plano 10-01 (já implementado — usar direto, sem exploração): -->
```js
// mp-react/src/utils/fluxoCaixa.js
fluxoCaixaDoMes({ notas, particulares, despesas, fechamentos }, mesRef)
// → { entradas: { notas: [], particulares: [], totalNotas, totalParticulares, total },
//     saidas:   { despesas: [], tecnicos: [], totalDespesas, totalTecnicos, total },
//     saldo }
dataEntradaNota(nota) // 'YYYY-MM-DD' usada como data de entrada (pago_em com fallback legado)
```

<!-- Padrão de componente de aba (de AbaFechamentoTecnicos.jsx — mesma profundidade de pasta): -->
```jsx
import { db, collection, getDocs } from '../../../firebase.js'
import { useAdminContext } from '../../../contexts/AdminContext.jsx'
import { fmtBRL } from '../../../utils/formatters.js'
export default function AbaCaixa({ mesRef }) {
  const { empresaId, showToast } = useAdminContext()
  ...
}
```

<!-- Integração no FinanceiroEmpresaTab (padrão existente, linhas 509-585): -->
```jsx
// array de abas: adicionar { id: 'caixa', label: '💵 Caixa' } após 'tecnicos'
// render: {!carregando && abaFin === 'caixa' && <AbaCaixa mesRef={mesRef} />}
// import no topo: import AbaCaixa from './caixa/AbaCaixa.jsx'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AbaCaixa.jsx — carga de dados + resumo Entradas/Saídas/Saldo</name>
  <files>mp-react/src/components/admin/caixa/AbaCaixa.jsx</files>
  <read_first>mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx (padrão de aba com carga própria), mp-react/src/utils/fluxoCaixa.js (contrato de fluxoCaixaDoMes)</read_first>
  <action>
Criar `mp-react/src/components/admin/caixa/AbaCaixa.jsx` (pasta nova `caixa/`, espelhando `faturamento/` e `fechamento/`). Componente `AbaCaixa({ mesRef })`:

1. **Carga (uma vez por empresaId, NUNCA por mesRef):** `useEffect` com `Promise.all` de 6 `getDocs(collection(db, ...))` SEM `where` de mês — decisão LOCKED ("UMA leitura completa de cada coleção; NÃO fazer 12×4 queries"):
   - `empresas/${empresaId}/notasFiscais`
   - `empresas/${empresaId}/fechamentosTecnicos`
   - `empresas/${empresaId}/despesasMensais`
   - `empresas/${empresaId}/deducoesMensais`
   - `empresas/${empresaId}/resultadoFinanceiroMensal`
   - `empresas/${empresaId}/servicosParticulares`
   Mapear `d => ({ id: d.id, ...d.data() })`, guardar em states separados (`notas`, `fechamentos`, `despesas`, `deducoes`, `resultadoFinanceiro`, `particulares`) + `carregando`. Erro → `showToast('Erro ao carregar caixa: ' + e.message, 'error')`. Todas as 6 coleções já têm rules deployadas — NÃO tocar em firestore.rules (LOCKED: sem coleção nova, sem rules novas).

2. **Cálculo:** `const caixa = fluxoCaixaDoMes({ notas, particulares, despesas, fechamentos }, mesRef)` — recalcula a cada navegação de mês sem refetch (dados já em memória). Guardar deducoes/resultadoFinanceiro em state desde já (serão usados pelo gráfico no plano 10-03).

3. **Resumo (linguagem direta, usuário não técnico):** três cards em linha (flex, inline styles como as demais abas): "💰 Entradas" (verde, `fmtBRL(caixa.entradas.total)`), "💸 Saídas" (vermelho, `fmtBRL(caixa.saidas.total)`), "🏦 Sobrou no caixa" com `fmtBRL(caixa.saldo)` em destaque — cor `var(--ok, green)`/verde se saldo ≥ 0, `var(--danger)` se negativo.

4. **Explicação didática (LOCKED):** caixinha de texto curta no topo (mesmo estilo do aviso ℹ️ da AbaImpostosFinanceiro): "ℹ️ O DRE responde 'o mês deu lucro?'. O Caixa responde 'quanto dinheiro entrou e saiu de verdade?' — aqui só contam notas, despesas e técnicos já PAGOS."

5. **Empty state (aprendizado LOCKED — explicativo):** se `caixa.entradas.total === 0 && caixa.saidas.total === 0`, mostrar `div.empty-state` com: "Nenhuma movimentação de caixa em {mês}. Entradas aparecem quando você marca notas como pagas na aba 📄 Faturamento; saídas quando paga despesas (💸 Despesas) e técnicos (👷 Técnicos)."
  </action>
  <verify>
    <automated>grep -c "getDocs(collection(db" mp-react/src/components/admin/caixa/AbaCaixa.jsx && grep -c "fluxoCaixaDoMes" mp-react/src/components/admin/caixa/AbaCaixa.jsx && grep -c "where(" mp-react/src/components/admin/caixa/AbaCaixa.jsx | grep -qx "0" && echo OK</automated>
  </verify>
  <done>AbaCaixa.jsx existe com 6 getDocs sem where; resumo Entradas/Saídas/Saldo renderizado a partir de fluxoCaixaDoMes; empty state e explicação DRE×Caixa presentes</done>
</task>

<task type="auto">
  <name>Task 2: Registrar aba 'caixa' no FinanceiroEmpresaTab + detalhamento expansível</name>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx, mp-react/src/components/admin/caixa/AbaCaixa.jsx</files>
  <read_first>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx linhas 505-590 (array de abas e renders condicionais)</read_first>
  <action>
**FinanceiroEmpresaTab.jsx (3 edits mínimos — NÃO reestruturar o arquivo):**
1. Import no topo: `import AbaCaixa from './caixa/AbaCaixa.jsx'` (junto de AbaFaturamento/AbaFechamentoTecnicos)
2. No array de abas internas, adicionar `{ id: 'caixa', label: '💵 Caixa' }` após `tecnicos`
3. Render condicional após o de tecnicos: `{!carregando && abaFin === 'caixa' && <AbaCaixa mesRef={mesRef} />}`

**AbaCaixa.jsx — detalhamento (discretion recomendada no CONTEXT, implementar):** abaixo dos cards, duas seções `<details>` nativas (expansíveis, sem lib):
- "Ver entradas ({n})": notas pagas do mês — `nota.numero` · `nota.seguradora` · pago em `dataEntradaNota(nota)` · `fmtBRL(nota.total)`; depois particulares — `p.descricao` · `fmtBRL(p.valor_recebido)`. Subtotais `totalNotas`/`totalParticulares`.
- "Ver saídas ({n})": despesas pagas — `d.descricao` · pago em `d.data_pagamento` · `fmtBRL(d.valor)`; fechamentos — `f.tecnico` · ref. `f.mes_referencia` · pago em `f.pago_em` · `fmtBRL(f.total)`. Subtotais.
Datas exibidas em DD/MM/AAAA (helper local simples de split/join, sem `new Date`). Cada seção com seu próprio empty state curto quando a lista está vazia.

Ao final: `cd mp-react && npx vitest run` — a suite completa (150 testes + fluxoCaixa) precisa continuar verde, e `npx vite build` (ou `npm run build`) precisa compilar sem erro de import.
  </action>
  <verify>
    <automated>grep -c "abaFin === 'caixa'" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx && grep -c "id: 'caixa'" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx && cd mp-react && npx vitest run</automated>
  </verify>
  <done>Aba '💵 Caixa' aparece e renderiza AbaCaixa com mesRef; detalhamento expansível de entradas e saídas funcional; suite completa verde; build sem erro</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | leituras de 6 coleções por empresa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure (cross-tenant) | getDocs AbaCaixa | mitigate | todos os paths são `empresas/${empresaId}/...` com rules existentes já deployadas; nenhuma coleção nova, nenhuma rule alterada |
| T-10-05 | Denial of Service (custo de leitura) | 6 leituras completas | accept | coleções pequenas (decisão LOCKED); carga 1× por empresaId, navegação de mês não refaz queries |
| T-10-06 | Tampering (doc malformado quebra render) | render das listas | mitigate | agregação delega a fluxoCaixaDoMes (já defensivo, T-10-01); render usa `\|\| ''`/fmtBRL |
</threat_model>

<verification>
- `grep -c "AbaCaixa" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` ≥ 2 (import + render)
- `grep -c "where(" mp-react/src/components/admin/caixa/AbaCaixa.jsx` retorna 0 (leituras completas, LOCKED)
- `cd mp-react && npx vitest run` — zero falhas
- Nenhuma mudança em firestore.rules (git diff limpo nesse arquivo)
</verification>

<success_criteria>
- CAIXA-01 observável: admin abre 💵 Caixa, vê entradas reais (notas pagas + particulares), saídas reais (despesas pagas + técnicos pagos) e saldo do mês em destaque
- Navegar de mês (◄ ►) atualiza o resumo sem novas queries
- Empty states explicativos e frase didática DRE × Caixa presentes
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/10-fluxo-de-caixa-e-evolucao/10-02-SUMMARY.md`
</output>

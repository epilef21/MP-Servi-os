---
phase: 02-camada-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/utils/formatters.js
  - mp-react/src/pages/AdminPage.jsx
  - mp-react/src/__tests__/formatters.test.js
autonomous: true
requirements:
  - UTIL-01
  - UTIL-02
  - UTIL-03
  - UTIL-04

must_haves:
  truths:
    - "fmtDate, fmtBRL, getLucro, maskPhone e maskCNPJ estao em src/utils/formatters.js com named exports"
    - "AdminPage.jsx importa os 5 utilitarios de '../utils/formatters' — as definicoes locais foram removidas"
    - "npm test passa verde com todos os casos de formatters.test.js"
    - "getLucro retorna null (nao 0) quando todos os campos sao falsy"
    - "fmtDate usa new Date(year, month, day) nos mocks de Timestamp — sem off-by-one de timezone"
  artifacts:
    - path: "mp-react/src/utils/formatters.js"
      provides: "5 funcoes utilitarias puras com named exports"
      exports: ["fmtDate", "fmtBRL", "getLucro", "maskPhone", "maskCNPJ"]
    - path: "mp-react/src/__tests__/formatters.test.js"
      provides: "Suite de testes cobrindo UTIL-01, UTIL-02, UTIL-03, UTIL-04"
      contains: "describe('fmtDate'"
    - path: "mp-react/src/pages/AdminPage.jsx"
      provides: "AdminPage atualizado sem definicoes locais das 5 funcoes"
      contains: "from '../utils/formatters'"
  key_links:
    - from: "mp-react/src/__tests__/formatters.test.js"
      to: "mp-react/src/utils/formatters.js"
      via: "import { fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ }"
      pattern: "from '../utils/formatters'"
    - from: "mp-react/src/pages/AdminPage.jsx"
      to: "mp-react/src/utils/formatters.js"
      via: "import { fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ }"
      pattern: "from '../utils/formatters'"
---

<objective>
Extrair as 5 funcoes utilitarias puras de AdminPage.jsx para src/utils/formatters.js, atualizar AdminPage.jsx para importa-las do novo modulo, e escrever formatters.test.js com cobertura completa de todos os comportamentos observaveis.

Purpose: As funcoes fmtDate, fmtBRL, getLucro, maskPhone e maskCNPJ estao definidas como funcoes locais nao-exportadas em AdminPage.jsx (linhas 46-97). Sem extrair, e impossivel importa-las em um arquivo de teste sem renderizar o componente inteiro (3264 linhas com dezenas de hooks e imports Firebase). A extracao e o prerequisito para UTIL-01..04.

Output: mp-react/src/utils/formatters.js (novo), mp-react/src/__tests__/formatters.test.js (novo), AdminPage.jsx (linhas 45-97 substituidas por import)
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-camada-base/02-RESEARCH.md

<interfaces>
<!-- Implementacoes exatas extraidas de AdminPage.jsx linhas 46-97 (Read confirmado) -->
<!-- COPIAR EXATAMENTE — nao reescrever a logica -->

```js
// fmtDate: null/undefined -> '—', string DD/MM/YYYY -> passthrough,
//          string YYYY-MM-DD -> DD/MM/YYYY, Timestamp (.toDate()) -> DD/MM/YYYY
function fmtDate(d) {
  if (!d) return '—'
  if (typeof d === 'string') {
    if (/^\d{2}\/\d{2}\/\d{4}$/.test(d)) return d
    if (d.includes('-')) {
      const [y, m, day] = d.split('-')
      return `${day}/${m}/${y}`
    }
  }
  if (d?.toDate) {
    const dt = d.toDate()
    return `${String(dt.getDate()).padStart(2,'0')}/${String(dt.getMonth()+1).padStart(2,'0')}/${dt.getFullYear()}`
  }
  return '—'
}

// fmtBRL: parseFloat -> toFixed(2) -> replace('.', ',')
// NaN -> '—'
function fmtBRL(v) {
  const n = parseFloat(v)
  if (isNaN(n)) return '—'
  return `R$ ${n.toFixed(2).replace('.', ',')}`
}

// getLucro: (mo_seguradora - valor_prestador) + valor_deslocamento
// Retorna null quando todos os tres campos sao 0 ou falsy
function getLucro(r) {
  const mos = parseFloat(r.mo_seguradora)      || 0
  const vpt = parseFloat(r.valor_prestador)    || 0
  const vds = parseFloat(r.valor_deslocamento) || 0
  return (mos || vpt || vds) ? (mos - vpt) + vds : null
}

// maskPhone: max 11 digitos
// 10 digitos -> (XX) XXXX-XXXX
// 11 digitos -> (XX) XXXXX-XXXX
function maskPhone(v) {
  const d = v.replace(/\D/g, '').slice(0, 11)
  if (d.length <= 10) return d.replace(/(\d{2})(\d{4})(\d{0,4})/, '($1) $2-$3').replace(/-$/, '')
  return d.replace(/(\d{2})(\d{5})(\d{0,4})/, '($1) $2-$3').replace(/-$/, '')
}

// maskCNPJ: XX.XXX.XXX/XXXX-XX
function maskCNPJ(v) {
  const d = v.replace(/\D/g, '').slice(0, 14)
  return d
    .replace(/(\d{2})(\d)/, '$1.$2')
    .replace(/(\d{3})(\d)/, '$1.$2')
    .replace(/(\d{3})(\d)/, '$1/$2')
    .replace(/(\d{4})(\d)/, '$1-$2')
}
```

<!-- AdminPage.jsx linha 1-43 (verificado) — imports existentes que devem ser preservados -->
<!-- A linha de import a ADICIONAR em AdminPage.jsx (apos remover as 5 definicoes locais): -->
```js
import { fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ } from '../utils/formatters.js'
```
<!-- Inserir ANTES da linha "const MESES_ABREV" (~linha 99 apos remover as 5 funcoes) -->
<!-- Nao remover fmtDatetime e fmtSN — essas funcoes NAO vao para formatters.js -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/utils/formatters.js com as 5 funcoes extraidas</name>
  <files>mp-react/src/utils/formatters.js</files>
  <read_first>
    - mp-react/src/pages/AdminPage.jsx linhas 44-98 — copiar as implementacoes exatas das 5 funcoes (fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ). NAO usar a versao da memoria — ler o arquivo.
    - mp-react/src/utils/ — verificar se o diretorio ja existe (pdfGenerator.js, pngGenerator.js etc. devem estar la)
  </read_first>
  <action>
Criar o arquivo mp-react/src/utils/formatters.js com o conteudo abaixo. As implementacoes devem ser copiadas LITERALMENTE de AdminPage.jsx linhas 46-97 — nao reescrever nem simplificar.

O arquivo deve exportar exatamente estas 5 funcoes como named exports. Nao exportar default. Nao exportar fmtDatetime nem fmtSN (essas permanecem em AdminPage.jsx).

Estilo obrigatorio (per CLAUDE.md): 2 espacos de indentacao, aspas simples, sem ponto-e-virgula.

```js
// Utilitarios de formatacao e mascaramento puros — sem dependencias externas.
// Extraidos de AdminPage.jsx para permitir testes isolados.

export function fmtDate(d) {
  if (!d) return '—'
  if (typeof d === 'string') {
    if (/^\d{2}\/\d{2}\/\d{4}$/.test(d)) return d
    if (d.includes('-')) {
      const [y, m, day] = d.split('-')
      return `${day}/${m}/${y}`
    }
  }
  if (d?.toDate) {
    const dt = d.toDate()
    return `${String(dt.getDate()).padStart(2,'0')}/${String(dt.getMonth()+1).padStart(2,'0')}/${dt.getFullYear()}`
  }
  return '—'
}

export function fmtBRL(v) {
  const n = parseFloat(v)
  if (isNaN(n)) return '—'
  return `R$ ${n.toFixed(2).replace('.', ',')}`
}

export function getLucro(r) {
  const mos = parseFloat(r.mo_seguradora)      || 0
  const vpt = parseFloat(r.valor_prestador)    || 0
  const vds = parseFloat(r.valor_deslocamento) || 0
  return (mos || vpt || vds) ? (mos - vpt) + vds : null
}

export function maskPhone(v) {
  const d = v.replace(/\D/g, '').slice(0, 11)
  if (d.length <= 10) return d.replace(/(\d{2})(\d{4})(\d{0,4})/, '($1) $2-$3').replace(/-$/, '')
  return d.replace(/(\d{2})(\d{5})(\d{0,4})/, '($1) $2-$3').replace(/-$/, '')
}

export function maskCNPJ(v) {
  const d = v.replace(/\D/g, '').slice(0, 14)
  return d
    .replace(/(\d{2})(\d)/, '$1.$2')
    .replace(/(\d{3})(\d)/, '$1.$2')
    .replace(/(\d{3})(\d)/, '$1/$2')
    .replace(/(\d{4})(\d)/, '$1-$2')
}
```
  </action>
  <acceptance_criteria>
    - Arquivo mp-react/src/utils/formatters.js existe
    - grep -c "export function fmtDate" mp-react/src/utils/formatters.js retorna 1
    - grep -c "export function fmtBRL" mp-react/src/utils/formatters.js retorna 1
    - grep -c "export function getLucro" mp-react/src/utils/formatters.js retorna 1
    - grep -c "export function maskPhone" mp-react/src/utils/formatters.js retorna 1
    - grep -c "export function maskCNPJ" mp-react/src/utils/formatters.js retorna 1
    - grep -c "export default" mp-react/src/utils/formatters.js retorna 0 (sem default export)
    - grep -c "fmtDatetime" mp-react/src/utils/formatters.js retorna 0 (nao deve estar aqui)
    - grep -c "fmtSN" mp-react/src/utils/formatters.js retorna 0 (nao deve estar aqui)
    - grep -c "import" mp-react/src/utils/formatters.js retorna 0 (sem dependencias externas)
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && node -e "const fs=require('fs'); const f=fs.readFileSync('src/utils/formatters.js','utf8'); ['fmtDate','fmtBRL','getLucro','maskPhone','maskCNPJ'].forEach(n=>{ if(!f.includes('export function '+n)) throw new Error('Missing export: '+n) }); if(f.includes('export default')) throw new Error('No default export allowed'); if(f.includes('fmtDatetime')) throw new Error('fmtDatetime must stay in AdminPage'); if(f.includes('import ')) throw new Error('formatters.js must have zero imports'); console.log('OK')"</automated>
  </verify>
  <done>mp-react/src/utils/formatters.js existe com 5 named exports (fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ), sem imports externos, sem export default, sem fmtDatetime/fmtSN.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar AdminPage.jsx para importar de ../utils/formatters</name>
  <files>mp-react/src/pages/AdminPage.jsx</files>
  <read_first>
    - mp-react/src/pages/AdminPage.jsx linhas 44-100 — localizar exatamente onde as 5 funcoes estao definidas (entre "// Helpers de formatacao" e "const MESES_ABREV") para remover apenas elas
    - mp-react/src/utils/formatters.js — confirmar que ja existe (Task 1 deste plano)
  </read_first>
  <action>
Editar mp-react/src/pages/AdminPage.jsx para:

1. REMOVER as definicoes locais das 5 funcoes: fmtDate (linhas ~46-63), fmtBRL (linhas ~74-78), getLucro (linhas ~79-84), maskPhone (linhas ~85-89), maskCNPJ (linhas ~90-97).

   NAO remover: fmtDatetime (linhas ~64-70) e fmtSN (linhas ~71-73) — essas permanecem em AdminPage.jsx.
   NAO remover: o comentario "// Helpers de formatacao" (pode manter ou remover — a criterio).

2. ADICIONAR o import das 5 funcoes logo apos os imports existentes (linha ~43, apos o import de relatorioMensalPdf.js):

```js
import { fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ } from '../utils/formatters.js'
```

Posicionar este import ANTES de qualquer linha que nao seja import (ex: antes do bloco "const MESES_ABREV").

3. Verificar que as referencias existentes a fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ dentro do corpo do componente continuam funcionando (elas funcionarao porque agora estao importadas ao inves de definidas localmente).

IMPORTANTE: Nao modificar nenhuma outra parte do arquivo. AdminPage.jsx tem 3264 linhas — editar apenas o trecho de linhas 44-100.
  </action>
  <acceptance_criteria>
    - grep -c "from '../utils/formatters'" mp-react/src/pages/AdminPage.jsx retorna 1
    - grep -c "import { fmtDate" mp-react/src/pages/AdminPage.jsx retorna 1
    - grep -c "^function fmtDate" mp-react/src/pages/AdminPage.jsx retorna 0 (definicao local removida)
    - grep -c "^function fmtBRL" mp-react/src/pages/AdminPage.jsx retorna 0 (definicao local removida)
    - grep -c "^function getLucro" mp-react/src/pages/AdminPage.jsx retorna 0 (definicao local removida)
    - grep -c "^function maskPhone" mp-react/src/pages/AdminPage.jsx retorna 0 (definicao local removida)
    - grep -c "^function maskCNPJ" mp-react/src/pages/AdminPage.jsx retorna 0 (definicao local removida)
    - grep -c "^function fmtDatetime" mp-react/src/pages/AdminPage.jsx retorna 1 (permanece)
    - grep -c "^function fmtSN" mp-react/src/pages/AdminPage.jsx retorna 1 (permanece)
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && node -e "const fs=require('fs'); const f=fs.readFileSync('src/pages/AdminPage.jsx','utf8'); if(!f.includes(\"from '../utils/formatters'\")) throw new Error('Missing formatters import'); ['fmtDate','fmtBRL','getLucro','maskPhone','maskCNPJ'].forEach(n=>{ const re=new RegExp('^function '+n,'m'); if(re.test(f)) throw new Error('Local definition of '+n+' still present — must be removed') }); if(!f.includes('function fmtDatetime')) throw new Error('fmtDatetime must remain in AdminPage'); if(!f.includes('function fmtSN')) throw new Error('fmtSN must remain in AdminPage'); console.log('OK')"</automated>
  </verify>
  <done>AdminPage.jsx importa fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ de '../utils/formatters.js'. As 5 definicoes locais foram removidas. fmtDatetime e fmtSN permanecem definidas localmente.</done>
</task>

<task type="auto">
  <name>Task 3: Escrever formatters.test.js com cobertura completa</name>
  <files>mp-react/src/__tests__/formatters.test.js</files>
  <read_first>
    - mp-react/src/utils/formatters.js — ler as implementacoes completas para escrever casos de teste corretos
    - mp-react/src/test-utils/setupTests.js — confirmar que existe (nao precisa importar — e carregado via setupFiles no vitest.config.js)
    - .planning/phases/02-camada-base/02-RESEARCH.md — secoes "Pitfall 5" (fmtDate month trap) e "Pitfall 6" (getLucro null vs 0)
  </read_first>
  <action>
Criar o diretorio mp-react/src/__tests__/ se nao existir.

Criar mp-react/src/__tests__/formatters.test.js com o conteudo abaixo. Estilo obrigatorio (CLAUDE.md): 2 espacos, aspas simples, sem ponto-e-virgula.

CRITICO — Pitfall de timezone (RESEARCH.md Pitfall 5): usar new Date(year, monthIndex, day) para o mock de Timestamp, NUNCA new Date('YYYY-MM-DD'). Exemplo correto: new Date(2024, 0, 15) = 15 de janeiro de 2024.

CRITICO — getLucro null vs 0 (RESEARCH.md Pitfall 6): quando todos os campos sao undefined/null/0, getLucro retorna null (nao 0). O guard e: (mos || vpt || vds) ? ... : null.

```js
import { describe, it, expect } from 'vitest'
import { fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ } from '../utils/formatters'

// UTIL-01: fmtDate
describe('fmtDate', () => {
  it('retorna "—" para null', () => {
    expect(fmtDate(null)).toBe('—')
  })

  it('retorna "—" para undefined', () => {
    expect(fmtDate(undefined)).toBe('—')
  })

  it('retorna string DD/MM/YYYY sem alteracao (passthrough)', () => {
    expect(fmtDate('15/01/2024')).toBe('15/01/2024')
  })

  it('converte string YYYY-MM-DD para DD/MM/YYYY', () => {
    expect(fmtDate('2024-03-07')).toBe('07/03/2024')
  })

  it('formata Firestore Timestamp via toDate() — janeiro (mes 0)', () => {
    // new Date(year, monthIndex, day) — local timezone, sem risco de off-by-one UTC
    const ts = { toDate: () => new Date(2024, 0, 15) }
    expect(fmtDate(ts)).toBe('15/01/2024')
  })

  it('formata Firestore Timestamp via toDate() — dezembro (mes 11)', () => {
    const ts = { toDate: () => new Date(2024, 11, 31) }
    expect(fmtDate(ts)).toBe('31/12/2024')
  })

  it('adiciona zero a esquerda em dia e mes de um digito', () => {
    const ts = { toDate: () => new Date(2024, 2, 5) } // 5 de marco
    expect(fmtDate(ts)).toBe('05/03/2024')
  })

  it('retorna "—" para string sem formato reconhecido', () => {
    expect(fmtDate('nao-e-data')).toBe('—')
  })
})

// UTIL-02: fmtBRL
describe('fmtBRL', () => {
  it('formata numero inteiro positivo', () => {
    expect(fmtBRL(100)).toBe('R$ 100,00')
  })

  it('formata numero decimal com duas casas', () => {
    expect(fmtBRL(99.9)).toBe('R$ 99,90')
  })

  it('formata string numerica', () => {
    expect(fmtBRL('250.50')).toBe('R$ 250,50')
  })

  it('formata zero', () => {
    expect(fmtBRL(0)).toBe('R$ 0,00')
  })

  it('retorna "—" para string nao-numerica', () => {
    expect(fmtBRL('abc')).toBe('—')
  })

  it('retorna "—" para undefined', () => {
    expect(fmtBRL(undefined)).toBe('—')
  })

  it('usa virgula como separador decimal (padrao pt-BR)', () => {
    expect(fmtBRL(1234.56)).toBe('R$ 1234,56')
  })
})

// UTIL-03: getLucro
describe('getLucro', () => {
  it('calcula lucro basico: (mo_seguradora - valor_prestador) + valor_deslocamento', () => {
    expect(getLucro({ mo_seguradora: '300', valor_prestador: '200', valor_deslocamento: '50' })).toBe(150)
  })

  it('retorna null quando todos os campos sao undefined (nao 0)', () => {
    expect(getLucro({ mo_seguradora: undefined, valor_prestador: undefined, valor_deslocamento: undefined })).toBeNull()
  })

  it('retorna null quando todos os campos sao string vazia', () => {
    expect(getLucro({ mo_seguradora: '', valor_prestador: '', valor_deslocamento: '' })).toBeNull()
  })

  it('calcula quando apenas mo_seguradora tem valor', () => {
    expect(getLucro({ mo_seguradora: '500', valor_prestador: '0', valor_deslocamento: '0' })).toBe(500)
  })

  it('calcula quando mo_seguradora e menor que valor_prestador (lucro negativo)', () => {
    expect(getLucro({ mo_seguradora: '100', valor_prestador: '200', valor_deslocamento: '0' })).toBe(-100)
  })

  it('calcula com valor_deslocamento contribuindo positivamente', () => {
    expect(getLucro({ mo_seguradora: '200', valor_prestador: '150', valor_deslocamento: '30' })).toBe(80)
  })
})

// UTIL-04: maskPhone e maskCNPJ
describe('maskPhone', () => {
  it('mascara celular com 11 digitos: (XX) XXXXX-XXXX', () => {
    expect(maskPhone('11987654321')).toBe('(11) 98765-4321')
  })

  it('mascara fixo com 10 digitos: (XX) XXXX-XXXX', () => {
    expect(maskPhone('1134567890')).toBe('(11) 3456-7890')
  })

  it('remove caracteres nao-numericos antes de mascarar', () => {
    expect(maskPhone('(11) 98765-4321')).toBe('(11) 98765-4321')
  })

  it('limita a 11 digitos (ignora excedente)', () => {
    expect(maskPhone('119876543211111')).toBe('(11) 98765-4321')
  })

  it('funciona com entrada parcial de 2 digitos', () => {
    const result = maskPhone('11')
    expect(result).toBe('(11) ')
  })
})

describe('maskCNPJ', () => {
  it('mascara CNPJ completo: XX.XXX.XXX/XXXX-XX', () => {
    expect(maskCNPJ('12345678000195')).toBe('12.345.678/0001-95')
  })

  it('remove caracteres nao-numericos antes de mascarar', () => {
    expect(maskCNPJ('12.345.678/0001-95')).toBe('12.345.678/0001-95')
  })

  it('limita a 14 digitos (ignora excedente)', () => {
    expect(maskCNPJ('123456780001951234')).toBe('12.345.678/0001-95')
  })

  it('mascara entrada parcial', () => {
    const result = maskCNPJ('12345')
    expect(result).toBe('12.345')
  })
})
```
  </action>
  <acceptance_criteria>
    - Arquivo mp-react/src/__tests__/formatters.test.js existe
    - grep -c "describe('fmtDate'" mp-react/src/__tests__/formatters.test.js retorna 1
    - grep -c "describe('fmtBRL'" mp-react/src/__tests__/formatters.test.js retorna 1
    - grep -c "describe('getLucro'" mp-react/src/__tests__/formatters.test.js retorna 1
    - grep -c "describe('maskPhone'" mp-react/src/__tests__/formatters.test.js retorna 1
    - grep -c "describe('maskCNPJ'" mp-react/src/__tests__/formatters.test.js retorna 1
    - grep -c "toBeNull" mp-react/src/__tests__/formatters.test.js retorna >= 2 (getLucro null cases)
    - grep -c "new Date(2024," mp-react/src/__tests__/formatters.test.js retorna >= 2 (local timezone, nao UTC)
    - grep -c "new Date('20" mp-react/src/__tests__/formatters.test.js retorna 0 (proibido — off-by-one UTC)
    - grep -c "from '../utils/formatters'" mp-react/src/__tests__/formatters.test.js retorna 1
    - cd mp-react && npm test -- formatters retorna exit code 0 com todos os testes passando
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npm test -- formatters 2>&1 | tail -30</automated>
  </verify>
  <done>mp-react/src/__tests__/formatters.test.js existe com describes para fmtDate, fmtBRL, getLucro, maskPhone e maskCNPJ. npm test -- formatters passa verde sem nenhum teste falhando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formatters.js -> AdminPage.jsx | Funcoes extraidas devem ter comportamento identico as originais — qualquer divergencia quebra o AdminPage silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/utils/formatters.js | mitigate | Implementacoes copiadas literalmente de AdminPage.jsx — acceptance_criteria verificam que as assinaturas das funcoes sao identicas antes de rodar npm test |
| T-02-02 | Tampering | AdminPage.jsx | mitigate | Aceitar apenas que as 5 definicoes locais foram removidas e o import foi adicionado — nenhuma outra linha deve ser tocada; o acceptance_criteria verifica fmtDatetime e fmtSN ainda presentes |
| T-02-03 | Information Disclosure | formatters.test.js | accept | Arquivo de teste sem credenciais nem dados reais — risco zero de exposicao |
</threat_model>

<verification>
Apos completar as 3 tasks deste plano, dentro de mp-react/:

```bash
# Verificar que formatters.js tem os 5 exports
node -e "import('./src/utils/formatters.js').then(m => { ['fmtDate','fmtBRL','getLucro','maskPhone','maskCNPJ'].forEach(n => { if(typeof m[n] !== 'function') throw new Error('Missing: '+n) }); console.log('exports OK') })"

# Verificar que AdminPage.jsx importa de formatters
grep "from '../utils/formatters'" mp-react/src/pages/AdminPage.jsx

# Rodar apenas os testes de formatters
cd mp-react && npm test -- formatters

# Rodar a suite completa para garantir que AdminPage nao quebrou nada
cd mp-react && npm test
```

Saida esperada do npm test -- formatters:
- Todos os describes passando (fmtDate, fmtBRL, getLucro, maskPhone, maskCNPJ)
- Zero falhas
- Nenhum erro de importacao
</verification>

<success_criteria>
- mp-react/src/utils/formatters.js existe com 5 named exports sem dependencias externas
- mp-react/src/pages/AdminPage.jsx importa as 5 funcoes de '../utils/formatters.js' e nao as define mais localmente
- mp-react/src/__tests__/formatters.test.js cobre todos os comportamentos observaveis de UTIL-01..04
- getLucro retorna null (nao 0) para campos falsy — testado explicitamente
- fmtDate usa new Date(year, month, day) nos mocks de Timestamp — sem UTC off-by-one
- npm test -- formatters passa verde
- npm test (suite completa) continua passando (AdminPage nao quebrou)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/02-camada-base/02-01-SUMMARY.md` usando o template em @C:/Users/felip/.claude/get-shit-done/templates/summary.md
</output>

---
phase: 02-camada-base
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/__tests__/firebase.test.js
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "criarOS e atualizarOS sao testados verificando que addDoc e updateDoc sao chamados com os argumentos corretos"
    - "getOSdaEmpresa e testado para o caminho feliz (2 documentos) e para lista vazia"
    - "getEmpresaBySlug e testado para slug encontrado e slug inexistente (retorna null)"
    - "cadastrarEmpresa e testado verificando que setDoc e chamado exatamente 2 vezes"
    - "contarOSdoMes e testado via getCountFromServer retornando { data: () => ({ count: N }) }"
    - "npm test passa com todos os testes de firebase.test.js verdes, sem chamadas reais ao Firebase"
  artifacts:
    - path: "mp-react/src/__tests__/firebase.test.js"
      provides: "Suite de testes cobrindo DATA-01..06 via mocks SDK-level"
      contains: "vi.mock('firebase/firestore'"
  key_links:
    - from: "mp-react/src/__tests__/firebase.test.js"
      to: "mp-react/src/firebase.js"
      via: "import { criarOS, atualizarOS, getOSdaEmpresa, getEmpresaBySlug, cadastrarEmpresa, contarOSdoMes }"
      pattern: "from '../firebase'"
    - from: "mp-react/src/__tests__/firebase.test.js"
      to: "firebase/firestore SDK"
      via: "vi.mock('firebase/firestore') + import { addDoc, getDocs, ... }"
      pattern: "vi.mock('firebase/firestore'"
---

<objective>
Escrever firebase.test.js com mocks ao nivel do SDK Firebase para testar as 6 funcoes exportadas de firebase.js: criarOS, atualizarOS, getOSdaEmpresa, getEmpresaBySlug, cadastrarEmpresa e contarOSdoMes.

Purpose: As funcoes de acesso ao Firestore em firebase.js devem ser testadas verificando que chamam o SDK com os argumentos corretos. Isso requer mockar firebase/firestore no nivel do SDK — a unica forma de interceptar as chamadas internas de firebase.js sem um emulador real.

Output: mp-react/src/__tests__/firebase.test.js (novo)

NOTA SOBRE DATA-06: O requisito DATA-06 menciona "verificarLimite" mas essa funcao esta em useEmpresa.js (um React hook), nao em firebase.js. A funcao de firebase.js que alimenta o limite e contarOSdoMes(empresaId). Este plano testa contarOSdoMes para satisfazer DATA-06. O teste de verificarLimite e deferido para a Phase 3 (testes de hooks).
</objective>

<execution_context>
@C:/Users/felip/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/felip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-camada-base/02-RESEARCH.md

<interfaces>
<!-- EXCECAO DOCUMENTADA AO CLAUDE.md -->
<!--
  CLAUDE.md diz: "mock Firebase via vi.mock('../firebase') — nunca mockar firebase/firestore diretamente"
  Essa regra se aplica a testes de COMPONENTES/HOOKS para que nao alcancem internals do Firestore.

  Para testar as proprias funcoes de firebase.js (a camada de dados sendo testada),
  e necessario mockar firebase/firestore ao nivel do SDK — e a unica forma de interceptar
  as chamadas internas sem um emulador real.

  EXCECAO: vi.mock('firebase/firestore') e PERMITIDO apenas em firebase.test.js.
  Todos os outros testes (componentes, hooks) continuam usando vi.mock('../firebase').
-->

<!-- Assinaturas verificadas de firebase.js (Read confirmado) -->
```js
// criarOS(empresaId, dados) -> Promise<DocumentReference>
// Chama: addDoc(refChecklist(empresaId), { ...dados, criado_em: serverTimestamp() })
// refChecklist(empresaId) = collection(db, 'empresas', empresaId, 'checklist')

// atualizarOS(empresaId, osId, dados) -> Promise<void>
// Chama: updateDoc(doc(db, 'empresas', empresaId, 'checklist', osId), dados)

// getOSdaEmpresa(empresaId) -> Promise<Array<{id, ...data}>>
// Chama: getDocs(query(refChecklist(empresaId), orderBy('criado_em', 'desc')))
// Mapeia: snap.docs.map(d => ({ id: d.id, ...d.data() }))

// getEmpresaBySlug(slug) -> Promise<{id, ...data} | null>
// Chama: getDocs(query(col, where('slug','==',slug), where('ativo','==',true), limit(1)))
// Retorna null se snap.empty === true

// cadastrarEmpresa(empresaId, dadosEmpresa, dadosConfig) -> Promise<void>
// Chama setDoc DUAS vezes:
//   1. setDoc(refEmpresa(empresaId), { ...dadosEmpresa, ativo: true, criadoEm: serverTimestamp() })
//   2. setDoc(refConfig(empresaId), { nome, telefone, seguradoras, logoUrl, corPrimaria })

// contarOSdoMes(empresaId) -> Promise<number>
// Chama: getCountFromServer(query(refChecklist(empresaId), where('criado_em', '>=', inicio)))
// Retorna: snap.data().count
```

<!-- Shape correta do QuerySnapshot mock -->
```js
// getDocs mock para getOSdaEmpresa (snap.docs.map):
{ docs: [{ id: 'os-1', data: () => ({ campo: 'valor' }) }] }

// getDocs mock para getEmpresaBySlug (snap.empty):
{ empty: false, docs: [{ id: 'emp-1', data: () => ({ nome: 'Test' }) }] }
{ empty: true, docs: [] }

// getCountFromServer mock para contarOSdoMes (snap.data().count):
{ data: () => ({ count: 37 }) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever firebase.test.js com mocks SDK-level e testes das 6 funcoes</name>
  <files>mp-react/src/__tests__/firebase.test.js</files>
  <read_first>
    - mp-react/src/firebase.js — ler as implementacoes completas de criarOS, atualizarOS, getOSdaEmpresa, getEmpresaBySlug, cadastrarEmpresa e contarOSdoMes para entender exatamente quais SDK calls sao feitas e com quais argumentos
    - mp-react/src/__tests__/ — verificar se o diretorio ja existe (o plano 02-01 pode ter criado); se nao, criar
    - .planning/phases/02-camada-base/02-RESEARCH.md — ler secoes "Pattern 2" a "Pattern 6" e todos os "Pitfalls" antes de escrever qualquer mock
  </read_first>
  <action>
Criar mp-react/src/__tests__/firebase.test.js.

O arquivo deve usar vi.mock ao nivel do SDK para mockar firebase/firestore, firebase/auth, firebase/storage e firebase/app — conforme documentado na EXCECAO ao CLAUDE.md no bloco de interfaces acima.

Estilo obrigatorio (CLAUDE.md): 2 espacos, aspas simples, sem ponto-e-virgula.

CRITICO — mock shape de QuerySnapshot:
- getDocs para getOSdaEmpresa: { docs: [{ id, data: () => ({}) }] } — se retornar [] diretamente, snap.docs.map e not a function
- getDocs para getEmpresaBySlug: { empty: false/true, docs: [...] }
- getCountFromServer: { data: () => ({ count: N }) } — se retornar { count: N }, snap.data() e not a function

CRITICO — setDoc chamado 2x em cadastrarEmpresa:
- expect(setDoc).toHaveBeenCalledTimes(2)
- Primeira chamada: tem { ativo: true, criadoEm: ... }
- Segunda chamada: tem { nome, telefone, seguradoras, ... }

CRITICO — clearMocks: true no vitest.config.js zera call counts mas NAO reseta mockResolvedValue.
Sempre configurar return values no beforeEach de cada describe.

```js
import { describe, it, expect, vi, beforeEach } from 'vitest'

// EXCECAO DOCUMENTADA: vi.mock ao nivel do SDK e necessario para testar firebase.js internamente.
// Regra do CLAUDE.md "mock via vi.mock('../firebase')" se aplica a testes de componentes/hooks —
// nao a testes da propria camada de dados. Ver 02-RESEARCH.md secao "Pattern 2".
vi.mock('firebase/firestore', () => ({
  getFirestore:       vi.fn(() => ({})),
  collection:         vi.fn(() => 'mock-collection-ref'),
  addDoc:             vi.fn(),
  updateDoc:          vi.fn(),
  getDocs:            vi.fn(),
  setDoc:             vi.fn(),
  doc:                vi.fn(() => 'mock-doc-ref'),
  query:              vi.fn(() => 'mock-query'),
  where:              vi.fn(() => 'mock-where'),
  orderBy:            vi.fn(() => 'mock-order'),
  limit:              vi.fn(() => 'mock-limit'),
  serverTimestamp:    vi.fn(() => 'SERVERTIMESTAMP'),
  getCountFromServer: vi.fn(),
  Timestamp:          { now: vi.fn() },
}))

vi.mock('firebase/auth', () => ({
  getAuth:                      vi.fn(() => ({})),
  signInWithEmailAndPassword:   vi.fn(),
  createUserWithEmailAndPassword: vi.fn(),
  signOut:                      vi.fn(),
  onAuthStateChanged:           vi.fn(),
}))

vi.mock('firebase/storage', () => ({
  getStorage:       vi.fn(() => ({})),
  ref:              vi.fn(),
  uploadBytes:      vi.fn(),
  getDownloadURL:   vi.fn(),
}))

vi.mock('firebase/app', () => ({
  initializeApp: vi.fn(() => ({})),
}))

import { addDoc, updateDoc, getDocs, setDoc, getCountFromServer } from 'firebase/firestore'
import {
  criarOS,
  atualizarOS,
  getOSdaEmpresa,
  getEmpresaBySlug,
  cadastrarEmpresa,
  contarOSdoMes,
} from '../firebase'

// DATA-01: criarOS
describe('criarOS', () => {
  beforeEach(() => {
    addDoc.mockResolvedValue({ id: 'new-os-id' })
  })

  it('chama addDoc e retorna o DocumentReference', async () => {
    const dados = { seguradora: 'Mapfre', nome_segurado: 'Joao Silva' }
    const result = await criarOS('emp-123', dados)
    expect(addDoc).toHaveBeenCalledOnce()
    expect(result.id).toBe('new-os-id')
  })

  it('inclui os campos do parametro dados no documento', async () => {
    const dados = { seguradora: 'Tempo', nome_segurado: 'Ana Lima' }
    await criarOS('emp-456', dados)
    const [, docArg] = addDoc.mock.calls[0]
    expect(docArg).toMatchObject({ seguradora: 'Tempo', nome_segurado: 'Ana Lima' })
  })

  it('adiciona criado_em via serverTimestamp ao documento', async () => {
    await criarOS('emp-123', { seguradora: 'Allianz' })
    const [, docArg] = addDoc.mock.calls[0]
    expect(docArg.criado_em).toBeDefined()
  })
})

// DATA-02: atualizarOS
describe('atualizarOS', () => {
  beforeEach(() => {
    updateDoc.mockResolvedValue(undefined)
  })

  it('chama updateDoc exatamente uma vez', async () => {
    await atualizarOS('emp-123', 'os-456', { status: 'processado' })
    expect(updateDoc).toHaveBeenCalledOnce()
  })

  it('passa os dados fornecidos para updateDoc', async () => {
    const dados = { status: 'finalizado', observacoes: 'Concluido' }
    await atualizarOS('emp-123', 'os-789', dados)
    const [, updateArg] = updateDoc.mock.calls[0]
    expect(updateArg).toMatchObject(dados)
  })
})

// DATA-03: getOSdaEmpresa
describe('getOSdaEmpresa', () => {
  it('retorna array mapeado de documentos quando ha OS', async () => {
    // Shape critica: { docs: [{ id, data: () => ({}) }] }
    // NAO retornar [] diretamente — snap.docs.map seria "not a function"
    getDocs.mockResolvedValue({
      docs: [
        { id: 'os-1', data: () => ({ seguradora: 'Mapfre', status: 'pendente' }) },
        { id: 'os-2', data: () => ({ seguradora: 'Tempo',  status: 'processado' }) },
      ],
    })
    const result = await getOSdaEmpresa('emp-123')
    expect(result).toHaveLength(2)
    expect(result[0]).toEqual({ id: 'os-1', seguradora: 'Mapfre', status: 'pendente' })
    expect(result[1]).toEqual({ id: 'os-2', seguradora: 'Tempo',  status: 'processado' })
  })

  it('retorna array vazio quando nao ha OS', async () => {
    getDocs.mockResolvedValue({ docs: [] })
    const result = await getOSdaEmpresa('emp-999')
    expect(result).toHaveLength(0)
  })

  it('chama getDocs exatamente uma vez por chamada', async () => {
    getDocs.mockResolvedValue({ docs: [] })
    await getOSdaEmpresa('emp-123')
    expect(getDocs).toHaveBeenCalledOnce()
  })
})

// DATA-04: getEmpresaBySlug
describe('getEmpresaBySlug', () => {
  it('retorna dados da empresa quando slug e encontrado', async () => {
    // Shape critica: { empty: false, docs: [{ id, data: () => ({}) }] }
    getDocs.mockResolvedValue({
      empty: false,
      docs: [{ id: 'emp-123', data: () => ({ nome: 'Empresa Teste', slug: 'teste', ativo: true }) }],
    })
    const result = await getEmpresaBySlug('teste')
    expect(result).toEqual({ id: 'emp-123', nome: 'Empresa Teste', slug: 'teste', ativo: true })
  })

  it('retorna null quando slug nao existe (snap.empty === true)', async () => {
    getDocs.mockResolvedValue({ empty: true, docs: [] })
    const result = await getEmpresaBySlug('slug-inexistente')
    expect(result).toBeNull()
  })
})

// DATA-05: cadastrarEmpresa
describe('cadastrarEmpresa', () => {
  beforeEach(() => {
    setDoc.mockResolvedValue(undefined)
  })

  it('chama setDoc exatamente 2 vezes (documento empresa + documento config)', async () => {
    await cadastrarEmpresa(
      'emp-123',
      { nome: 'Empresa Teste', plano: 'basico' },
      { telefone: '(11) 9999-9999', seguradoras: ['Mapfre'], logoUrl: '', corPrimaria: '#000' }
    )
    expect(setDoc).toHaveBeenCalledTimes(2)
  })

  it('primeira chamada ao setDoc inclui ativo: true', async () => {
    await cadastrarEmpresa(
      'emp-123',
      { nome: 'Empresa Teste', plano: 'basico' },
      { telefone: '(11) 9999-9999', seguradoras: ['Mapfre'], logoUrl: '', corPrimaria: '#000' }
    )
    const [, primeiroDoc] = setDoc.mock.calls[0]
    expect(primeiroDoc).toMatchObject({ nome: 'Empresa Teste', ativo: true })
  })

  it('primeira chamada ao setDoc inclui criadoEm via serverTimestamp', async () => {
    await cadastrarEmpresa(
      'emp-123',
      { nome: 'Empresa Teste', plano: 'basico' },
      { telefone: '(11) 9999-9999', seguradoras: ['Mapfre'], logoUrl: '', corPrimaria: '#000' }
    )
    const [, primeiroDoc] = setDoc.mock.calls[0]
    expect(primeiroDoc.criadoEm).toBeDefined()
  })

  it('segunda chamada ao setDoc inclui dados de config', async () => {
    await cadastrarEmpresa(
      'emp-123',
      { nome: 'Empresa Teste', plano: 'basico' },
      { telefone: '(11) 9999-9999', seguradoras: ['Mapfre'], logoUrl: 'logo.png', corPrimaria: '#fff' }
    )
    const [, segundoDoc] = setDoc.mock.calls[1]
    expect(segundoDoc).toMatchObject({ nome: 'Empresa Teste', telefone: '(11) 9999-9999' })
  })
})

// DATA-06: contarOSdoMes (satisfaz DATA-06 — verificarLimite e deferido para Phase 3)
// NOTA: verificarLimite esta em useEmpresa.js (React hook), nao em firebase.js.
// contarOSdoMes e a funcao de firebase.js que alimenta o limite. Testada aqui.
// verificarLimite sera testada na Phase 3 junto com os demais hooks.
describe('contarOSdoMes', () => {
  it('retorna o count retornado por getCountFromServer', async () => {
    // Shape critica: { data: () => ({ count: N }) }
    // NAO retornar { count: N } diretamente — snap.data() seria "not a function"
    getCountFromServer.mockResolvedValue({ data: () => ({ count: 37 }) })
    const count = await contarOSdoMes('emp-123')
    expect(count).toBe(37)
  })

  it('retorna 0 quando nao ha OS no mes', async () => {
    getCountFromServer.mockResolvedValue({ data: () => ({ count: 0 }) })
    const count = await contarOSdoMes('emp-123')
    expect(count).toBe(0)
  })

  it('chama getCountFromServer exatamente uma vez', async () => {
    getCountFromServer.mockResolvedValue({ data: () => ({ count: 5 }) })
    await contarOSdoMes('emp-123')
    expect(getCountFromServer).toHaveBeenCalledOnce()
  })
})
```
  </action>
  <acceptance_criteria>
    - Arquivo mp-react/src/__tests__/firebase.test.js existe
    - grep -c "vi.mock('firebase/firestore'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "vi.mock('firebase/auth'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "vi.mock('firebase/storage'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "vi.mock('firebase/app'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "describe('criarOS'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "describe('atualizarOS'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "describe('getOSdaEmpresa'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "describe('getEmpresaBySlug'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "describe('cadastrarEmpresa'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "describe('contarOSdoMes'" mp-react/src/__tests__/firebase.test.js retorna 1
    - grep -c "toHaveBeenCalledTimes(2)" mp-react/src/__tests__/firebase.test.js retorna >= 1 (cadastrarEmpresa)
    - grep -c "toBeNull" mp-react/src/__tests__/firebase.test.js retorna >= 1 (getEmpresaBySlug nao encontrado)
    - grep -c "data: () =>" mp-react/src/__tests__/firebase.test.js retorna >= 2 (QuerySnapshot shapes corretas)
    - cd mp-react && npm test -- firebase retorna exit code 0 com todos os testes passando
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npm test -- firebase 2>&1 | tail -30</automated>
  </verify>
  <done>mp-react/src/__tests__/firebase.test.js existe com 6 describes cobrindo DATA-01..06. npm test -- firebase passa verde sem nenhuma chamada real ao Firebase. A excecao ao CLAUDE.md (vi.mock SDK-level) esta documentada no arquivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vi.mock('firebase/firestore') -> firebase.js | Intercepta todas as chamadas SDK; nenhuma requisicao real e feita durante os testes |
| firebase.test.js -> EXCECAO ao CLAUDE.md | SDK-level mock e permitido apenas neste arquivo; todos os outros testes continuam usando vi.mock('../firebase') |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | firebase.test.js mocks | mitigate | Configurar mock return values no beforeEach de cada describe — clearMocks: true zera call counts mas nao mockResolvedValue; tests que nao re-configuram podem receber undefined silenciosamente |
| T-02-05 | Information Disclosure | firebase.test.js | accept | Arquivo de teste usa apenas IDs e dados ficticios (emp-123, os-456, Joao Silva); nenhuma credencial real |
| T-02-06 | Denial of Service | vi.mock hoisting | mitigate | vi.mock() e hoisted pelo Vitest antes dos imports — a ordem correta e: vi.mock() primeiro, depois import das funcoes testadas; qualquer inversao causa "Cannot access before initialization" |
</threat_model>

<verification>
Apos completar a task deste plano, dentro de mp-react/:

```bash
# Rodar apenas os testes de firebase
cd mp-react && npm test -- firebase

# Rodar a suite completa (deve continuar passando — smoke test + formatters + firebase)
cd mp-react && npm test
```

Saida esperada do npm test -- firebase:
- Todos os describes passando: criarOS, atualizarOS, getOSdaEmpresa, getEmpresaBySlug, cadastrarEmpresa, contarOSdoMes
- Zero falhas
- Nenhum erro "Cannot read properties of undefined" (indicaria mock shape incorreta)
- Nenhuma chamada real ao Firebase (todos os SDK calls sao vi.fn())

Saida esperada do npm test (suite completa):
- smoke.test.jsx: 3 passando
- formatters.test.js: todos passando (se 02-01 foi executado antes)
- firebase.test.js: todos passando
- Zero falhas
</verification>

<success_criteria>
- mp-react/src/__tests__/firebase.test.js existe com 6 describes (criarOS, atualizarOS, getOSdaEmpresa, getEmpresaBySlug, cadastrarEmpresa, contarOSdoMes)
- vi.mock ao nivel do SDK esta documentado como excecao ao CLAUDE.md no arquivo de teste
- cadastrarEmpresa e testado com expect(setDoc).toHaveBeenCalledTimes(2)
- getEmpresaBySlug retorna null quando snap.empty === true — testado explicitamente
- contarOSdoMes usa mock { data: () => ({ count: N }) } — shape correta do AggregateQuerySnapshot
- npm test -- firebase passa verde sem nenhuma falha
- npm test (suite completa) continua passando
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/02-camada-base/02-02-SUMMARY.md` usando o template em @C:/Users/felip/.claude/get-shit-done/templates/summary.md
</output>

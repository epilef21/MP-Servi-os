---
phase: 11-exportacao-e-fechamento-do-mes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - mp-react/src/utils/fechamentoMes.js
  - mp-react/src/__tests__/fechamentoMes.test.js
autonomous: true
requirements: [EXP-02]

must_haves:
  truths:
    - "A coleção empresas/{empresaId}/fechamentosMes tem regra de segurança deployada (sem ela a aba quebra com 'Missing or insufficient permissions')"
    - "Existe um helper puro que diz se um mês está fechado a partir do doc lido"
    - "Existe um helper puro que formata a data de fechamento (DD/MM/AAAA) para o banner"
  artifacts:
    - path: "firestore.rules"
      provides: "Regra da subcoleção fechamentosMes (read/write para usuário da empresa ou superadmin)"
      contains: "fechamentosMes"
    - path: "mp-react/src/utils/fechamentoMes.js"
      provides: "estaFechado + formatarFechadoEm (puros, testáveis)"
      exports: ["estaFechado", "formatarFechadoEm"]
    - path: "mp-react/src/__tests__/fechamentoMes.test.js"
      provides: "Testes Vitest dos helpers puros"
      contains: "estaFechado"
  key_links:
    - from: "firestore.rules"
      to: "empresas/{empresaId}/fechamentosMes/{mesRef}"
      via: "match block + firebase deploy --only firestore:rules"
      pattern: "match /fechamentosMes"
---

<objective>
Preparar a infraestrutura do EXP-02 (fechar/reabrir mês): a regra de segurança da NOVA coleção `fechamentosMes` (deployada) e os helpers puros de estado "mês fechado" que a UI (Plano 03) vai consumir.

Purpose: Sem a regra deployada, qualquer leitura da coleção derruba a aba em produção (aprendizado LOCKED da Fase 7/8). Os helpers puros isolam a lógica testável do componente.
Output: `firestore.rules` com a regra + deploy; `utils/fechamentoMes.js` com testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-exportacao-e-fechamento-do-mes/11-CONTEXT.md

<read_first>
- firestore.rules — a regra da subcoleção `fechamentosTecnicos` (o exemplo MAIS RECENTE) está por volta das linhas 219-223 dentro de `match /empresas/{empresaId}`:
  ```
  match /fechamentosTecnicos/{id} {
    allow read, write: if isUserOfEmpresa(empresaId) || isSuperAdmin();
  }
  ```
  A nova regra segue EXATAMENTE esse padrão. Já existem os helpers `isUserOfEmpresa` e `isSuperAdmin`.
- mp-react/src/utils/contasPagar.js — padrão de módulo puro (comentário de cabeçalho, sem Firebase/React, formatação de data local sem toISOString).
- mp-react/src/__tests__/contasPagar.test.js — padrão de teste Vitest puro.
- CLAUDE.md — decisões de deploy: `firebase deploy --only firestore:rules`.
</read_first>

<interfaces>
<!-- Doc lido de empresas/{empresaId}/fechamentosMes/{mesRef} (id = 'YYYY-MM'):
     { fechado: true, fechado_em: <Firestore Timestamp>, fechado_por?: <string> }
     Se o mês nunca foi fechado, o getDoc não existe → o consumidor passa null/undefined. -->

// estaFechado(doc): boolean — true SOMENTE se doc existe e doc.fechado === true.
// formatarFechadoEm(fechadoEm): string 'DD/MM/AAAA' aceitando Firestore Timestamp
//   ({ toDate() }), Date, ou string ISO 'YYYY-MM-DD'; retorna '' para valor ausente/ inválido.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regra fechamentosMes no firestore.rules + deploy</name>
  <files>firestore.rules</files>
  <action>
    Dentro do bloco `match /empresas/{empresaId} { ... }`, logo APÓS o bloco
    `match /fechamentosTecnicos/{id}`, adicione:
    ```
    // ── /empresas/{empresaId}/fechamentosMes/{mesRef} ────
    // Trava de "mês fechado" (id = YYYY-MM). Enforcement de edição é client-side
    // (risco aceito, single-admin — ver threat model T-11-04); a regra só garante
    // isolamento multi-tenant da coleção.
    match /fechamentosMes/{mesRef} {
      allow read, write: if isUserOfEmpresa(empresaId) || isSuperAdmin();
    }
    ```
    Em seguida faça o deploy (OBRIGATÓRIO antes de qualquer getDoc na coleção — aprendizado LOCKED):
    `npx firebase deploy --only firestore:rules`.
    Se o deploy pedir autenticação/projeto e falhar, crie um checkpoint de autenticação e reporte;
    NÃO prossiga como se tivesse deployado.
    OBRIGATÓRIO: cole as últimas linhas da saída do deploy (contendo "Deploy complete!") no SUMMARY.md
    como evidência — sem essa evidência o plano não está completo.
  </action>
  <verify>
    <automated>grep -n "match /fechamentosMes" firestore.rules</automated>
  </verify>
  <done>firestore.rules contém a regra de fechamentosMes e o deploy `firestore:rules` foi executado com sucesso.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: utils/fechamentoMes.js — helpers puros + testes</name>
  <files>mp-react/src/utils/fechamentoMes.js, mp-react/src/__tests__/fechamentoMes.test.js</files>
  <behavior>
    estaFechado(doc):
    - Test: estaFechado({ fechado: true }) === true
    - Test: estaFechado({ fechado: false }) === false
    - Test: estaFechado(null) === false e estaFechado(undefined) === false
    - Test: estaFechado({}) === false
    formatarFechadoEm(v):
    - Test: aceita Date → 'DD/MM/AAAA' com dois dígitos (ex.: new Date(2026,5,3) → '03/06/2026')
    - Test: aceita objeto tipo Timestamp { toDate: () => new Date(2026,5,3) } → '03/06/2026'
    - Test: aceita string ISO '2026-06-03' → '03/06/2026'
    - Test: retorna '' para null/undefined/valor inválido
  </behavior>
  <action>
    Crie `mp-react/src/utils/fechamentoMes.js` — módulo 100% puro (sem Firebase/React), cabeçalho
    de comentário no padrão de `contasPagar.js`. Exporte:
    - `export function estaFechado(doc)` — conforme <behavior>.
    - `export function formatarFechadoEm(v)` — normaliza para um Date (Timestamp via `v.toDate()`,
      Date direto, ou string ISO 'YYYY-MM-DD' via split LOCAL — NUNCA `new Date(string)` para ISO
      curto por causa de fuso, e NUNCA toISOString) e retorna `DD/MM/AAAA` com `padStart(2,'0')`.
      Valor ausente/ inválido → ''.
    Crie `mp-react/src/__tests__/fechamentoMes.test.js` cobrindo os casos de <behavior>
    (`import { estaFechado, formatarFechadoEm } from '../utils/fechamentoMes'`).
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run src/__tests__/fechamentoMes.test.js</automated>
  </verify>
  <done>fechamentoMes.test.js passa; helpers exportados e puros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (browser) → Firestore | Escrita/leitura da coleção fechamentosMes atravessa a fronteira de confiança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Elevation of Privilege | fechamentosMes (cross-tenant) | mitigate | Regra `isUserOfEmpresa(empresaId) || isSuperAdmin()` — isolamento multi-tenant idêntico às demais subcoleções financeiras |
| T-11-04 | Tampering | trava de "mês fechado" | accept | Enforcement de edição é client-side (single-admin), mesmo precedente aceito das Fases 7-8 (T-08-12). A regra não impede o admin de reescrever o doc — o objetivo é evitar alteração ACIDENTAL, não maliciosa |
</threat_model>

<verification>
- `grep -n "match /fechamentosMes" firestore.rules` retorna a regra.
- Deploy `firestore:rules` concluído (saída do firebase confirmando release).
- `npx vitest run src/__tests__/fechamentoMes.test.js` verde.
</verification>

<success_criteria>
Coleção fechamentosMes com regra deployada (sem quebrar a aba) e helpers puros `estaFechado`/`formatarFechadoEm` testados, prontos para o Plano 03 consumir.
</success_criteria>

<output>
After completion, create `.planning/phases/11-exportacao-e-fechamento-do-mes/11-02-SUMMARY.md`
</output>

---
phase: 11-exportacao-e-fechamento-do-mes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/utils/drePdf.js
  - mp-react/src/__tests__/drePdf.test.js
  - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx
autonomous: true
requirements: [EXP-01]

must_haves:
  truths:
    - "Admin clica em '⬇ Baixar DRE em PDF' na aba DRE e o navegador baixa um PDF do mês selecionado"
    - "O PDF contém as mesmas seções da tela DRE: receita bruta, deduções, receita líquida, custos variáveis, lucro bruto, resultado financeiro, despesas fixas por grupo, lucro líquido + margem"
    - "O cabeçalho do PDF traz o nome da empresa e o mês/ano"
    - "jsPDF é carregado sob demanda (import dinâmico) — não entra no bundle inicial"
  artifacts:
    - path: "mp-react/src/utils/drePdf.js"
      provides: "montarLinhasDre (puro, testável) + gerarDrePdf (lazy jsPDF)"
      exports: ["montarLinhasDre", "gerarDrePdf"]
      contains: "await import('jspdf')"
    - path: "mp-react/src/__tests__/drePdf.test.js"
      provides: "Testes Vitest da montagem pura das linhas do DRE"
      contains: "montarLinhasDre"
  key_links:
    - from: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      to: "mp-react/src/utils/drePdf.js"
      via: "import gerarDrePdf + onClick no botão da AbaDRE"
      pattern: "gerarDrePdf\\("
---

<objective>
Entregar o EXP-01: botão "⬇ Baixar DRE em PDF" na aba DRE do FinanceiroEmpresaTab que gera, no navegador (jsPDF), um PDF do DRE do mês selecionado — reaproveitando o padrão de `utils/relatorioMensalPdf.js`.

Purpose: O admin fecha o mês com o PDF do DRE em mãos para enviar ao contador.
Output: `utils/drePdf.js` (montagem pura + gerador lazy) com testes, e o botão fiado na `AbaDRE`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-exportacao-e-fechamento-do-mes/11-CONTEXT.md

<read_first>
Releia estes arquivos ANTES de codar (a Fase 10 pode ter alterado o FinanceiroEmpresaTab entre o planejamento e a execução):
- mp-react/src/utils/relatorioMensalPdf.js — PADRÃO A ESPELHAR: `const { default: jsPDF } = await import('jspdf')`, helpers `checkPage`, `sectionHeader`/`tableRow`, cabeçalho azul, função `s()` para remover acentos, `fmtBRL` local, `doc.save(...)`.
- mp-react/src/components/admin/RelatorioTab.jsx — como um botão chama o gerador: `onClick={() => gerarRelatorioMensalPdf(relDados, relMes, relAno, empresa?.nome)}`.
- mp-react/src/components/admin/FinanceiroEmpresaTab.jsx — a função `AbaDRE({ dre, onIrImpostos })` (renderiza o DRE); o componente principal já expõe `nomeEmpresa` via `useAdminContext()` e `mesRef` (string 'YYYY-MM').
- mp-react/src/__tests__/contasPagar.test.js — padrão de teste Vitest puro (`import { describe, it, expect } from 'vitest'`, import de `../utils/...`).
</read_first>

<interfaces>
<!-- Objeto `dre` produzido por calcularDRE() em FinanceiroEmpresaTab.jsx. É a ENTRADA de montarLinhasDre/gerarDrePdf. NÃO reimplementar o cálculo — apenas consumir estes campos. -->
dre = {
  qtdOS: number,
  receitaOS: number,
  receitaParticulares: number,
  margemMaterial: number,           // pode ser 0 → seção some na tela
  receitaTotal: number,
  deducoes: [{ descricao, valor }], // array (impostos lançados)
  totalDeducoes: number,
  receitaLiquida: number,
  custoTecnicos: number,
  custoMaterial: number,
  custoParticulares: number,        // 0 → linha some
  custoVariavelTotal: number,
  lucroBruto: number,
  despesasFin: number,              // taxas/tarifas
  receitasFin: number,              // juros/rendimentos
  saldoFinanceiro: number,
  resultFinanceiro: [{...}],        // array (só para saber se vazio)
  despesasPorGrupo: {               // agrupado por '👤 Pessoal' etc.
    [grupo]: { label, itens: [{ descricao, valor }], subtotal }
  },
  totalDespesas: number,
  lucroLiquido: number,
  margemLiquida: number,            // percentual (ex.: 23.4)
}

// fmtMes já existe no FinanceiroEmpresaTab, mas para o PDF gere o nome do mês a
// partir de mesRef ('YYYY-MM') internamente em drePdf.js (não importar do componente).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: utils/drePdf.js — montagem pura + gerador jsPDF lazy + testes</name>
  <files>mp-react/src/utils/drePdf.js, mp-react/src/__tests__/drePdf.test.js</files>
  <behavior>
    montarLinhasDre(dre) — função PURA (sem jsPDF, sem React), retorna um array ordenado
    de linhas descrevendo o DRE, na MESMA ordem/seções da tela AbaDRE. Cada item:
    `{ tipo: 'secao' | 'linha' | 'subtotal' | 'resultado', label, valor }` (valor number ou null p/ 'secao').
    - Test: seção "(+) Receita Bruta" aparece antes de "(=) LUCRO LÍQUIDO".
    - Test: com dre.deducoes = [] há uma linha explicativa (label contém "Nenhum imposto") em vez de linhas de imposto.
    - Test: com margemMaterial === 0 a linha "Margem em Material" NÃO aparece; com margemMaterial !== 0 aparece.
    - Test: a última linha 'resultado' é o LUCRO LÍQUIDO com valor === dre.lucroLiquido.
    - Test: despesasPorGrupo com 1 grupo gera cabeçalho do grupo + item + subtotal.
    - Test: margemLiquida entra no label ou num campo do lucro líquido (ex.: label inclui "Margem").
  </behavior>
  <action>
    Crie `mp-react/src/utils/drePdf.js` — módulo com DUAS partes, espelhando `relatorioMensalPdf.js`:

    1) `export function montarLinhasDre(dre)` — PURA. Percorre o objeto `dre` (ver <interfaces>) e
       devolve o array de linhas descrito em <behavior>, respeitando as MESMAS condições de
       ocultação da tela (margemMaterial===0 some; custoParticulares>0 aparece; deducoes vazio →
       linha explicativa; resultFinanceiro vazio → "Nenhuma taxa ou juro lançado"; despesasPorGrupo
       vazio → "Nenhuma despesa lançada"). Inclua a margem no lucro líquido.
       NÃO recalcule nada — só formate a partir dos campos já prontos.

    2) `export async function gerarDrePdf(dre, mesRef, nomeEmpresa)` — carrega jsPDF sob demanda:
       `const { default: jsPDF } = await import('jspdf')` (NUNCA import estático — não crescer o
       bundle). Monte o PDF reaproveitando o layout de `relatorioMensalPdf.js`: cabeçalho azul
       (26,63,168) com `nomeEmpresa` e subtítulo "Demonstrativo de Resultado (DRE)"; à direita o
       mês por extenso derivado de `mesRef` ('YYYY-MM') + o ano. Itere sobre `montarLinhasDre(dre)`
       renderizando cada `tipo` (secao = faixa/sectionHeader; linha = label à esquerda, valor à
       direita; subtotal/resultado = destaque). Use a função `s()` (remove acentos, copie de
       relatorioMensalPdf) e um `fmtBRL` local. Rodapé com nome da empresa + data de geração +
       paginação. Finalize com `doc.save(\`dre_${mesRef}.pdf\`)`.

    Crie `mp-react/src/__tests__/drePdf.test.js` cobrindo `montarLinhasDre` conforme <behavior>
    (`import { describe, it, expect } from 'vitest'` e `import { montarLinhasDre } from '../utils/drePdf'`).
    NÃO teste `gerarDrePdf` (geração de PDF não é testada, mesmo padrão do relatório mensal).
    Use apenas formatação de data LOCAL (split de 'YYYY-MM'), nunca toISOString.
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run src/__tests__/drePdf.test.js</automated>
  </verify>
  <done>drePdf.test.js passa; drePdf.js exporta montarLinhasDre (pura) e gerarDrePdf (com `await import('jspdf')`).</done>
</task>

<task type="auto">
  <name>Task 2: Botão "⬇ Baixar DRE em PDF" na AbaDRE</name>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx</files>
  <action>
    Releia FinanceiroEmpresaTab.jsx (pode ter mudado na Fase 10). Então:
    1. Adicione o import: `import { gerarDrePdf } from '../../utils/drePdf.js'`.
    2. `AbaDRE` hoje recebe `{ dre, onIrImpostos }`. Passe também os dados necessários para o PDF:
       na chamada `<AbaDRE dre={dre} onIrImpostos={...} />` acrescente `mesRef={mesRef}` e
       `nomeEmpresa={nomeEmpresa}` (ambos já existem no escopo do componente principal — `mesRef`
       é state, `nomeEmpresa` vem de useAdminContext). Atualize a assinatura de `AbaDRE` para
       `function AbaDRE({ dre, onIrImpostos, mesRef, nomeEmpresa })`.
    3. No topo da `AbaDRE` (junto ao título/início do `.dre-container`, conforme CONTEXT: "aba DRE,
       junto ao título/topo"), adicione um botão:
       `<button className="btn-primary" onClick={() => gerarDrePdf(dre, mesRef, nomeEmpresa)}>⬇️ Baixar DRE em PDF</button>`.
       Desabilite-o quando não houver dados (`dre.qtdOS === 0 && dre.receitaParticulares === 0`)
       para não gerar PDF vazio.
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run && npm run build</automated>
  </verify>
  <done>Build passa; AbaDRE renderiza o botão que chama gerarDrePdf(dre, mesRef, nomeEmpresa); suíte de testes segue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (browser) → jsPDF | Geração 100% client-side; nenhum dado sai do dispositivo do admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | gerarDrePdf (dados financeiros no PDF) | accept | PDF gerado localmente pelo próprio admin autenticado; download manual, nada trafega para terceiros |
| T-11-02 | Denial of Service | import('jspdf') bundle | mitigate | jsPDF via import dinâmico (lazy) — não infla o bundle inicial nem trava o load do app |
</threat_model>

<verification>
- `npx vitest run src/__tests__/drePdf.test.js` verde.
- `npm run build` sem erros (import dinâmico do jsPDF preservado).
- Grep: `grep -n "await import('jspdf')" mp-react/src/utils/drePdf.js` retorna 1+ linha.
- Grep: `grep -n "gerarDrePdf(" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna a chamada do botão.
</verification>

<success_criteria>
Admin, na aba DRE, baixa um PDF do mês com todas as seções do DRE, cabeçalho com empresa e mês, sem crescer o bundle inicial. Montagem das linhas coberta por testes Vitest.
</success_criteria>

<output>
After completion, create `.planning/phases/11-exportacao-e-fechamento-do-mes/11-01-SUMMARY.md`
</output>

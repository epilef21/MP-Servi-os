---
phase: 11-exportacao-e-fechamento-do-mes
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx
  - mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx
autonomous: true
requirements: [EXP-02]

must_haves:
  truths:
    - "No cabeçalho do Financeiro, perto do seletor de mês, há um botão com cadeado que fecha/reabre o mês selecionado"
    - "Com o mês fechado, aparece um banner explicativo '🔒 Mês fechado em DD/MM — reabra para alterar'"
    - "Com o mês fechado, os botões de criar/editar/excluir/pagar de despesas, particulares, impostos e fechamento de técnicos daquele mês ficam ocultos ou desabilitados"
    - "Reabrir o mês (com confirmação) volta a liberar as edições"
    - "A aba Faturamento NÃO é bloqueada pelo fechamento (fatura por seguradora, não por mês) — documentado"
  artifacts:
    - path: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      provides: "Estado mesFechado (getDoc por mês), botão fechar/reabrir, banner, guardas de escrita, props para sub-abas"
      contains: "fechamentosMes"
    - path: "mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx"
      provides: "Prop mesFechado que oculta/desabilita 'Marcar como pago'"
      contains: "mesFechado"
  key_links:
    - from: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      to: "empresas/{empresaId}/fechamentosMes/{mesRef}"
      via: "getDoc ao trocar de mês + setDoc ao fechar/reabrir"
      pattern: "fechamentosMes"
    - from: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      to: "mp-react/src/utils/fechamentoMes.js"
      via: "import estaFechado + formatarFechadoEm"
      pattern: "estaFechado|formatarFechadoEm"
---

<objective>
Entregar o EXP-02: "🔒 Fechar o mês" — botão de cadeado no cabeçalho do Financeiro que trava os lançamentos mensais contra alteração acidental, com banner explicativo e reabertura possível.

Purpose: O admin encerra o mês e evita mexer sem querer nos lançamentos já consolidados; se precisar corrigir, reabre.
Output: estado `mesFechado` + botão fechar/reabrir + banner + guardas de escrita no FinanceiroEmpresaTab; prop `mesFechado` na aba Técnicos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-exportacao-e-fechamento-do-mes/11-CONTEXT.md
@.planning/phases/11-exportacao-e-fechamento-do-mes/11-02-SUMMARY.md

<read_first>
RELEIA o arquivo inteiro antes de editar — a Fase 10 provavelmente adicionou uma aba "💵 Caixa"
ao array de abas e um novo componente; use âncoras estáveis a essa mudança.
- mp-react/src/components/admin/FinanceiroEmpresaTab.jsx:
  - Imports do firebase.js já incluem `doc`; ACRESCENTE `getDoc, setDoc` (ambos são reexportados por firebase.js).
  - Bloco do cabeçalho "fin-periodo" (o `<div className="fin-periodo">` com os botões ◄ ►
    e `<span className="fin-periodo-label">{fmtMes(mesRef)}</span>`) — ÂNCORA para o botão de cadeado.
  - Array literal das abas (objetos `{ id, label }` dentro de `<div className="fin-tabs">`) — pode ter
    ganhado a aba 'caixa' na Fase 10; NÃO dependa da contagem de abas.
  - Handlers de ESCRITA a proteger (todos no componente principal): salvarRecorrente, excluirRecorrente,
    salvarMensal, excluirMensal, pagarMensal, autoLancarFixas, salvarParticular, excluirParticular,
    salvarDeducao, excluirDeducao, salvarResultFin, excluirResultFin.
  - Sub-abas (definidas NESTE arquivo): AbaDespesas, AbaParticulares, AbaImpostosFinanceiro recebem
    callbacks via props (onNovo*, onEditar*, onExcluir*, onPagar*).
  - `carregarDados` é um useCallback com deps [empresaId, mesRef]; `hojeISO()` já existe no arquivo.
- mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx:
  - Recebe `{ mesRef }`. Handler de escrita: `marcarPago(g)`. Botão "💵 Marcar como pago" por volta
    das linhas 170-181; já tem `disabled={pagando === g.tecnicoNorm || g.tecnicoNorm === ''}`.
- mp-react/src/utils/fechamentoMes.js (criado no Plano 02): `estaFechado(doc)`, `formatarFechadoEm(v)`.
</read_first>

<interfaces>
<!-- Doc: empresas/{empresaId}/fechamentosMes/{mesRef} (id = mesRef 'YYYY-MM')
     Fechar:  setDoc(ref, { fechado: true, fechado_em: serverTimestamp(), fechado_por: <email?> })
     Reabrir: setDoc(ref, { fechado: false, reaberto_em: serverTimestamp() }, { merge: true })
              (ou deleteDoc — escolha setDoc com fechado:false para preservar histórico).
     Ler:     getDoc(doc(db, `empresas/${empresaId}/fechamentosMes/${mesRef}`)) → snap.exists() ? snap.data() : null -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estado mesFechado + botão cadeado (fechar/reabrir) + banner</name>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx</files>
  <action>
    1. Imports: adicione `getDoc, setDoc` à lista importada de `../../firebase.js`; adicione
       `import { estaFechado, formatarFechadoEm } from '../../utils/fechamentoMes.js'`.
    2. Estado: `const [fechamentoMes, setFechamentoMes] = useState(null)` (guarda o doc lido ou null).
       Derive `const mesFechado = estaFechado(fechamentoMes)`.
    3. Leitura: dentro de `carregarDados` (uma leitura por mês selecionado, conforme CONTEXT), após
       carregar os demais dados, leia
       `const fmSnap = await getDoc(doc(db, \`empresas/${empresaId}/fechamentosMes/${mesRef}\`))`
       e `setFechamentoMes(fmSnap.exists() ? fmSnap.data() : null)`. Trate erro no mesmo try/catch.
    4. Ações: crie `async function fecharMes()` — confirm ("Fechar {fmtMes(mesRef)}? Os lançamentos
       ficam protegidos contra alteração."), `setDoc(ref, { fechado: true, fechado_em: serverTimestamp() })`,
       recarrega. E `async function reabrirMes()` — confirm ("Reabrir {fmtMes(mesRef)} para editar?"),
       `setDoc(ref, { fechado: false, reaberto_em: serverTimestamp() }, { merge: true })`, recarrega.
    5. Botão de cadeado: dentro do `<div className="fin-periodo">` (ou imediatamente ao lado dele),
       renderize um botão que mostra 🔒/🔓 conforme `mesFechado`, chamando `reabrirMes`/`fecharMes`.
       Ex.: `mesFechado ? <button ... onClick={reabrirMes}>🔓 Reabrir mês</button> : <button ... onClick={fecharMes}>🔒 Fechar mês</button>`.
    6. Banner: quando `mesFechado`, renderize acima das abas um banner explicativo (texto claro para
       usuário não técnico): `🔒 Mês fechado em {formatarFechadoEm(fechamentoMes?.fechado_em)} — reabra para alterar.`
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run && npm run build</automated>
  </verify>
  <done>Build passa; o cabeçalho mostra 🔒/🔓 conforme o mês; fechar grava o doc e o banner aparece; reabrir remove a trava.</done>
</task>

<task type="auto">
  <name>Task 2: Guardas de escrita + desabilitar botões nas sub-abas (inclui Técnicos)</name>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx, mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx</files>
  <action>
    OBJETIVO: com o mês fechado, nenhuma ação que GRAVE um lançamento daquele mês pode ser disparada
    (regra geral do CONTEXT). Ações de leitura/navegação continuam. Faturamento NÃO é bloqueado
    (fatura por seguradora, não por mês) — deixe um comentário explicando isso perto da AbaFaturamento.

    A) Guarda nos handlers (defesa central, robusta): no INÍCIO de cada handler de escrita listado no
       <read_first> (salvar*/excluir*/pagarMensal/autoLancarFixas), adicione um early-return quando
       `mesFechado`:
       `if (mesFechado) { showToast('Mês fechado — reabra para alterar.', 'error'); return }`.
       Isso garante o bloqueio mesmo que algum botão escape da desabilitação visual.

    B) Desabilitar/ocultar botões nas sub-abas do próprio arquivo (AbaDespesas, AbaParticulares,
       AbaImpostosFinanceiro): passe `mesFechado={mesFechado}` como prop a cada uma e, dentro delas,
       oculte OU desabilite os botões de escrita (+ Nova, + Avulso, + Novo Serviço, + Lançar Imposto,
       + Lançar Taxa/Juro, ✏️, 🗑️, ✓ Pagar, banner de auto-lançamento de fixas). Escolha o padrão
       mais claro para usuário não técnico (recomendado: desabilitar com `title="Mês fechado"` para
       as ações inline ✏️/🗑️/Pagar; ocultar os botões de "adicionar"). Quando `mesFechado`, não
       renderize o banner de auto-lançamento nem os alertas de "lançar" que levam a gravar.

    C) Aba Técnicos: passe `mesFechado={mesFechado}` em `<AbaFechamentoTecnicos mesRef={mesRef} ... />`.
       Em AbaFechamentoTecnicos.jsx, receba `mesFechado` nas props, e no botão "💵 Marcar como pago"
       agregue a condição ao `disabled` (`disabled={pagando === g.tecnicoNorm || g.tecnicoNorm === '' || mesFechado}`)
       com `title` explicativo quando fechado; e como defesa, `if (mesFechado) return` no início de
       `marcarPago`. O histórico e a lista continuam visíveis (somente leitura).
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run && npm run build</automated>
  </verify>
  <done>Build + testes verdes; com mês fechado nenhum handler de escrita executa (early-return) e os botões correspondentes ficam ocultos/desabilitados nas abas Despesas, Particulares, Impostos e Técnicos; Faturamento permanece operável.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (browser) → Firestore | Fechar/reabrir grava em fechamentosMes; a trava de edição é avaliada no cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | trava "mês fechado" (client-side) | accept | Enforcement no cliente (single-admin), mesmo precedente aceito das Fases 7-8 (T-08-12). Objetivo é evitar alteração ACIDENTAL, não maliciosa; reabrir é intencional e confirmado |
| T-11-05 | Repudiation | fechar/reabrir sem trilha | mitigate | Grava `fechado_em` (e `reaberto_em` no merge) via serverTimestamp — registro de quando o estado mudou |
</threat_model>

<verification>
- `grep -n "fechamentosMes" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna as chamadas getDoc/setDoc.
- `grep -n "estaFechado\|formatarFechadoEm" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` retorna os usos.
- `grep -c "if (mesFechado)" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx` ≥ 1 (guardas nos handlers).
- `grep -n "mesFechado" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx` retorna a prop + guarda.
- `cd mp-react && npx vitest run && npm run build` verdes.
</verification>

<success_criteria>
O admin fecha o mês pelo cadeado no cabeçalho, vê o banner explicativo, e não consegue mais criar/editar/excluir/pagar lançamentos mensais (despesas, particulares, impostos, fechamento de técnicos) daquele mês; reabrir libera tudo de novo. Faturamento segue operável. Enforcement client-side documentado como risco aceito.
</success_criteria>

<output>
After completion, create `.planning/phases/11-exportacao-e-fechamento-do-mes/11-03-SUMMARY.md`
</output>

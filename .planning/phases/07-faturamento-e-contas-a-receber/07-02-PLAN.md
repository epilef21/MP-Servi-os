---
phase: 07-faturamento-e-contas-a-receber
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/components/admin/DetalheOSModal.jsx
autonomous: true
requirements: [FAT-01]
must_haves:
  truths:
    - "Numa OS Mapfre/Allianz/Mondial o admin registra o código de MÃO DE OBRA (com valor) e, opcionalmente, o código de DESLOCAMENTO (com valor)"
    - "O código de deslocamento é opcional — OS sem deslocamento fica vazia e não vira pendência"
    - "O nº de dígitos do campo respeita a seguradora (Mapfre 8, Allianz/Mondial 2)"
    - "Numa OS Tempo/Maxpar não aparece campo de código — a UI explica que a OS é faturada pela fila pelo nº da assistência"
    - "Dados de código legado (fat_codigo/fat_valor_aprovado) continuam visíveis, tratados como o código de mão de obra"
  artifacts:
    - path: "mp-react/src/components/admin/DetalheOSModal.jsx"
      provides: "Seção de faturamento com DOIS códigos (MO + deslocamento) gravados no doc da OS, migrando o campo único legado"
      contains: "fat_codigo_mo"
  key_links:
    - from: "mp-react/src/components/admin/DetalheOSModal.jsx"
      to: "checklist/{osId} (doc da OS via atualizarOS)"
      via: "atualizarOS(empresaId, selected.id, { fat_codigo_mo, fat_valor_mo, fat_codigo_desloc, fat_valor_desloc })"
      pattern: "fat_codigo_mo|fat_valor_mo|fat_codigo_desloc"
    - from: "mp-react/src/components/admin/DetalheOSModal.jsx"
      to: "mp-react/src/utils/faturamento.js"
      via: "import { digitosCodigo, SEGS_COM_CODIGO }"
      pattern: "digitosCodigo|SEGS_COM_CODIGO"
---

<objective>
Estender a seção "Tarifação" do DetalheOSModal para registrar DOIS códigos de faturamento por OS — MÃO DE OBRA (obrigatório quando há faturamento) e DESLOCAMENTO (opcional) — cada um com seu valor, gravados no doc da OS. Corrige o modelo legado de código único (`fat_codigo`/`fat_valor_aprovado`), migrando-o para o par de campos de mão de obra, e remove a regra errada `seg === 'Tempo' ? 2 : 8` (Tempo não tem código).

Purpose: FAT-01. A Mapfre libera dois "movimentos económicos" por OS (MO + deslocamento). O modelo atual só comporta um. A fila de faturamento (Plano 04) lê exatamente estes campos como itens separados do checklist e compara o valor liberado com o valor da OS (divergência FAT-05).
Output: DetalheOSModal.jsx com seção de dois códigos + migração do legado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md
@.planning/phases/07-faturamento-e-contas-a-receber/07-PATTERNS.md

<interfaces>
<!-- Estado e handlers atuais em DetalheOSModal.jsx (linhas 46-51, 247-294) — o ANALOG a duplicar de 1 para 2 códigos. -->

Estado atual (linhas 49-51):
```javascript
const [codigoForm,    setCodigoForm]    = useState({ codigo: '', valor_aprovado: '' })
const [savingCodigo,  setSavingCodigo]  = useState(false)
const [savingLancado, setSavingLancado] = useState(false)
```

Handler atual saveCodigo (linhas 248-280) grava no doc da OS via atualizarOS e sincroniza reports/selected:
```javascript
await atualizarOS(empresaId, selected.id, payload)
const updated = { ...selected, ...payload }
setReports(p => p.map(r => r.id === selected.id ? updated : r))
setSelected(updated)
```

Regra de dígitos BUGADA a substituir (linha 657): `const codigoDigitos = seg === 'Allianz' || seg === 'Tempo' ? 2 : 8`
→ substituir por `digitosCodigo(seg)` importado de utils/faturamento.js (Mapfre 8, Allianz/Mondial 2).

De mp-react/src/utils/faturamento.js (Plano 01):
```javascript
export const SEGS_COM_CODIGO = ['Mapfre', 'Allianz', 'Mondial']
export function digitosCodigo(seguradora) { return (seguradora==='Allianz'||seguradora==='Mondial') ? 2 : 8 }
```
IMPORTANTE (dependência de wave): este plano roda na MESMA wave do Plano 01. Se `utils/faturamento.js` ainda não existir no momento da execução, defina `digitosCodigo`/`SEGS_COM_CODIGO` inline neste arquivo como fallback local e deixe um comentário `// TODO: migrar para import de utils/faturamento.js`. Se o arquivo já existir, importe dele. Verifique com `ls mp-react/src/utils/faturamento.js` antes de decidir.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Duplicar estado e handlers de código para MO + Deslocamento</name>
  <files>mp-react/src/components/admin/DetalheOSModal.jsx</files>
  <read_first>
    - mp-react/src/components/admin/DetalheOSModal.jsx (linhas 46-51 estado; 247-294 handlers saveCodigo/marcarLancado; 643-829 render da seção Tarifação — LER TUDO para não quebrar o passo 1 de cálculo WhatsApp)
    - .planning/phases/07-faturamento-e-contas-a-receber/07-PATTERNS.md (seção "DetalheOSModal.jsx — códigos MO + Deslocamento" e o "Achado crítico")
    - mp-react/src/utils/faturamento.js (se já existir — SEGS_COM_CODIGO, digitosCodigo)
  </read_first>
  <action>
Trabalhar SOMENTE na seção "Tarifação & Faturamento" (linhas ~643-829) e no bloco de estado/handlers (linhas ~46-51 e ~247-294). NÃO tocar no passo 1 (cálculo + WhatsApp) nem no Fechamento Financeiro Interno.

1. Import: no topo do arquivo, adicionar (após os imports de tarifas.js):
```javascript
import { SEGS_COM_CODIGO, digitosCodigo } from '../../utils/faturamento.js'
```
(Se `utils/faturamento.js` ainda não existir — checar com `ls` —, definir localmente `const SEGS_COM_CODIGO = ['Mapfre','Allianz','Mondial']` e `const digitosCodigo = s => (s==='Allianz'||s==='Mondial') ? 2 : 8` no corpo do componente com um comentário TODO.)

2. Estado — substituir a linha `const [codigoForm, setCodigoForm] = useState({ codigo: '', valor_aprovado: '' })` por DOIS forms:
```javascript
const [codigoMoForm,     setCodigoMoForm]     = useState({ codigo: '', valor: '' })
const [codigoDeslocForm, setCodigoDeslocForm] = useState({ codigo: '', valor: '' })
const [savingCodigo,     setSavingCodigo]     = useState(false)
```

3. Migração / sincronização do form ao abrir a OS: no `useEffect` que sincroniza forms (começa na linha ~60), popular os dois forms a partir dos campos NOVOS, com fallback ao LEGADO para MO:
```javascript
setCodigoMoForm({
  codigo: selected.fat_codigo_mo ?? selected.fat_codigo ?? '',        // legado fat_codigo = código de MO
  valor:  String(selected.fat_valor_mo ?? selected.fat_valor_aprovado ?? ''),
})
setCodigoDeslocForm({
  codigo: selected.fat_codigo_desloc ?? '',
  valor:  String(selected.fat_valor_desloc ?? ''),
})
```

4. Substituir o handler `saveCodigo` por `saveCodigos` que grava os DOIS pares no doc da OS. IMPORTANTE (decisão de modelagem para FAT-05): o valor do código é o valor LIBERADO pela seguradora e é gravado APENAS em `fat_valor_mo`/`fat_valor_desloc`. NÃO sobrescrever `mo_seguradora` nem `valor_deslocamento` — esses continuam sendo o "valor da OS" (o valor esperado/registrado no fechamento), para que a fila (Plano 04) possa DESTACAR a divergência entre o valor liberado e o valor da OS. Deslocamento vazio = não pendente (grava string vazia / valor 0):
```javascript
async function saveCodigos() {
  if (!codigoMoForm.codigo.trim()) { showToast('Informe ao menos o código de mão de obra.', 'error'); return }
  setSavingCodigo(true)
  try {
    const valorMo     = parseFloat(codigoMoForm.valor) || 0
    const temDesloc   = !!codigoDeslocForm.codigo.trim()
    const valorDesloc = parseFloat(codigoDeslocForm.valor) || 0
    const payload = {
      fat_codigo_mo:      codigoMoForm.codigo.trim(),
      fat_valor_mo:       valorMo,
      fat_codigo_desloc:  temDesloc ? codigoDeslocForm.codigo.trim() : '',
      fat_valor_desloc:   temDesloc ? valorDesloc : 0,
      fat_codigo_em:      serverTimestamp(),
      // NÃO alimentar mo_seguradora/valor_deslocamento aqui: preservar o valor da OS para a divergência (FAT-05)
    }
    await atualizarOS(empresaId, selected.id, payload)
    const updated = { ...selected, ...payload }
    setReports(p => p.map(r => r.id === selected.id ? updated : r))
    setSelected(updated)
    showToast('🔑 Códigos salvos!')
  } catch (e) { showToast('Erro: ' + e.message, 'error') }
  finally { setSavingCodigo(false) }
}
```
Manter o handler `marcarLancado` existente por ora (o checklist "lançado" definitivo vive na fila do Plano 04); ele pode continuar gravando `fat_lancado_em` como estava.
  </action>
  <verify>
    <automated>cd mp-react && grep -c "fat_codigo_mo" src/components/admin/DetalheOSModal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fat_codigo_mo" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 2 ou mais
    - `grep -c "fat_codigo_desloc" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 2 ou mais
    - `grep -c "selected.fat_codigo ??" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 1 (migração do legado fat_codigo → MO)
    - `grep -c "async function saveCodigos" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 1
    - `grep -c "codigoForm.codigo" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 0 (o form único legado foi removido)
    - `grep -c "mo_seguradora:      valorMo" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 0 (não sobrescreve o valor da OS — preserva divergência)
  </acceptance_criteria>
  <done>Estado tem dois forms (MO + deslocamento), o handler saveCodigos grava os quatro campos fat_* no doc da OS sem sobrescrever mo_seguradora, e a leitura migra o legado fat_codigo/fat_valor_aprovado para o par de MO.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar os dois campos de código só para seguradoras com código; corrigir dígitos e aviso Tempo/Maxpar</name>
  <files>mp-react/src/components/admin/DetalheOSModal.jsx</files>
  <read_first>
    - mp-react/src/components/admin/DetalheOSModal.jsx (render do Passo 2 "Código recebido", linhas ~761-801; e a linha 657 do codigoDigitos)
    - .planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md (linhas 22-25: Tempo/Maxpar NÃO têm código)
  </read_first>
  <action>
No render da seção Tarifação:

1. Substituir a linha `const codigoDigitos = seg === 'Allianz' || seg === 'Tempo' ? 2 : 8` por:
```javascript
const codigoDigitos = digitosCodigo(seg)
const temCodigo = SEGS_COM_CODIGO.includes(seg)
```

2. No bloco "2 · Código recebido da seguradora", renderizar condicionalmente:
   - Se `temCodigo` (Mapfre/Allianz/Mondial): renderizar DOIS pares de campos (código + valor), um rotulado "Mão de obra" e outro "Deslocamento (opcional)", cada um usando `maxLength={codigoDigitos}`, `placeholder={'X'.repeat(codigoDigitos)}`, ligados a `codigoMoForm`/`setCodigoMoForm` e `codigoDeslocForm`/`setCodigoDeslocForm`. Copiar o estilo do input monospace atual (linha 779: `fontFamily:'monospace', letterSpacing:3`). Abaixo, um único botão "💾 Salvar códigos" chamando `saveCodigos` (disabled quando `savingCodigo`). Preservar o resumo verde de código já salvo (linhas 765-770) mostrando `selected.fat_codigo_mo` e, se houver, `selected.fat_codigo_desloc` com seus valores via `fmtBRL`.
   - Senão (Tempo/Maxpar/sem código): NÃO renderizar campos de código. Renderizar um aviso informativo:
```jsx
<div style={{ background:'#eef3ff', border:'1px solid #adc5f5', borderRadius:6, padding:'10px 14px', fontSize:'.86rem', color:'#1a3fa8' }}>
  ℹ️ {seg || 'Esta seguradora'} não usa código de faturamento. Esta OS entra automaticamente na fila de faturamento pelo nº da assistência ({selected.num_assist || '—'}).
</div>
```

3. O Passo 3 "Lançado no portal" existente: manter, mas ajustar o `disabled`/`title` do botão marcarLancado de `!selected.fat_codigo` para `temCodigo && !selected.fat_codigo_mo` (só exige código quando a seguradora usa código). Para Tempo/Maxpar o botão fica habilitado.

Rótulos e mensagens em português simples (usuário não técnico).
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run 2>&1 | tail -5; grep -c "digitosCodigo(seg)" src/components/admin/DetalheOSModal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "seg === 'Allianz' || seg === 'Tempo' ? 2 : 8" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 0 (regra bugada removida)
    - `grep -c "digitosCodigo(seg)" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 1
    - `grep -c "temCodigo" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 2 ou mais
    - `grep -c "não usa código de faturamento" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 1 (aviso Tempo/Maxpar)
    - `grep -c "Deslocamento (opcional)" mp-react/src/components/admin/DetalheOSModal.jsx` retorna 1
    - `cd mp-react && npx vitest run` continua verde (nenhum teste existente quebrado)
    - `cd mp-react && npm run build` conclui sem erro de sintaxe/JSX
  </acceptance_criteria>
  <done>Mapfre/Allianz/Mondial mostram dois campos de código (MO obrigatório + deslocamento opcional) com dígitos corretos; Tempo/Maxpar mostram aviso de fila automática; build e testes existentes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin (browser) → Firestore checklist/{osId} | Admin digita códigos e valores que são gravados no doc da OS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | valores de faturamento gravados na OS | mitigate | parseFloat(...) || 0 em todos os valores; maxLength no campo de código; escrita restrita à empresa do admin pelas Firestore rules já vigentes |
| T-07-04 | Elevation of Privilege | escrita em checklist/{osId} de outra empresa | accept | Firestore rules existentes já isolam por empresaId (multi-tenant); fora do escopo deste plano alterá-las |
</threat_model>

<verification>
- `cd mp-react && npx vitest run` verde
- `cd mp-react && npm run build` sem erro
- Abrir uma OS Mapfre no modal → dois campos de código (8 dígitos); OS Allianz → 2 dígitos; OS Tempo → aviso, sem campos
</verification>

<success_criteria>
- FAT-01 entregue: dois códigos por OS (MO + deslocamento opcional) com valor, gravados em fat_codigo_mo/fat_valor_mo/fat_codigo_desloc/fat_valor_desloc
- Legado fat_codigo/fat_valor_aprovado migrado para MO na leitura
- Regra errada de dígitos (Tempo=2) removida; Tempo/Maxpar sem campo de código
- mo_seguradora preservado (não sobrescrito pelo código) para permitir a divergência do Plano 04
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-faturamento-e-contas-a-receber/07-02-SUMMARY.md`
</output>

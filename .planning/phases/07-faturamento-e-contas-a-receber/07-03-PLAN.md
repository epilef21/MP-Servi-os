---
phase: 07-faturamento-e-contas-a-receber
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - mp-react/src/components/admin/ConfigTab.jsx
autonomous: true
requirements: [FAT-07]
must_haves:
  truths:
    - "O admin vê e edita, na aba Config, as faixas do calendário de pagamento da Mapfre e da Allianz/Mondial"
    - "As regras iniciam pré-preenchidas com o calendário oficial (fallback REGRAS_FATURAMENTO_PADRAO) quando o admin ainda não customizou"
    - "Salvar grava em config.calendarioFaturamento sem apagar outras chaves do config nem outras seguradoras"
    - "A faixa Allianz 26–31 fica marcada como não faturável e é editável"
  artifacts:
    - path: "mp-react/src/components/admin/ConfigTab.jsx"
      provides: "Sub-aba/seção de edição dos calendários de pagamento por seguradora, persistida em config.calendarioFaturamento"
      contains: "calendarioFaturamento"
  key_links:
    - from: "mp-react/src/components/admin/ConfigTab.jsx"
      to: "config doc (refConfig) via updateDoc"
      via: "updateDoc(refConfig(empresaId), { calendarioFaturamento })"
      pattern: "calendarioFaturamento"
    - from: "mp-react/src/components/admin/ConfigTab.jsx"
      to: "mp-react/src/utils/faturamento.js"
      via: "import REGRAS_FATURAMENTO_PADRAO como fallback do form"
      pattern: "REGRAS_FATURAMENTO_PADRAO"
---

<objective>
Adicionar à aba Config uma seção editável para os calendários de pagamento por seguradora (Mapfre e Allianz/Mondial), inicializada a partir de `config.calendarioFaturamento` com fallback para `REGRAS_FATURAMENTO_PADRAO` (Plano 01). Salvar mescla no doc `config` sem sobrescrever outras chaves.

Purpose: FAT-07 — "regras editáveis na Config" (decisão LOCKED). Se a seguradora mudar o calendário, o admin ajusta sem programador. A função `getRegrasFaturamento` (Plano 01) já lê exatamente de `config.calendarioFaturamento`.
Output: ConfigTab.jsx com nova seção "Calendário de pagamento".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md
@.planning/phases/07-faturamento-e-contas-a-receber/07-PATTERNS.md

<interfaces>
<!-- Analog EXATO: sub-aba "tarifas" do ConfigTab (config editável por seguradora, mesclado num doc único). NÃO reaproveitar a sub-aba 'calendario' — ela é Google Calendar. -->

De mp-react/src/components/admin/ConfigTab.jsx:
```javascript
import { db, auth, storage, refConfig, refEmpresa, updateDoc, doc, ... } from '../../firebase.js'
import { TABELAS, DESL_FAIXAS_PADRAO } from '../../utils/tarifas.js'
const { empresa, config, empresaId, showToast, ... } = useAdminContext()
const [configAba, setConfigAba] = useState('empresa')
```

Padrão de salvar mesclando (saveTarifas, linhas 213-236) — NÃO sobrescrever config inteiro:
```javascript
const tarifasAtuais = config?.tarifas || {}
const tarifas = { ...tarifasAtuais }
// ...preencher por seguradora...
await updateDoc(refConfig(empresaId), { tarifas })
```

Barra de sub-abas (linhas 252-262) — 'calendario' JÁ É Google Calendar, não confundir:
```javascript
{ id: 'empresa', ... }, { id: 'tarifas', ... }, { id: 'calendario', ... /* Google Calendar */ }, { id: 'conta', ... }
```

De mp-react/src/utils/faturamento.js (Plano 01 — já existe nesta wave):
```javascript
export const REGRAS_FATURAMENTO_PADRAO = {
  Mapfre:  { faixas: [ {diaDe:1,diaAte:10,addMeses:1,diaPagto:1}, {diaDe:11,diaAte:25,addMeses:1,diaPagto:16}, {diaDe:26,diaAte:31,addMeses:2,diaPagto:1} ] },
  Allianz: { faixas: [ {diaDe:1,diaAte:3,addMeses:0,diaPagto:13}, {diaDe:4,diaAte:10,addMeses:0,diaPagto:25}, {diaDe:11,diaAte:20,addMeses:1,diaPagto:3}, {diaDe:21,diaAte:25,addMeses:1,diaPagto:13}, {diaDe:26,diaAte:31,naoFaturavel:true} ] },
  // Mondial = Allianz
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estado + inicialização do form de calendário a partir do config com fallback padrão</name>
  <files>mp-react/src/components/admin/ConfigTab.jsx</files>
  <read_first>
    - mp-react/src/components/admin/ConfigTab.jsx (linhas 1-60 imports/estado; 124-135 padrão de init de form; 213-236 saveTarifas; 250-263 barra de sub-abas)
    - mp-react/src/utils/faturamento.js (REGRAS_FATURAMENTO_PADRAO)
    - .planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md (regras LOCKED do calendário)
  </read_first>
  <action>
1. Import: adicionar `import { REGRAS_FATURAMENTO_PADRAO } from '../../utils/faturamento.js'`.

2. Estado: adicionar
```javascript
const [calForm,      setCalForm]      = useState({}) // { Mapfre: {faixas:[...]}, Allianz: {faixas:[...]} }
const [savingCal,    setSavingCal]    = useState(false)
```

3. Inicializar `calForm` num `useEffect` que roda quando `config` muda (espelhar o padrão de init do tarifaForm, linhas 124-133). Para cada seguradora em `['Mapfre','Allianz']`, usar `config?.calendarioFaturamento?.[seg]?.faixas` se existir, senão `REGRAS_FATURAMENTO_PADRAO[seg].faixas`. Fazer deep copy (map) para não mutar a constante:
```javascript
useEffect(() => {
  const cf = {}
  ;['Mapfre','Allianz'].forEach(seg => {
    const saved = config?.calendarioFaturamento?.[seg]?.faixas
    const base  = (Array.isArray(saved) && saved.length) ? saved : REGRAS_FATURAMENTO_PADRAO[seg].faixas
    cf[seg] = { faixas: base.map(f => ({ ...f })) }
  })
  setCalForm(cf)
}, [config])
```
(Mondial compartilha o calendário da Allianz — não editar Mondial separado; documentar isso na UI com um texto "Allianz/Mondial usam o mesmo calendário".)

4. Handler `saveCalendario` mesclando no config (NÃO sobrescrever outras chaves) e refletindo Mondial = Allianz ao salvar:
```javascript
async function saveCalendario() {
  if (!empresaId) return
  setSavingCal(true)
  try {
    const atual = config?.calendarioFaturamento || {}
    const calendarioFaturamento = {
      ...atual,
      Mapfre:  { faixas: (calForm.Mapfre?.faixas  || []).map(f => normalizarFaixa(f)) },
      Allianz: { faixas: (calForm.Allianz?.faixas || []).map(f => normalizarFaixa(f)) },
    }
    calendarioFaturamento.Mondial = calendarioFaturamento.Allianz // espelho
    await updateDoc(refConfig(empresaId), { calendarioFaturamento })
    showToast('✅ Calendário de pagamento salvo!')
  } catch (e) { showToast('Erro ao salvar: ' + e.message, 'error') }
  finally { setSavingCal(false) }
}
```
onde `normalizarFaixa` converte os campos numéricos com parseInt e preserva `naoFaturavel`:
```javascript
function normalizarFaixa(f) {
  const base = { diaDe: parseInt(f.diaDe)||1, diaAte: parseInt(f.diaAte)||31 }
  if (f.naoFaturavel) return { ...base, naoFaturavel: true }
  return { ...base, addMeses: parseInt(f.addMeses)||0, diaPagto: parseInt(f.diaPagto)||1 }
}
```
Definir `normalizarFaixa` como função de módulo (fora do componente) ou helper interno.
  </action>
  <verify>
    <automated>cd mp-react && grep -c "calendarioFaturamento" src/components/admin/ConfigTab.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REGRAS_FATURAMENTO_PADRAO" mp-react/src/components/admin/ConfigTab.jsx` retorna 2 ou mais (import + uso no init)
    - `grep -c "async function saveCalendario" mp-react/src/components/admin/ConfigTab.jsx` retorna 1
    - `grep -c "calendarioFaturamento.Mondial = calendarioFaturamento.Allianz" mp-react/src/components/admin/ConfigTab.jsx` retorna 1 (espelho Mondial)
    - `grep -c "updateDoc(refConfig(empresaId), { calendarioFaturamento })" mp-react/src/components/admin/ConfigTab.jsx` retorna 1
    - `grep -c "normalizarFaixa" mp-react/src/components/admin/ConfigTab.jsx` retorna 3 ou mais
  </acceptance_criteria>
  <done>calForm inicializa do config com fallback padrão, saveCalendario mescla em config.calendarioFaturamento sem apagar outras chaves e espelha Mondial=Allianz.</done>
</task>

<task type="auto">
  <name>Task 2: UI de edição das faixas de calendário na aba Config</name>
  <files>mp-react/src/components/admin/ConfigTab.jsx</files>
  <read_first>
    - mp-react/src/components/admin/ConfigTab.jsx (barra de sub-abas linhas 250-263; render da sub-aba tarifas linhas 372-450 como analog de layout)
  </read_first>
  <action>
Expor a edição do calendário. Escolha a opção mais simples: adicionar uma SEÇÃO dentro da sub-aba `tarifas` existente (logo abaixo das tarifas de deslocamento), com um título próprio — evita criar sub-aba nova e o risco de colidir com a sub-aba 'calendario' do Google Calendar.

Renderizar, quando `configAba === 'tarifas'`, uma seção:
```jsx
<h3 className="config-section-title" style={{ marginTop: 28 }}>📅 Calendário de pagamento por seguradora</h3>
<p style={{ fontSize:'.82rem', color:'var(--muted)', marginBottom:12 }}>
  Quando você envia a nota fiscal, o sistema calcula sozinho a data prevista de pagamento usando estas regras. Ajuste aqui se a seguradora mudar o calendário. Allianz e Mondial usam o mesmo calendário.
</p>
```
Para cada seguradora em `['Mapfre','Allianz']`, renderizar um bloco com o nome e uma lista das faixas de `calForm[seg].faixas`. Cada faixa é uma linha editável com inputs numéricos:
- "Do dia" (`diaDe`), "Até o dia" (`diaAte`)
- Se `faixa.naoFaturavel`: mostrar um selo "🚫 Não faturável (não emitir nesse período)" em vez dos campos de pagamento.
- Senão: "Paga em +N meses" (`addMeses`) e "no dia" (`diaPagto`).

Handler de edição inline (imutável):
```javascript
function setFaixa(seg, idx, campo, valor) {
  setCalForm(p => {
    const faixas = p[seg].faixas.map((f, i) => i === idx ? { ...f, [campo]: valor } : f)
    return { ...p, [seg]: { faixas } }
  })
}
```
Exemplo de linha (repetir para cada campo):
```jsx
{calForm[seg]?.faixas.map((f, idx) => (
  <div key={idx} style={{ display:'flex', gap:8, alignItems:'center', flexWrap:'wrap', marginBottom:6 }}>
    <span>Do dia</span>
    <input type="number" min="1" max="31" value={f.diaDe} onChange={e => setFaixa(seg, idx, 'diaDe', e.target.value)} style={{ width:56 }} />
    <span>até</span>
    <input type="number" min="1" max="31" value={f.diaAte} onChange={e => setFaixa(seg, idx, 'diaAte', e.target.value)} style={{ width:56 }} />
    {f.naoFaturavel
      ? <span style={{ color:'#c0392b', fontWeight:700 }}>🚫 Não faturável</span>
      : <>
          <span>→ paga em +</span>
          <input type="number" min="0" max="3" value={f.addMeses} onChange={e => setFaixa(seg, idx, 'addMeses', e.target.value)} style={{ width:48 }} />
          <span>mês(es), no dia</span>
          <input type="number" min="1" max="31" value={f.diaPagto} onChange={e => setFaixa(seg, idx, 'diaPagto', e.target.value)} style={{ width:56 }} />
        </>}
  </div>
))}
```
Ao final, um botão "💾 Salvar calendário" chamando `saveCalendario` (disabled quando `savingCal`), no mesmo estilo do botão de salvar tarifas.

Usar classes/estilos coerentes com o restante do ConfigTab. Todo texto em português simples.
  </action>
  <verify>
    <automated>cd mp-react && npx vite build 2>&1 | tail -5; grep -c "Salvar calendário" src/components/admin/ConfigTab.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Calendário de pagamento por seguradora" mp-react/src/components/admin/ConfigTab.jsx` retorna 1
    - `grep -c "function setFaixa" mp-react/src/components/admin/ConfigTab.jsx` retorna 1
    - `grep -c "Não faturável" mp-react/src/components/admin/ConfigTab.jsx` retorna 1 ou mais
    - `grep -c "onClick={saveCalendario}" mp-react/src/components/admin/ConfigTab.jsx` retorna 1
    - `grep -c "id: 'calendario'" mp-react/src/components/admin/ConfigTab.jsx` retorna 1 (a sub-aba Google Calendar NÃO foi duplicada nem renomeada)
    - `cd mp-react && npx vite build` conclui sem erro
  </acceptance_criteria>
  <done>Seção editável de calendário aparece na sub-aba Tarifas, permite ajustar as faixas de Mapfre e Allianz (incluindo marca de não-faturável), e salva via saveCalendario; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin (browser) → Firestore config doc | Admin edita regras de calendário gravadas em config.calendarioFaturamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | valores de faixa fora de intervalo (dia 99) | mitigate | normalizarFaixa aplica parseInt com fallback; inputs com min/max; calcularDataPrevista (Plano 01) tolera faixa não encontrada retornando semCalendario |
| T-07-06 | Denial of Service | updateDoc sobrescrever config inteiro | mitigate | Merge explícito: só a chave calendarioFaturamento é enviada; outras chaves do config preservadas |
</threat_model>

<verification>
- `cd mp-react && npx vite build` sem erro
- Abrir Config → Tarifas → seção Calendário → editar uma faixa → Salvar → recarregar mostra o valor persistido
</verification>

<success_criteria>
- FAT-07 (parte de config): calendários de Mapfre e Allianz/Mondial editáveis e persistidos em config.calendarioFaturamento, com fallback pré-preenchido do REGRAS_FATURAMENTO_PADRAO
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-faturamento-e-contas-a-receber/07-03-SUMMARY.md`
</output>

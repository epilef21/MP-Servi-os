---
phase: 07-faturamento-e-contas-a-receber
plan: 05
type: execute
wave: 3
depends_on: [07-01, 07-04]
files_modified:
  - mp-react/src/components/admin/faturamento/ModalFecharNota.jsx
  - mp-react/src/components/admin/faturamento/AbaFaturamento.jsx
autonomous: true
requirements: [FAT-06, FAT-07, FAT-08, FAT-09, FAT-10]
must_haves:
  truths:
    - "O admin fecha uma nota informando número da fatura e data de emissão, vinculando todos os itens marcados como lançados; a nota guarda o total somado"
    - "Ao escolher a data de emissão, o sistema mostra a data prevista de pagamento calculada pelo calendário da seguradora"
    - "Se a data de emissão cai em período sem faturamento (Allianz 26–31), o sistema avisa e impede fechar a nota"
    - "Seguradora sem calendário (Tempo/Maxpar) permite informar a data prevista manualmente"
    - "As notas aparecem com status aguardando / paga / atrasada, e há um painel de total a receber por seguradora com datas previstas"
    - "Marcar a nota como paga quita de uma vez todas as OS vinculadas"
  artifacts:
    - path: "mp-react/src/components/admin/faturamento/ModalFecharNota.jsx"
      provides: "Modal de fechamento de nota: número, data emissão, preview da data prevista, aviso não-faturável, data manual, total e gravação em notasFiscais"
      min_lines: 90
      contains: "calcularDataPrevista"
    - path: "mp-react/src/components/admin/faturamento/AbaFaturamento.jsx"
      provides: "Botão Fechar Nota, lista de notas com status derivado, painel de total a receber por seguradora, e ação marcar paga"
      contains: "notasFiscais"
  key_links:
    - from: "mp-react/src/components/admin/faturamento/ModalFecharNota.jsx"
      to: "empresas/{empresaId}/notasFiscais (addDoc)"
      via: "addDoc(collection(db, `empresas/${empresaId}/notasFiscais`), payload)"
      pattern: "notasFiscais"
    - from: "mp-react/src/components/admin/faturamento/ModalFecharNota.jsx"
      to: "mp-react/src/utils/faturamento.js"
      via: "import { calcularDataPrevista }"
      pattern: "calcularDataPrevista"
    - from: "mp-react/src/components/admin/faturamento/AbaFaturamento.jsx"
      to: "checklist/{osId} (fat_nota_*_id) via atualizarOS"
      via: "vincular itens à nota e quitar ao marcar paga"
      pattern: "fat_nota_mo_id|fat_nota_id|fat_pago_em"
---

<objective>
Fechar o ciclo de faturamento: criar o modal "Fechar Nota" (número da fatura + data de emissão, com preview da data prevista de pagamento pelo calendário, aviso de período não faturável e data manual quando não há calendário), gravar a nota na subcoleção `notasFiscais` vinculando os itens marcados como lançados, e adicionar à aba Faturamento a lista de notas com status (aguardando/paga/atrasada), o painel de total a receber por seguradora e a ação de marcar nota como paga (quitando todas as OS vinculadas de uma vez).

Purpose: FAT-06 (fechar nota com número/data/total), FAT-07 (data prevista pelo calendário — consumindo utils/faturamento.js), FAT-08 (aviso período não faturável), FAT-09 (status + painel a receber), FAT-10 (marcar paga quita tudo).
Output: ModalFecharNota.jsx + extensão de AbaFaturamento.jsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md
@.planning/phases/07-faturamento-e-contas-a-receber/07-PATTERNS.md

<interfaces>
<!-- De utils/faturamento.js (Plano 01) -->
```javascript
export function calcularDataPrevista(seguradora, dataEmissao, config)
// → { data: 'YYYY-MM-DD'|null, naoFaturavel: boolean, semCalendario: boolean }
```

<!-- De firebase.js (re-exports disponíveis) -->
```javascript
import { db, collection, addDoc, getDocs, updateDoc, doc, query, orderBy, serverTimestamp, atualizarOS } from '../../../firebase.js'
```
NOTA: firebase.js NÃO exporta writeBatch. Para quitar várias OS de uma vez, usar Promise.all de atualizarOS (loop), NÃO writeBatch.

<!-- Contexto -->
```javascript
const { empresaId, reports, setReports, showToast, config } = useAdminContext()
```

<!-- Estrutura da fila (Plano 04, AbaFaturamento.jsx) — cada item: -->
// { key, osId, os, tipo:'mo'|'desloc'|'single', codigo, valorCodigo, valorOS, valorSugerido, lancado, seguradora }

<!-- Padrão CRUD de subcoleção (FinanceiroEmpresaTab.jsx linhas 100-128) -->
```javascript
await addDoc(collection(db, `empresas/${empresaId}/despesasMensais`), { ...payload, criado_em: serverTimestamp() })
await updateDoc(doc(db, `empresas/${empresaId}/despesasMensais`, id), payload)
```

<!-- Modelo notasFiscais (empresas/{empresaId}/notasFiscais/{notaId}) -->
// { seguradora, numero, data_emissao:'YYYY-MM-DD', data_prevista:'YYYY-MM-DD', data_prevista_manual:bool,
//   total:number, itens:[{os_id, tipo, codigo, valor, num_assist, nome_segurado}], status:'aguardando'|'paga',
//   pago_em:'YYYY-MM-DD'|null, criado_em: serverTimestamp() }
// 'atrasada' é DERIVADO (hoje > data_prevista && status != 'paga'), nunca gravado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModalFecharNota.jsx + carga de notas + botão Fechar Nota na aba</name>
  <files>mp-react/src/components/admin/faturamento/ModalFecharNota.jsx, mp-react/src/components/admin/faturamento/AbaFaturamento.jsx</files>
  <read_first>
    - mp-react/src/components/admin/faturamento/AbaFaturamento.jsx (fila, item shape, seguradora selecionada, useAdminContext)
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx (linhas 100-128 CRUD de subcoleção; 550-583 padrão de render de modal condicional)
    - mp-react/src/utils/faturamento.js (calcularDataPrevista)
    - mp-react/src/utils/formatters.js (fmtBRL, fmtDate)
    - .planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md (seção Notas/Faturas + exemplo real nota nº 81)
  </read_first>
  <action>
1. Criar `mp-react/src/components/admin/faturamento/ModalFecharNota.jsx`. Props: `{ seguradora, itens, config, onConfirmar, onFechar, salvando }` onde `itens` são os itens marcados como lançados a vincular.

   Imports:
```javascript
import { useState, useMemo } from 'react'
import { calcularDataPrevista } from '../../../utils/faturamento.js'
import { fmtBRL, fmtDate } from '../../../utils/formatters.js'
```
   Estado: `numero`, `dataEmissao` ('YYYY-MM-DD', default hoje), `dataManual` ('YYYY-MM-DD' vazio).
   Total: `const total = useMemo(() => itens.reduce((a,i)=>a+(i.valorCodigo||0),0), [itens])`.
   Preview de data prevista (recalcula quando dataEmissao muda):
```javascript
const prev = useMemo(() => calcularDataPrevista(seguradora, dataEmissao, config), [seguradora, dataEmissao, config])
```
   Render:
   - Cabeçalho "Fechar nota — {seguradora}" e total `fmtBRL(total)` + "({itens.length} itens)".
   - Campo "Número da fatura" (input) e "Data de emissão" (input type=date).
   - Bloco de data prevista:
     - Se `prev.naoFaturavel`: aviso VERMELHO "🚫 A {seguradora} não fatura notas emitidas entre os dias 26 e 31 — escolha outra data de emissão." (FAT-08). Botão confirmar DESABILITADO.
     - Se `prev.semCalendario`: mostrar campo obrigatório "Data prevista de pagamento" (input type=date, liga em `dataManual`) com texto "Esta seguradora ainda não tem calendário cadastrado — informe a data prevista." Confirmar exige `dataManual` preenchida.
     - Senão: mostrar em VERDE "💰 Pagamento previsto para {fmtDate(prev.data)}" (bônus LOCKED: ajuda a escolher o melhor dia de envio).
   - Botão "✅ Fechar nota" chamando `onConfirmar({ numero, dataEmissao, dataPrevista: prev.semCalendario ? dataManual : prev.data, dataPrevistaManual: prev.semCalendario, total })`. Desabilitar quando: `salvando`, `!numero.trim()`, `prev.naoFaturavel`, ou (`prev.semCalendario && !dataManual`).
   - Lista compacta dos `itens` (código + valor) para conferência, espelhando o print MAWDY.
   Estilos fin-*/modal-* do projeto; português simples.

2. Em AbaFaturamento.jsx, adicionar imports e carga das notas:
```javascript
import { db, collection, addDoc, getDocs, updateDoc, doc, query, orderBy, serverTimestamp, atualizarOS } from '../../../firebase.js'
import ModalFecharNota from './ModalFecharNota.jsx'
import { useEffect } from 'react'
```
   Estado: `const [notas, setNotas] = useState([])`, `const [showFechar, setShowFechar] = useState(false)`, `const [salvandoNota, setSalvandoNota] = useState(false)`. Pegar `config` do contexto.
   Carregar notas quando muda `empresaId`:
```javascript
useEffect(() => {
  if (!empresaId) return
  getDocs(query(collection(db, `empresas/${empresaId}/notasFiscais`), orderBy('criado_em','desc')))
    .then(s => setNotas(s.docs.map(d => ({ id: d.id, ...d.data() }))))
    .catch(e => showToast('Erro ao carregar notas: ' + e.message, 'error'))
}, [empresaId])
```

3. Itens lançados a vincular e botão "Fechar Nota":
```javascript
const lancadosPendentes = fila.filter(i => i.lancado)
```
   Renderizar um botão "🧾 Fechar nota com os {lancadosPendentes.length} itens lançados" (disabled quando `lancadosPendentes.length === 0`), que faz `setShowFechar(true)`.

4. Handler `confirmarNota` (passado ao modal) — grava a nota e vincula os itens:
```javascript
async function confirmarNota({ numero, dataEmissao, dataPrevista, dataPrevistaManual, total }) {
  setSalvandoNota(true)
  try {
    const itensSnap = lancadosPendentes.map(i => ({
      os_id: i.osId, tipo: i.tipo, codigo: i.codigo, valor: i.valorCodigo,
      num_assist: i.os.num_assist || '', nome_segurado: i.os.nome_segurado || '',
    }))
    const ref = await addDoc(collection(db, `empresas/${empresaId}/notasFiscais`), {
      seguradora: segSel, numero: numero.trim(), data_emissao: dataEmissao,
      data_prevista: dataPrevista || '', data_prevista_manual: !!dataPrevistaManual,
      total, itens: itensSnap, status: 'aguardando', pago_em: null,
      criado_em: serverTimestamp(),
    })
    // Vincular cada item à nota (sai da fila)
    const campoNota = t => t === 'mo' ? 'fat_nota_mo_id' : t === 'desloc' ? 'fat_nota_desloc_id' : 'fat_nota_id'
    await Promise.all(lancadosPendentes.map(i =>
      atualizarOS(empresaId, i.osId, { [campoNota(i.tipo)]: ref.id })
    ))
    setReports(p => p.map(r => {
      const meus = lancadosPendentes.filter(i => i.osId === r.id)
      if (!meus.length) return r
      const patch = {}; meus.forEach(i => { patch[campoNota(i.tipo)] = ref.id })
      return { ...r, ...patch }
    }))
    setNotas(prev => [{ id: ref.id, seguradora: segSel, numero: numero.trim(), data_emissao: dataEmissao, data_prevista: dataPrevista||'', total, itens: itensSnap, status: 'aguardando', pago_em: null }, ...prev])
    setShowFechar(false)
    showToast('🧾 Nota fechada!')
  } catch (e) { showToast('Erro ao fechar nota: ' + e.message, 'error') }
  finally { setSalvandoNota(false) }
}
```
   Renderizar o modal:
```jsx
{showFechar && (
  <ModalFecharNota seguradora={segSel} itens={lancadosPendentes} config={config}
    salvando={salvandoNota} onConfirmar={confirmarNota} onFechar={() => setShowFechar(false)} />
)}
```
  </action>
  <verify>
    <automated>cd mp-react && npx vite build 2>&1 | tail -8; grep -c "notasFiscais" src/components/admin/faturamento/AbaFaturamento.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f mp-react/src/components/admin/faturamento/ModalFecharNota.jsx` (arquivo existe)
    - `grep -c "calcularDataPrevista" mp-react/src/components/admin/faturamento/ModalFecharNota.jsx` retorna 1 ou mais
    - `grep -c "naoFaturavel" mp-react/src/components/admin/faturamento/ModalFecharNota.jsx` retorna 1 ou mais (aviso FAT-08)
    - `grep -c "semCalendario" mp-react/src/components/admin/faturamento/ModalFecharNota.jsx` retorna 1 ou mais (data manual)
    - `grep -c "Pagamento previsto para" mp-react/src/components/admin/faturamento/ModalFecharNota.jsx` retorna 1 (preview FAT-07)
    - `grep -c "addDoc(collection(db, \`empresas/\${empresaId}/notasFiscais\`)" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1
    - `grep -c "fat_nota_mo_id\|fat_nota_id\|fat_nota_desloc_id" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1 ou mais (vínculo)
    - `grep -c "Promise.all" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1 ou mais (sem writeBatch)
    - `cd mp-react && npx vite build` conclui sem erro
  </acceptance_criteria>
  <done>Modal Fechar Nota calcula/mostra a data prevista pelo calendário, avisa e bloqueia no período não faturável (FAT-08), pede data manual quando não há calendário, e ao confirmar grava a nota em notasFiscais com o total somado e vincula os itens lançados (que saem da fila). Build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Lista de notas com status, painel a receber por seguradora e marcar paga</name>
  <files>mp-react/src/components/admin/faturamento/AbaFaturamento.jsx</files>
  <read_first>
    - mp-react/src/components/admin/faturamento/AbaFaturamento.jsx (estado notas, config, handlers da Task 1)
    - mp-react/src/utils/formatters.js (fmtBRL, fmtDate)
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx (linhas 260/313 window.confirm)
  </read_first>
  <action>
1. Helper de status derivado (definir como função de módulo, fora do componente, no topo de AbaFaturamento.jsx):
```javascript
function statusNota(nota) {
  if (nota.status === 'paga') return 'paga'
  const hoje = new Date()
  const hojeStr = `${hoje.getFullYear()}-${String(hoje.getMonth()+1).padStart(2,'0')}-${String(hoje.getDate()).padStart(2,'0')}`
  if (nota.data_prevista && nota.data_prevista < hojeStr) return 'atrasada'
  return 'aguardando'
}
const STATUS_NOTA_META = {
  aguardando: { label: '⏳ Aguardando pagamento', cor: '#b8860b', bg: '#fff8e1' },
  paga:       { label: '✅ Paga',                  cor: '#1e6e3e', bg: '#e8f5e9' },
  atrasada:   { label: '🔴 Atrasada',              cor: '#c0392b', bg: '#fdecea' },
}
```

2. Seção "Notas" na aba (abaixo da fila), listando `notas` da seguradora selecionada (`notas.filter(n => n.seguradora === segSel)`). Para cada nota mostrar: número, data de emissão (`fmtDate`), data prevista (`fmtDate(n.data_prevista)`), total (`fmtBRL(n.total)`), qtd de itens, e um selo com `STATUS_NOTA_META[statusNota(n)]` (label/cor/bg). Nota `aguardando`/`atrasada` mostra botão "💵 Marcar como paga".

3. Painel-resumo "Total a receber por seguradora" (FAT-09) — no topo da aba ou em bloco próprio. Somar as notas NÃO pagas por seguradora, com a data prevista mais próxima:
```javascript
const aReceber = useMemo(() => {
  const m = {}
  notas.filter(n => n.status !== 'paga').forEach(n => {
    if (!m[n.seguradora]) m[n.seguradora] = { total: 0, proxData: null }
    m[n.seguradora].total += (n.total || 0)
    if (n.data_prevista && (!m[n.seguradora].proxData || n.data_prevista < m[n.seguradora].proxData))
      m[n.seguradora].proxData = n.data_prevista
  })
  return m
}, [notas])
```
   Renderizar cada seguradora: "{seg}: {fmtBRL(total)} até {fmtDate(proxData)}" (ex.: "Mapfre R$ 3.557,80 até 01/08"). Se `proxData` vazia, mostrar "(data manual pendente)".

4. Handler `marcarNotaPaga(nota)` (FAT-10 — quita todas as OS vinculadas de uma vez):
```javascript
async function marcarNotaPaga(nota) {
  if (!window.confirm(`Marcar a nota ${nota.numero} como paga? Isso quita todas as ${nota.itens?.length||0} OS vinculadas.`)) return
  const hoje = new Date()
  const hojeStr = `${hoje.getFullYear()}-${String(hoje.getMonth()+1).padStart(2,'0')}-${String(hoje.getDate()).padStart(2,'0')}`
  try {
    await updateDoc(doc(db, `empresas/${empresaId}/notasFiscais`, nota.id), { status: 'paga', pago_em: hojeStr })
    // Quitar as OS vinculadas (carimbo fat_pago_em) — de uma vez, via Promise.all
    await Promise.all((nota.itens||[]).map(it => atualizarOS(empresaId, it.os_id, { fat_pago_em: hojeStr })))
    setNotas(prev => prev.map(n => n.id === nota.id ? { ...n, status: 'paga', pago_em: hojeStr } : n))
    setReports(p => p.map(r => (nota.itens||[]).some(it => it.os_id === r.id) ? { ...r, fat_pago_em: hojeStr } : r))
    showToast('💵 Nota marcada como paga! OS vinculadas quitadas.')
  } catch (e) { showToast('Erro: ' + e.message, 'error') }
}
```

Todo o texto em português simples (usuário não técnico).
  </action>
  <verify>
    <automated>cd mp-react && npx vite build 2>&1 | tail -6; grep -c "statusNota\|marcarNotaPaga\|aReceber" src/components/admin/faturamento/AbaFaturamento.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function statusNota" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1
    - `grep -c "atrasada" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 2 ou mais (derivação + meta)
    - `grep -c "async function marcarNotaPaga" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1
    - `grep -c "status: 'paga', pago_em: hojeStr" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1
    - `grep -c "Promise.all((nota.itens" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1 (quita OS vinculadas de uma vez — FAT-10)
    - `grep -c "Total a receber por seguradora" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 1 (painel FAT-09)
    - `grep -c "window.confirm" mp-react/src/components/admin/faturamento/AbaFaturamento.jsx` retorna 2 ou mais (desmarcar lançado do Plano 04 + marcar paga)
    - `cd mp-react && npx vite build` conclui sem erro
  </acceptance_criteria>
  <done>Notas listadas com status aguardando/paga/atrasada derivado; painel de total a receber por seguradora com a data prevista mais próxima (FAT-09); marcar nota como paga grava status/pago_em e quita todas as OS vinculadas de uma vez (FAT-10). Build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin (browser) → Firestore notasFiscais + checklist/{osId} | Criação de nota, vínculo de itens e quitação em massa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | total da nota / valores dos itens | mitigate | total é somado no cliente a partir de itens já validados na fila (parseFloat||0); itens gravados como snapshot imutável na nota |
| T-07-11 | Integridade | data de emissão em período não faturável (Allianz 26–31) | mitigate | calcularDataPrevista retorna naoFaturavel; modal desabilita o botão confirmar nesse caso (FAT-08) |
| T-07-12 | Consistência | quitação parcial se Promise.all falhar no meio | mitigate | erro é capturado e mostrado via showToast; a nota já gravada permanece; admin pode reprocessar (idempotente: fat_pago_em/status são set absolutos, não incrementais) |
| T-07-13 | Elevation of Privilege | escrita cross-tenant | accept | Firestore rules existentes isolam por empresaId |
</threat_model>

<verification>
- `cd mp-react && npx vite build` sem erro
- Marcar itens como lançados → Fechar nota (nº + data) → data prevista aparece; itens somem da fila e viram nota
- Emitir Allianz com data 28 → aviso + botão bloqueado
- Nota vencida sem pagar → status "Atrasada"; marcar paga → some do painel a receber
</verification>

<success_criteria>
- FAT-06: fechar nota (número + data emissão) vinculando itens marcados, com total somado
- FAT-07: data prevista calculada e exibida no fechamento
- FAT-08: aviso + bloqueio no período não faturável
- FAT-09: notas com status aguardando/paga/atrasada + painel total a receber por seguradora
- FAT-10: marcar paga quita todas as OS vinculadas de uma vez
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-faturamento-e-contas-a-receber/07-05-SUMMARY.md`
</output>

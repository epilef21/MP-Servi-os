---
phase: 07-faturamento-e-contas-a-receber
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/utils/faturamento.js
  - mp-react/src/__tests__/faturamento.test.js
autonomous: true
requirements: [FAT-07, FAT-08]
must_haves:
  truths:
    - "Dado uma data de emissão e a seguradora, o sistema devolve a data prevista de pagamento pelo calendário"
    - "Quando a emissão da Allianz cai entre os dias 26 e 31, o sistema sinaliza período não faturável"
    - "Quando a seguradora não tem calendário cadastrado (Tempo/Maxpar), o sistema sinaliza que a data é manual"
    - "Datas de pagamento que caem em sábado ou domingo são empurradas para a segunda-feira"
    - "A divergência entre valor do código e valor da OS é calculada como número (positivo/negativo/zero)"
  artifacts:
    - path: "mp-react/src/utils/faturamento.js"
      provides: "Regras de calendário por seguradora + cálculo puro de data prevista, próximo dia útil, aviso não-faturável e divergência de valor"
      exports: ["REGRAS_FATURAMENTO_PADRAO", "SEGS_COM_CODIGO", "SEGS_AUTO_NUM_ASSIST", "STATUS_FATURAVEIS", "getRegrasFaturamento", "calcularDataPrevista", "proximoDiaUtil", "getDivergenciaFat", "digitosCodigo"]
    - path: "mp-react/src/__tests__/faturamento.test.js"
      provides: "Testes Vitest das funções puras de calendário"
      contains: "describe('calcularDataPrevista'"
  key_links:
    - from: "mp-react/src/utils/faturamento.js"
      to: "config.calendarioFaturamento (config da empresa)"
      via: "getRegrasFaturamento(config, seguradora) com fallback para REGRAS_FATURAMENTO_PADRAO"
      pattern: "config\\?\\.calendarioFaturamento"
---

<objective>
Criar o módulo utilitário puro `utils/faturamento.js` que concentra TODA a lógica de calendário de pagamento por seguradora (Mapfre e Allianz/Mondial), o cálculo da data prevista de pagamento a partir da data de emissão da nota, o ajuste para o próximo dia útil, o aviso de período não faturável (Allianz 26–31), e o cálculo de divergência entre valor do código e valor da OS. Tudo coberto por testes Vitest.

Purpose: Este módulo é a fundação consumida por 3 outros planos (ConfigTab edita as regras, Fila de faturamento mostra divergência, Fechar Nota calcula a data prevista). Isolar em função pura garante testabilidade e uma única fonte de verdade das regras LOCKED do CONTEXT.md.
Output: `mp-react/src/utils/faturamento.js` + `mp-react/src/__tests__/faturamento.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md

<interfaces>
<!-- Analog direto: utils/tarifas.js já usa este mesmo padrão (constantes por seguradora + getX(config, seg) com fallback + função pura de cálculo). Copiar o estilo. -->

De mp-react/src/utils/tarifas.js (padrão a espelhar):
```javascript
export const DESL_FAIXAS_PADRAO = { ate200km: 1.20, acima200km: 1.80 }
export function getDeslocFaixas(config, seguradora) {
  return config?.tarifas?.[seguradora]?.deslocamento || DESL_FAIXAS_PADRAO
}
export function calcDeslocamento(km, faixas) {
  if (!km || km <= 0) return 0
  const taxa = km <= 200 ? (faixas?.ate200km || 1.20) : (faixas?.acima200km || 1.80)
  return parseFloat((km * taxa).toFixed(2))
}
```

De mp-react/src/components/admin/statusMeta.js e DashboardTab.jsx (o que conta como OS "finalizada"/faturável):
```javascript
// DashboardTab.jsx linha 104: if (r.status === 'processado' || r.status === 'enviado') m.finalizadas++
```

Padrão de teste puro (mp-react/src/__tests__/formatters.test.js):
```javascript
import { describe, it, expect } from 'vitest'
import { fmtBRL } from '../utils/formatters'
describe('fmtBRL', () => {
  it('formata numero inteiro positivo', () => { expect(fmtBRL(100)).toBe('R$ 100,00') })
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar utils/faturamento.js com regras de calendário e funções puras</name>
  <files>mp-react/src/utils/faturamento.js</files>
  <read_first>
    - mp-react/src/utils/tarifas.js (padrão exato de constantes por seguradora + getX(config,seg) com fallback + função pura)
    - mp-react/src/components/admin/statusMeta.js (valores de status válidos)
    - .planning/phases/07-faturamento-e-contas-a-receber/07-CONTEXT.md (regras LOCKED do calendário — seção "Calendário de pagamento")
  </read_first>
  <behavior>
    - calcularDataPrevista('Mapfre','2026-06-05',null) → { data:'2026-07-01', naoFaturavel:false, semCalendario:false }   (faixa 01–10 → +1 mês, dia 01; 01/07/2026 é quarta)
    - calcularDataPrevista('Mapfre','2026-06-15',null) → { data:'2026-07-16', ... }   (faixa 11–25 → +1 mês, dia 16; quinta)
    - calcularDataPrevista('Mapfre','2026-06-29',null) → { data:'2026-08-03', ... }   (faixa 26–31 → +2 meses, dia 01; 01/08/2026 é sábado → empurra p/ segunda 03/08)
    - calcularDataPrevista('Allianz','2026-06-02',null) → { data:'2026-06-15', ... }   (faixa 01–03 → mesmo mês dia 13; 13/06/2026 é sábado → segunda 15/06)
    - calcularDataPrevista('Allianz','2026-06-07',null) → { data:'2026-06-25', ... }   (faixa 04–10 → mesmo mês dia 25; quinta)
    - calcularDataPrevista('Allianz','2026-06-15',null) → { data:'2026-07-03', ... }   (faixa 11–20 → +1 mês dia 03; sexta)
    - calcularDataPrevista('Allianz','2026-06-23',null) → { data:'2026-07-13', ... }   (faixa 21–25 → +1 mês dia 13; segunda)
    - calcularDataPrevista('Allianz','2026-06-28',null) → { data:null, naoFaturavel:true, semCalendario:false }   (faixa 26–31 = NÃO faturada)
    - calcularDataPrevista('Tempo','2026-06-10',null) → { data:null, naoFaturavel:false, semCalendario:true }   (sem calendário cadastrado)
    - proximoDiaUtil(new Date(2026,7,1)) → Date de 03/08/2026 (sábado→segunda); proximoDiaUtil(new Date(2026,6,1)) → 01/07/2026 inalterado (dia útil)
    - getDivergenciaFat('130','130') → { diff:0, bateu:true }; getDivergenciaFat('150','130') → { diff:20, bateu:false }; getDivergenciaFat('','130') → { diff:-130, bateu:false }
    - digitosCodigo('Mapfre') → 8; digitosCodigo('Allianz') → 2; digitosCodigo('Mondial') → 2
    - getRegrasFaturamento({calendarioFaturamento:{Mapfre:{faixas:[{diaDe:1,diaAte:31,addMeses:0,diaPagto:5}]}}},'Mapfre') retorna as faixas customizadas do config; getRegrasFaturamento(null,'Mapfre') retorna REGRAS_FATURAMENTO_PADRAO.Mapfre.faixas
  </behavior>
  <action>
Criar `mp-react/src/utils/faturamento.js` (função pura, ZERO import de firebase/React) com EXATAMENTE estes exports:

1. Constantes de agrupamento de seguradoras (LOCKED — CONTEXT.md linhas 22–25):
```javascript
// Seguradoras que liberam DOIS códigos por OS (mão de obra + deslocamento)
export const SEGS_COM_CODIGO = ['Mapfre', 'Allianz', 'Mondial']
// Seguradoras cuja fila monta automática pelo nº da assistência, sem código
export const SEGS_AUTO_NUM_ASSIST = ['Tempo', 'Maxpar']
// Status de OS considerados "finalizados"/faturáveis (espelha DashboardTab.jsx:104)
export const STATUS_FATURAVEIS = ['processado', 'enviado']
```

2. Nº de dígitos do código por seguradora (corrige o bug do DetalheOSModal que usava 'Tempo'):
```javascript
// Mapfre = 8 dígitos ("movimento económico" MAWDY); Allianz/Mondial = 2 dígitos
export function digitosCodigo(seguradora) {
  return (seguradora === 'Allianz' || seguradora === 'Mondial') ? 2 : 8
}
```

3. Regras de calendário PADRÃO (LOCKED — copiar verbatim do CONTEXT.md "Calendário de pagamento"). Estrutura de faixa: `{ diaDe, diaAte, addMeses, diaPagto }` ou `{ diaDe, diaAte, naoFaturavel:true }`:
```javascript
export const REGRAS_FATURAMENTO_PADRAO = {
  Mapfre: {
    faixas: [
      { diaDe: 1,  diaAte: 10, addMeses: 1, diaPagto: 1  }, // envio 01–10 → paga dia 01 do mês seguinte
      { diaDe: 11, diaAte: 25, addMeses: 1, diaPagto: 16 }, // envio 11–25 → paga dia 16 do mês seguinte
      { diaDe: 26, diaAte: 31, addMeses: 2, diaPagto: 1  }, // envio 26–31 → paga dia 01 do 2º mês seguinte
    ],
  },
  Allianz: {
    faixas: [
      { diaDe: 1,  diaAte: 3,  addMeses: 0, diaPagto: 13 }, // 01–03 → dia 13 do mesmo mês
      { diaDe: 4,  diaAte: 10, addMeses: 0, diaPagto: 25 }, // 04–10 → dia 25 do mesmo mês
      { diaDe: 11, diaAte: 20, addMeses: 1, diaPagto: 3  }, // 11–20 → dia 03 do mês seguinte
      { diaDe: 21, diaAte: 25, addMeses: 1, diaPagto: 13 }, // 21–25 → dia 13 do mês seguinte
      { diaDe: 26, diaAte: 31, naoFaturavel: true         }, // 26–31 → NÃO faturada nem paga (FAT-08)
    ],
  },
}
// Mondial usa o mesmo calendário da Allianz
REGRAS_FATURAMENTO_PADRAO.Mondial = REGRAS_FATURAMENTO_PADRAO.Allianz
```

4. `getRegrasFaturamento(config, seguradora)` — faixas customizadas do admin (ConfigTab) com fallback para o padrão; retorna `null` quando a seguradora não tem calendário (Tempo/Maxpar):
```javascript
export function getRegrasFaturamento(config, seguradora) {
  const custom = config?.calendarioFaturamento?.[seguradora]?.faixas
  if (Array.isArray(custom) && custom.length > 0) return custom
  return REGRAS_FATURAMENTO_PADRAO[seguradora]?.faixas || null
}
```

5. `proximoDiaUtil(date)` — recebe um objeto Date e, enquanto cair em sábado (getDay()===6) ou domingo (getDay()===0), soma 1 dia. Feriados são IGNORADOS nesta versão (CONTEXT.md deferred). Retorna um Date:
```javascript
export function proximoDiaUtil(date) {
  const d = new Date(date.getFullYear(), date.getMonth(), date.getDate())
  while (d.getDay() === 0 || d.getDay() === 6) d.setDate(d.getDate() + 1)
  return d
}
```

6. `calcularDataPrevista(seguradora, dataEmissao, config)` — `dataEmissao` no formato 'YYYY-MM-DD'. Retorna `{ data, naoFaturavel, semCalendario }` onde `data` é string 'YYYY-MM-DD' ou null. Regras:
   - Se não há regras (getRegrasFaturamento retorna null) → `{ data:null, naoFaturavel:false, semCalendario:true }`.
   - Parse: `const [ano, mes, dia] = dataEmissao.split('-').map(Number)`.
   - Encontrar a faixa onde `dia >= diaDe && dia <= diaAte`.
   - Se a faixa tem `naoFaturavel` → `{ data:null, naoFaturavel:true, semCalendario:false }`.
   - Senão: `const alvo = new Date(ano, (mes - 1) + faixa.addMeses, faixa.diaPagto)`, aplicar `proximoDiaUtil(alvo)`, formatar como 'YYYY-MM-DD' com padStart(2,'0') em mês e dia (NÃO usar toISOString — evita bug de timezone), retornar `{ data: 'YYYY-MM-DD', naoFaturavel:false, semCalendario:false }`.
   - Guard: se dataEmissao vazia/inválida → `{ data:null, naoFaturavel:false, semCalendario:true }`.

7. `getDivergenciaFat(valorCodigo, valorOS)` — compara o valor liberado pela seguradora com o valor registrado na OS (usado para destaque verde/vermelho na Mapfre/Allianz — decisão LOCKED "Valores"). Retorna `{ diff, bateu }`:
```javascript
export function getDivergenciaFat(valorCodigo, valorOS) {
  const vc = parseFloat(valorCodigo) || 0
  const vo = parseFloat(valorOS) || 0
  const diff = parseFloat((vc - vo).toFixed(2))
  return { diff, bateu: Math.abs(diff) < 0.005 }
}
```

Formatar a data com helper local:
```javascript
function fmtISO(d) {
  return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`
}
```
  </action>
  <verify>
    <automated>cd mp-react && node -e "import('./src/utils/faturamento.js').then(m=>{const r=m.calcularDataPrevista('Mapfre','2026-06-29',null); if(r.data!=='2026-08-03') throw new Error('esperava 2026-08-03, veio '+r.data); const a=m.calcularDataPrevista('Allianz','2026-06-28',null); if(!a.naoFaturavel) throw new Error('Allianz 26-31 deveria ser naoFaturavel'); console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function calcularDataPrevista" mp-react/src/utils/faturamento.js` retorna 1
    - `grep -c "export function proximoDiaUtil" mp-react/src/utils/faturamento.js` retorna 1
    - `grep -c "export function getDivergenciaFat" mp-react/src/utils/faturamento.js` retorna 1
    - `grep -c "export function digitosCodigo" mp-react/src/utils/faturamento.js` retorna 1
    - `grep -c "export function getRegrasFaturamento" mp-react/src/utils/faturamento.js` retorna 1
    - `grep -c "REGRAS_FATURAMENTO_PADRAO" mp-react/src/utils/faturamento.js` retorna 3 ou mais (definição + Mondial alias + uso)
    - `grep -c "naoFaturavel: true" mp-react/src/utils/faturamento.js` retorna 1 (faixa Allianz 26–31)
    - `grep -v "^#" mp-react/src/utils/faturamento.js | grep -c "import.*firebase"` retorna 0 (função pura, sem I/O)
    - O comando node de verify imprime "OK"
  </acceptance_criteria>
  <done>Módulo puro criado, exporta todas as 9 constantes/funções, e o teste inline de fumaça (Mapfre 26–31 → 03/08, Allianz 26–31 → naoFaturavel) passa.</done>
</task>

<task type="auto">
  <name>Task 2: Criar suite Vitest de faturamento.test.js</name>
  <files>mp-react/src/__tests__/faturamento.test.js</files>
  <read_first>
    - mp-react/src/utils/faturamento.js (o módulo recém-criado — importar de '../utils/faturamento')
    - mp-react/src/__tests__/formatters.test.js (padrão de teste puro: import { describe, it, expect } from 'vitest', sem vi.mock)
    - CLAUDE.md (decisões de teste — Vitest, nunca Jest)
  </read_first>
  <action>
Criar `mp-react/src/__tests__/faturamento.test.js` importando de `'../utils/faturamento'`. Sem `vi.mock` (funções puras). Cobrir:

`describe('calcularDataPrevista')` com um `it` por caso da tabela de behavior da Task 1:
```javascript
import { describe, it, expect } from 'vitest'
import {
  calcularDataPrevista, proximoDiaUtil, getDivergenciaFat,
  digitosCodigo, getRegrasFaturamento, REGRAS_FATURAMENTO_PADRAO,
  SEGS_COM_CODIGO, SEGS_AUTO_NUM_ASSIST, STATUS_FATURAVEIS,
} from '../utils/faturamento'

describe('calcularDataPrevista', () => {
  it('Mapfre 01–10 → dia 01 do mês seguinte', () => {
    expect(calcularDataPrevista('Mapfre','2026-06-05',null).data).toBe('2026-07-01')
  })
  it('Mapfre 11–25 → dia 16 do mês seguinte', () => {
    expect(calcularDataPrevista('Mapfre','2026-06-15',null).data).toBe('2026-07-16')
  })
  it('Mapfre 26–31 → dia 01 do 2º mês, empurrado p/ dia útil (01/08 sáb → 03/08)', () => {
    expect(calcularDataPrevista('Mapfre','2026-06-29',null).data).toBe('2026-08-03')
  })
  it('Allianz 01–03 → dia 13 mesmo mês, empurrado (13/06 sáb → 15/06)', () => {
    expect(calcularDataPrevista('Allianz','2026-06-02',null).data).toBe('2026-06-15')
  })
  it('Allianz 04–10 → dia 25 do mesmo mês', () => {
    expect(calcularDataPrevista('Allianz','2026-06-07',null).data).toBe('2026-06-25')
  })
  it('Allianz 11–20 → dia 03 do mês seguinte', () => {
    expect(calcularDataPrevista('Allianz','2026-06-15',null).data).toBe('2026-07-03')
  })
  it('Allianz 21–25 → dia 13 do mês seguinte', () => {
    expect(calcularDataPrevista('Allianz','2026-06-23',null).data).toBe('2026-07-13')
  })
  it('Allianz 26–31 → naoFaturavel, data null (FAT-08)', () => {
    const r = calcularDataPrevista('Allianz','2026-06-28',null)
    expect(r.naoFaturavel).toBe(true)
    expect(r.data).toBeNull()
  })
  it('seguradora sem calendário (Tempo) → semCalendario, data null', () => {
    const r = calcularDataPrevista('Tempo','2026-06-10',null)
    expect(r.semCalendario).toBe(true)
    expect(r.data).toBeNull()
  })
  it('usa faixas customizadas do config quando presentes', () => {
    const cfg = { calendarioFaturamento: { Mapfre: { faixas: [{ diaDe:1, diaAte:31, addMeses:0, diaPagto:20 }] } } }
    expect(calcularDataPrevista('Mapfre','2026-06-05',cfg).data).toBe('2026-06-22') // 20/06 sáb → segunda 22/06
  })
})

describe('proximoDiaUtil', () => {
  it('sábado 01/08/2026 vira segunda 03/08', () => {
    expect(proximoDiaUtil(new Date(2026,7,1)).getDate()).toBe(3)
  })
  it('dia útil permanece inalterado', () => {
    const d = proximoDiaUtil(new Date(2026,6,1)) // 01/07/2026 quarta
    expect(d.getDate()).toBe(1)
  })
})

describe('getDivergenciaFat', () => {
  it('valores iguais → bateu true, diff 0', () => {
    expect(getDivergenciaFat('130','130')).toEqual({ diff: 0, bateu: true })
  })
  it('código maior que OS → diff positivo, bateu false', () => {
    expect(getDivergenciaFat('150','130')).toEqual({ diff: 20, bateu: false })
  })
  it('valor vazio no código → diff negativo', () => {
    expect(getDivergenciaFat('','130')).toEqual({ diff: -130, bateu: false })
  })
})

describe('digitosCodigo', () => {
  it('Mapfre = 8', () => { expect(digitosCodigo('Mapfre')).toBe(8) })
  it('Allianz = 2', () => { expect(digitosCodigo('Allianz')).toBe(2) })
  it('Mondial = 2', () => { expect(digitosCodigo('Mondial')).toBe(2) })
})

describe('grupos de seguradora', () => {
  it('Mapfre e Allianz têm código; Tempo e Maxpar entram pelo num_assist', () => {
    expect(SEGS_COM_CODIGO).toContain('Mapfre')
    expect(SEGS_AUTO_NUM_ASSIST).toEqual(['Tempo','Maxpar'])
    expect(STATUS_FATURAVEIS).toEqual(['processado','enviado'])
  })
})
```
  </action>
  <verify>
    <automated>cd mp-react && npx vitest run src/__tests__/faturamento.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd mp-react && npx vitest run src/__tests__/faturamento.test.js` termina com exit 0 e todos os testes verdes
    - `grep -c "describe('calcularDataPrevista'" mp-react/src/__tests__/faturamento.test.js` retorna 1
    - `grep -c "naoFaturavel).toBe(true)" mp-react/src/__tests__/faturamento.test.js` retorna 1 (cobre FAT-08)
    - `grep -v "^//" mp-react/src/__tests__/faturamento.test.js | grep -c "vi.mock"` retorna 0
    - Contagem de `it(` no arquivo é 20 ou mais
  </acceptance_criteria>
  <done>Suite roda verde no Vitest, cobrindo todos os casos de calendário Mapfre/Allianz, aviso não-faturável (FAT-08), sem-calendário, próximo dia útil, divergência e grupos de seguradora.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum) | Módulo 100% puro, sem I/O, sem entrada não confiável de rede. Recebe apenas dados já carregados pela aplicação. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | calcularDataPrevista com dataEmissao malformada | mitigate | Guard: dataEmissao vazia/sem faixa correspondente retorna { data:null, semCalendario:true } em vez de quebrar |
| T-07-02 | Information Disclosure | funções puras | accept | Sem PII nem I/O; nada a vazar |
</threat_model>

<verification>
- `cd mp-react && npx vitest run src/__tests__/faturamento.test.js` verde
- Nenhum import de firebase/React em faturamento.js
</verification>

<success_criteria>
- Módulo `utils/faturamento.js` exporta REGRAS_FATURAMENTO_PADRAO, SEGS_COM_CODIGO, SEGS_AUTO_NUM_ASSIST, STATUS_FATURAVEIS, getRegrasFaturamento, calcularDataPrevista, proximoDiaUtil, getDivergenciaFat, digitosCodigo
- FAT-07 (cálculo da data prevista por calendário com fallback e config editável) e FAT-08 (aviso período não faturável) cobertos por testes verdes
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-faturamento-e-contas-a-receber/07-01-SUMMARY.md`
</output>

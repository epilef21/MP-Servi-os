---
phase: 08-fechamento-de-tecnicos
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - firestore.rules
  - mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx
  - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx
autonomous: true
requirements: [TEC-02, TEC-03]
must_haves:
  truths:
    - "A nova aba interna '👷 Técnicos' aparece no Financeiro da empresa e respeita o mês selecionado (◄ ►)"
    - "Cada técnico do mês aparece com total a pagar, quantidade de OS e a lista das OS com valor_prestador"
    - "A chave PIX do técnico aparece ao lado do total com botão de copiar com um clique"
    - "A coleção fechamentosTecnicos tem regra no firestore.rules e as rules foram deployadas"
    - "Um técnico já pago no mês aparece com selo PAGO; os demais aparecem como PENDENTE"
  artifacts:
    - path: "firestore.rules"
      provides: "Regra da coleção fechamentosTecnicos"
      contains: "match /fechamentosTecnicos/{id}"
    - path: "mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx"
      provides: "Aba de fechamento mensal por técnico (visão + status derivado)"
      contains: "agruparPorTecnico"
    - path: "mp-react/src/components/admin/FinanceiroEmpresaTab.jsx"
      provides: "Entrada da aba 'tecnicos' + render de AbaFechamentoTecnicos com mesRef"
      contains: "AbaFechamentoTecnicos"
  key_links:
    - from: "AbaFechamentoTecnicos"
      to: "empresas/{id}/fechamentosTecnicos"
      via: "getDocs para derivar status pago/pendente"
      pattern: "fechamentosTecnicos"
    - from: "FinanceiroEmpresaTab"
      to: "AbaFechamentoTecnicos"
      via: "render condicional abaFin === 'tecnicos' com prop mesRef"
      pattern: "mesRef=\\{mesRef\\}"
---

<objective>
Habilitar a coleção `fechamentosTecnicos` no Firestore (regra + deploy) e criar a aba interna "👷 Técnicos" no Financeiro da empresa mostrando o **fechamento mensal por técnico**: para o mês selecionado, cada técnico com seu total a pagar, a lista das OS (valor_prestador de cada), a chave PIX à mão com copiar-com-um-clique, e o selo de status (PAGO/PENDENTE) derivado dos fechamentos já gravados. Esta é a visão de leitura do TEC-02; o marcar-pago + histórico completo vêm no plano 04.

Purpose: dar ao admin, em poucos cliques e sempre no mês certo, "quanto devo a cada técnico e por quais OS".
Output: regra deployada + novo componente `AbaFechamentoTecnicos.jsx` + wiring no `FinanceiroEmpresaTab.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fechamento-de-tecnicos/08-CONTEXT.md
@.planning/phases/07-faturamento-e-contas-a-receber/07-PATTERNS.md
@mp-react/src/utils/fechamentoTecnicos.js
@mp-react/src/components/admin/faturamento/AbaFaturamento.jsx

<interfaces>
<!-- APRENDIZADO Fase 7 (LOCKED): coleção nova SEM regra = "Missing or insufficient permissions" em produção.
     Por isso a regra + deploy são a Task 1 deste plano, antes de qualquer leitura da coleção. -->

Funções puras já disponíveis (plano 08-01) em mp-react/src/utils/fechamentoTecnicos.js:
```javascript
export function agruparPorTecnico(reports, mesRef)  // [{ tecnicoNome, tecnicoNorm, osList, total, count }]
export function acharFechamento(fechamentos, tecnicoNorm, mesRef)  // doc | null
export function statusFechamento(fechamento)  // 'pago' | 'pendente'
export function normNome(s)
```

Contrato do doc fechamentosTecnicos (gravado no plano 04, apenas LIDO aqui):
```
empresas/{id}/fechamentosTecnicos/{docId} = {
  tecnico, tecnico_norm, mes_referencia, os_ids, total, qtd_os,
  status: 'pago', pago_em, criado_em
}
```

Analog direto: mp-react/src/components/admin/faturamento/AbaFaturamento.jsx
- Imports do firebase (linha 8): `db, collection, getDocs, query, orderBy` de '../../../firebase.js'
- Contexto (linha 30): `const { empresaId, reports, showToast } = useAdminContext()`
- Carga de subcoleção via getDocs num useEffect por empresaId (linhas 37-43)
- copyToClipboard de '../../../utils/clipboard.js' (linha 11) + showToast
- fmtBRL, fmtDate de '../../../utils/formatters.js'
- Empty-state com diagnóstico explicando POR QUE está vazio (linhas 279-301)

Wiring no FinanceiroEmpresaTab.jsx:
- import de AbaFaturamento (linha 21) e entrada `{ id: 'faturamento', label: '📄 Faturamento' }` (linha 476)
- render `{!carregando && abaFin === 'faturamento' && <AbaFaturamento />}` (linha 542)
- `mesRef` é estado do FinanceiroEmpresaTab (linha 102), navegável ◄ ► (linhas 462-466)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regra da coleção fechamentosTecnicos + deploy das rules</name>
  <files>firestore.rules</files>
  <read_first>
    - firestore.rules linhas 188-217 (bloco das subcoleções financeiras: despesasMensais, notasFiscais — padrão a copiar)
  </read_first>
  <action>
Em `firestore.rules`, dentro do bloco `match /empresas/{empresaId}`, ao lado das demais subcoleções financeiras (logo após o bloco `match /notasFiscais/{id}` das linhas 213-217), adicionar:

```
      // ── /empresas/{empresaId}/fechamentosTecnicos/{id} ───
      // Fechamento mensal de pagamento por técnico (aba Técnicos).
      match /fechamentosTecnicos/{id} {
        allow read, write: if isUserOfEmpresa(empresaId) || isSuperAdmin();
      }
```

Depois, deployar SOMENTE as rules (Claude executa via CLI — não é passo manual):
```
npx firebase deploy --only firestore:rules
```
Se o CLI pedir autenticação/login e falhar, criar checkpoint de autenticação e reexecutar; não pular o deploy (aprendizado LOCKED da Fase 7).
  </action>
  <acceptance_criteria>
    - `grep -q "match /fechamentosTecnicos/{id}" firestore.rules`
    - `grep -A2 "match /fechamentosTecnicos/{id}" firestore.rules | grep -q "isUserOfEmpresa(empresaId) || isSuperAdmin()"`
    - Deploy concluído: a saída do comando contém "firestore: released rules" ou "Deploy complete!"
  </acceptance_criteria>
  <verify>
    <automated>grep -q "match /fechamentosTecnicos/{id}" "firestore.rules" && echo RULE_OK</automated>
  </verify>
  <done>Regra presente em firestore.rules e `npx firebase deploy --only firestore:rules` concluído com sucesso.</done>
</task>

<task type="auto">
  <name>Task 2: Criar AbaFechamentoTecnicos.jsx — visão do fechamento mensal por técnico</name>
  <files>mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx</files>
  <read_first>
    - mp-react/src/components/admin/faturamento/AbaFaturamento.jsx (analog: imports, contexto, getDocs, copyToClipboard, empty-state com diagnóstico)
    - mp-react/src/utils/fechamentoTecnicos.js (funções consumidas)
  </read_first>
  <action>
Criar `mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`. Componente que recebe a prop `mesRef` e consome `reports` do contexto.

Imports:
```javascript
import { useState, useEffect, useMemo } from 'react'
import { db, collection, getDocs, query, orderBy } from '../../../firebase.js'
import { useAdminContext } from '../../../contexts/AdminContext.jsx'
import { fmtBRL, fmtDate } from '../../../utils/formatters.js'
import { copyToClipboard } from '../../../utils/clipboard.js'
import { agruparPorTecnico, acharFechamento, statusFechamento, normNome } from '../../../utils/fechamentoTecnicos.js'
```

Assinatura: `export default function AbaFechamentoTecnicos({ mesRef }) { ... }`.

Comportamento:
1. `const { empresaId, tecnicos, reports, showToast } = useAdminContext()`.
2. Estado `const [fechamentos, setFechamentos] = useState([])`.
3. `useEffect` por `empresaId`: `getDocs(query(collection(db, ` + "`empresas/${empresaId}/fechamentosTecnicos`" + `), orderBy('criado_em', 'desc')))` → `setFechamentos(snap.docs.map(d => ({ id: d.id, ...d.data() })))`; `.catch(e => showToast('Erro ao carregar fechamentos: ' + e.message, 'error'))`. (mesmo padrão de AbaFaturamento linhas 37-43).
4. `const grupos = useMemo(() => agruparPorTecnico(reports, mesRef), [reports, mesRef])`.
5. Mapa de chave PIX / forma de pagamento por nome normalizado, a partir de `tecnicos` (cadastro): `const pixPorTecnico = useMemo(() => { const m = {}; (tecnicos||[]).forEach(t => { m[normNome(t.nome)] = { pix: t.chave_pix || '', forma: t.forma_pagamento || 'pix' } }); return m }, [tecnicos])`.
6. Render:
   - Título "👷 Fechamento de Técnicos — {mês}" no padrão de heading Barlow Condensed usado em AbaFaturamento.
   - Se `grupos.length === 0`: empty-state com diagnóstico: "Nenhuma OS com técnico neste mês." + linha explicando o critério ("As OS entram pelo mês de criação — navegue com ◄ ► para outro mês.").
   - Para cada `g` de `grupos`:
     - `const fech = acharFechamento(fechamentos, g.tecnicoNorm, mesRef)`
     - `const status = statusFechamento(fech)`
     - `const info = pixPorTecnico[g.tecnicoNorm] || { pix: '', forma: 'pix' }`
     - Card com: nome grande do técnico, selo de status (PAGO verde `#e8f5e9`/`#1e6e3e` quando `status === 'pago'`, senão PENDENTE âmbar `#fff8e1`/`#b8860b`), o **total** grande via `fmtBRL(fech?.total ?? g.total)` (usa o snapshot do fechamento quando pago, senão o total ao vivo), e `{fech?.qtd_os ?? g.count} OS`.
     - Se `info.pix`: mostrar a chave PIX em `fontFamily: monospace` com botão `btn-sm btn-view` "📋 Copiar PIX" que faz `await copyToClipboard(info.pix); showToast('📋 Chave PIX copiada!')`. Mostrar também a forma de pagamento (label legível: pix→PIX, dinheiro→Dinheiro, transferencia→Transferência).
     - Lista das OS do técnico (`g.osList`): cada linha com `#{os.num_assist || '—'} — {os.nome_segurado || '—'} · {os.cidade || '—'}` à esquerda e `fmtBRL(parseFloat(os.valor_prestador)||0)` à direita. Quando `status === 'pago'`, ainda mostrar a lista ao vivo (informativo) — o valor pago é o do snapshot no total.
   - Formato dos totais grande e claro (padrão pedido no CONTEXT: "João: R$ 1.240,00 — 8 OS — PENDENTE").

NÃO implementar o botão "marcar como pago" nem a seção de histórico aqui — isso é o plano 04 (deixe um espaço/placeholder de comentário `{/* marcar pago + histórico: plano 08-04 */}` no final do card e ao fim do componente para o plano 04 preencher).

Usar estilos inline no mesmo tom de AbaFaturamento (border var(--border), borderRadius 8, padding). Não criar CSS novo.
  </action>
  <acceptance_criteria>
    - `grep -q "export default function AbaFechamentoTecnicos" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "agruparPorTecnico(reports, mesRef)" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "fechamentosTecnicos" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "copyToClipboard" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "Copiar PIX" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "valor_prestador" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "plano 08-04" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npx eslint src/components/admin/fechamento/AbaFechamentoTecnicos.jsx</automated>
  </verify>
  <done>Componente criado; deriva grupos via util, status por fechamento, mostra total/lista de OS/PIX copiável; eslint sem erros.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar a aba "👷 Técnicos" no FinanceiroEmpresaTab.jsx</name>
  <files>mp-react/src/components/admin/FinanceiroEmpresaTab.jsx</files>
  <read_first>
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx linhas 19-21 (imports), 470-484 (barra de abas), 542 (render de AbaFaturamento)
  </read_first>
  <action>
Em `mp-react/src/components/admin/FinanceiroEmpresaTab.jsx`:

1. Adicionar o import (junto ao de AbaFaturamento, linha 21):
```javascript
import AbaFechamentoTecnicos from './fechamento/AbaFechamentoTecnicos.jsx'
```

2. Na barra de abas internas (array das linhas 471-476), adicionar a entrada APÓS `faturamento`:
```javascript
{ id: 'tecnicos', label: '👷 Técnicos' },
```

3. Adicionar o render condicional logo após a linha de `abaFin === 'faturamento'` (linha 542), passando `mesRef`:
```jsx
{!carregando && abaFin === 'tecnicos' && <AbaFechamentoTecnicos mesRef={mesRef} />}
```

Não alterar o comportamento das demais abas nem o seletor de mês.
  </action>
  <acceptance_criteria>
    - `grep -q "import AbaFechamentoTecnicos from './fechamento/AbaFechamentoTecnicos.jsx'" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx`
    - `grep -q "id: 'tecnicos'" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx`
    - `grep -q "abaFin === 'tecnicos' && <AbaFechamentoTecnicos mesRef={mesRef} />" mp-react/src/components/admin/FinanceiroEmpresaTab.jsx`
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npm run build</automated>
  </verify>
  <done>Aba "👷 Técnicos" aparece na barra do Financeiro e renderiza AbaFechamentoTecnicos com o mês selecionado; `npm run build` compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (client) → Firestore fechamentosTecnicos | Admin autenticado lê fechamentos da própria empresa. |
| admin (client) → Firestore checklist/tecnicos | Leitura de OS (valor_prestador) e cadastro (chave PIX) já cobertos por regras existentes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | acesso a fechamentos de outra empresa | mitigate | Regra `fechamentosTecnicos` exige `isUserOfEmpresa(empresaId)` (Task 1); path sempre `empresas/${empresaId}/...`. |
| T-08-06 | Denial (permissions) | coleção nova sem regra derruba a aba em produção | mitigate | Regra criada E deployada na Task 1 antes de qualquer getDocs (aprendizado Fase 7). |
| T-08-07 | Information Disclosure | chave PIX exibida na aba | accept | Só admin autenticado da empresa vê a aba; mesma sensibilidade já aceita para telefone/e-mail do técnico. |
</threat_model>

<verification>
- `grep -q "match /fechamentosTecnicos/{id}" firestore.rules`.
- `cd mp-react && npm run build` compila.
- Deploy das rules concluído (Task 1).
</verification>

<success_criteria>
- Aba "👷 Técnicos" no Financeiro mostra, para o mês selecionado, cada técnico com total a pagar, quantidade e lista de OS (valor_prestador), com chave PIX copiável e selo PAGO/PENDENTE.
- Coleção fechamentosTecnicos acessível sem erro de permissão.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fechamento-de-tecnicos/08-03-SUMMARY.md`
</output>

---
phase: 08-fechamento-de-tecnicos
plan: 04
type: execute
wave: 3
depends_on: [08-01, 08-03]
files_modified:
  - mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx
autonomous: true
requirements: [TEC-03]
must_haves:
  truths:
    - "Admin marca o fechamento de um técnico como pago informando a data, e o card passa a PAGO"
    - "Ao marcar pago, o sistema grava um snapshot imutável (os_ids + total da época) — histórico não muda se uma OS mudar de valor depois"
    - "Admin consulta depois o histórico de pagamentos daquele técnico (fechamentos passados, com mês, total e data de pagamento)"
    - "Não é possível pagar duas vezes o mesmo técnico no mesmo mês (botão some quando já pago)"
  artifacts:
    - path: "mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx"
      provides: "Marcar pago (addDoc snapshot) + seção de histórico de pagamentos"
      contains: "fechamentosTecnicos"
  key_links:
    - from: "botão Marcar pago"
      to: "empresas/{id}/fechamentosTecnicos"
      via: "addDoc com snapshot os_ids/total/status:'pago'/pago_em"
      pattern: "addDoc"
    - from: "seção histórico"
      to: "fechamentos (state)"
      via: "filtro por técnico + render ordenado por mês"
      pattern: "historico"
---

<objective>
Fechar o TEC-03 na aba de Técnicos: permitir **marcar o fechamento do técnico como pago** (com data), gravando um **snapshot imutável** (os_ids + total da época, como as notas da Fase 7), e exibir o **histórico consultável** de pagamentos por técnico. Isso protege o admin em discussões de "você não me pagou aquela OS".

Purpose: transformar a visão de leitura (plano 03) em fechamento de verdade — pagar, registrar e consultar depois.
Output: `AbaFechamentoTecnicos.jsx` estendido com marcar-pago + histórico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fechamento-de-tecnicos/08-CONTEXT.md
@mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx
@mp-react/src/utils/fechamentoTecnicos.js

<interfaces>
<!-- AbaFechamentoTecnicos.jsx já existe (plano 03): tem `fechamentos` (state), `grupos` (useMemo),
     acharFechamento/statusFechamento, e os placeholders `{/* marcar pago + histórico: plano 08-04 */}`. -->

Analog do padrão "gravar snapshot imutável + quitar de uma vez + status derivado" — AbaFaturamento.jsx (Fase 7):
```javascript
// snapshot imutável dos itens ao fechar a nota (linhas 194-203)
const ref = await addDoc(collection(db, `empresas/${empresaId}/notasFiscais`), {
  seguradora: segSel, numero: numero.trim(), total, itens: itensSnap,
  status: 'aguardando', pago_em: null, criado_em: serverTimestamp(),
})
// marcar paga com data no formato YYYY-MM-DD local (linhas 223-234)
const hojeStr = `${hoje.getFullYear()}-${String(hoje.getMonth()+1).padStart(2,'0')}-${String(hoje.getDate()).padStart(2,'0')}`
await updateDoc(doc(db, `empresas/${empresaId}/notasFiscais`, nota.id), { status: 'paga', pago_em: hojeStr })
```

Contrato do doc a GRAVAR (fechamentosTecnicos) — snapshot imutável:
```
{
  tecnico:         g.tecnicoNome,     // nome de exibição
  tecnico_norm:    g.tecnicoNorm,     // p/ matching (acharFechamento)
  mes_referencia:  mesRef,            // 'YYYY-MM'
  os_ids:          g.osList.map(o => o.id),   // snapshot
  total:           g.total,           // snapshot (valor da época)
  qtd_os:          g.count,           // snapshot
  status:          'pago',            // só grava quando pago (pendente é derivado)
  pago_em:         'YYYY-MM-DD',      // data escolhida pelo admin (default hoje)
  criado_em:       serverTimestamp(),
}
```
Imports do firebase já em uso no arquivo: adicionar `addDoc, serverTimestamp` (e `updateDoc`/`doc` não são necessários — o fechamento é imutável, sem edição).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marcar fechamento do técnico como pago (snapshot imutável, sem pagar 2x)</name>
  <files>mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx</files>
  <read_first>
    - mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx (estado fechamentos, grupos, placeholders do plano 04)
    - mp-react/src/components/admin/faturamento/AbaFaturamento.jsx linhas 190-235 (snapshot + marcar pago + Promise.all + hojeStr)
  </read_first>
  <action>
Em `mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`:

1. Estender o import do firebase para incluir `addDoc` e `serverTimestamp`:
```javascript
import { db, collection, getDocs, query, orderBy, addDoc, serverTimestamp } from '../../../firebase.js'
```

2. Adicionar estado de controle:
```javascript
const [pagando, setPagando] = useState(null)  // tecnicoNorm em processamento
```

3. Adicionar helper de data local (YYYY-MM-DD) e o handler `marcarPago(grupo)`:
```javascript
function hojeISO() {
  const h = new Date()
  return `${h.getFullYear()}-${String(h.getMonth() + 1).padStart(2, '0')}-${String(h.getDate()).padStart(2, '0')}`
}

async function marcarPago(g) {
  // trava de "pagar 2x": nunca grava se já existe fechamento pago para técnico+mês
  if (acharFechamento(fechamentos, g.tecnicoNorm, mesRef)) {
    showToast('Este técnico já foi pago neste mês.', 'error'); return
  }
  const dataPagto = window.prompt('Data do pagamento (AAAA-MM-DD):', hojeISO())
  if (!dataPagto) return
  if (!/^\d{4}-\d{2}-\d{2}$/.test(dataPagto)) { showToast('Data inválida. Use AAAA-MM-DD.', 'error'); return }
  if (!window.confirm(`Confirmar pagamento de ${g.tecnicoNome}: ${fmtBRL(g.total)} (${g.count} OS)?`)) return
  setPagando(g.tecnicoNorm)
  try {
    const payload = {
      tecnico: g.tecnicoNome, tecnico_norm: g.tecnicoNorm, mes_referencia: mesRef,
      os_ids: g.osList.map(o => o.id), total: g.total, qtd_os: g.count,
      status: 'pago', pago_em: dataPagto, criado_em: serverTimestamp(),
    }
    const ref = await addDoc(collection(db, `empresas/${empresaId}/fechamentosTecnicos`), payload)
    // atualização local (evita novo fetch) — mesmo padrão de AbaFaturamento
    setFechamentos(prev => [{ id: ref.id, ...payload }, ...prev])
    showToast('💵 Fechamento pago e registrado!')
  } catch (e) { showToast('Erro ao registrar pagamento: ' + e.message, 'error') }
  finally { setPagando(null) }
}
```

4. No card de cada técnico (onde estava o placeholder `{/* marcar pago + histórico: plano 08-04 */}`), quando `status !== 'pago'`, renderizar o botão:
```jsx
<button
  className="btn-sm btn-primary"
  disabled={pagando === g.tecnicoNorm || g.tecnicoNorm === ''}
  title={g.tecnicoNorm === '' ? 'OS sem técnico não podem ser pagas' : ''}
  onClick={() => marcarPago(g)}
>
  {pagando === g.tecnicoNorm ? '⏳ Registrando...' : '💵 Marcar como pago'}
</button>
```
Quando `status === 'pago'`, em vez do botão mostrar a data: `<span>✅ Pago em {fmtDate(fech.pago_em)}</span>`.

Não permitir editar/estornar o fechamento (imutável nesta versão — CONTEXT deferred).
  </action>
  <acceptance_criteria>
    - `grep -q "async function marcarPago" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "addDoc(collection(db, \`empresas/\${empresaId}/fechamentosTecnicos\`" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "os_ids: g.osList.map" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "já foi pago neste mês" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "Marcar como pago" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "serverTimestamp" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npx eslint src/components/admin/fechamento/AbaFechamentoTecnicos.jsx</automated>
  </verify>
  <done>Botão "Marcar como pago" grava snapshot imutável em fechamentosTecnicos, bloqueia pagamento duplo e pede data; card vira PAGO com a data; eslint sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Seção de histórico de pagamentos por técnico</name>
  <files>mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx</files>
  <read_first>
    - mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx (estado fechamentos já carregado; placeholder de fim do componente)
    - mp-react/src/components/admin/faturamento/AbaFaturamento.jsx linhas 343-372 (seção de lista de notas — padrão de heading + cards)
  </read_first>
  <action>
No fim do componente `AbaFechamentoTecnicos` (onde há o placeholder de histórico do plano 04), adicionar uma seção "🗂️ Histórico de pagamentos" que lista TODOS os fechamentos já gravados (de qualquer mês), permitindo consulta posterior (TEC-03).

1. Derivar a lista ordenada por mês desc e depois nome:
```javascript
const historico = useMemo(() =>
  [...fechamentos].sort((a, b) =>
    (b.mes_referencia || '').localeCompare(a.mes_referencia || '') ||
    (a.tecnico || '').localeCompare(b.tecnico || '')
  ), [fechamentos])
```

2. Render abaixo dos cards do mês (heading no mesmo estilo Barlow Condensed usado no topo):
```jsx
<h4 style={{ fontFamily: 'Barlow Condensed,sans-serif', fontSize: '.95rem', fontWeight: 800, color: 'var(--primary)', textTransform: 'uppercase', margin: '22px 0 12px' }}>
  🗂️ Histórico de pagamentos
</h4>
{historico.length === 0 && (
  <div className="empty-state" style={{ padding: 24 }}>
    <p>Nenhum pagamento registrado ainda.</p>
  </div>
)}
{historico.map(f => (
  <div key={f.id} style={{ display: 'flex', alignItems: 'center', gap: 12, padding: '10px 12px', border: '1px solid var(--border)', borderRadius: 8, marginBottom: 8, flexWrap: 'wrap' }}>
    <div style={{ flex: 1, minWidth: 180 }}>
      <div style={{ fontWeight: 600 }}>{f.tecnico}</div>
      <div style={{ fontSize: '.8rem', color: 'var(--muted)' }}>
        Ref. {f.mes_referencia} · {f.qtd_os} OS · Pago em {fmtDate(f.pago_em)}
      </div>
    </div>
    <div style={{ fontWeight: 700 }}>{fmtBRL(f.total)}</div>
    <span className="badge" style={{ background: '#e8f5e9', color: '#1e6e3e' }}>✅ Pago</span>
  </div>
))}
```

O histórico usa SEMPRE os campos do snapshot (`f.total`, `f.qtd_os`) — não recalcula a partir das OS ao vivo, garantindo imutabilidade mesmo que uma OS mude de valor depois.
  </action>
  <acceptance_criteria>
    - `grep -q "Histórico de pagamentos" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "const historico = useMemo" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "f.mes_referencia" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "fmtBRL(f.total)" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - `grep -q "Nenhum pagamento registrado" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx`
    - Placeholder do plano 04 removido: `grep -q "plano 08-04" mp-react/src/components/admin/fechamento/AbaFechamentoTecnicos.jsx` retorna 1 (não encontra)
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npm run build</automated>
  </verify>
  <done>Seção de histórico lista todos os fechamentos pagos (snapshot) com técnico, mês, qtd, data e total; empty-state quando vazio; placeholder removido; `npm run build` compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (client) → Firestore fechamentosTecnicos | Admin autenticado grava o snapshot de pagamento da própria empresa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Repudiation | "você não me pagou aquela OS" | mitigate | Snapshot imutável os_ids+total+pago_em gravado no fechamento; histórico consultável (Task 2). |
| T-08-09 | Tampering | pagar o mesmo técnico 2x no mês | mitigate | `acharFechamento` bloqueia gravação se já existe fechamento pago para técnico+mês (Task 1). |
| T-08-10 | Tampering | histórico muda se OS mudar de valor | mitigate | Histórico usa campos do snapshot (f.total/f.qtd_os), nunca recalcula das OS ao vivo. |
| T-08-11 | Elevation of Privilege | gravar fechamento em outra empresa | mitigate | Path `empresas/${empresaId}/fechamentosTecnicos`; regra isUserOfEmpresa (plano 03 Task 1). |
</threat_model>

<verification>
- `cd mp-react && npm run build` compila.
- Marcar pago grava snapshot; segundo clique no mesmo mês é bloqueado.
- Histórico lista pagamentos anteriores por técnico.
</verification>

<success_criteria>
- Admin marca fechamento como pago com data; snapshot imutável gravado.
- Histórico de pagamentos consultável por técnico, com total da época.
- Pagamento duplo no mesmo mês bloqueado.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fechamento-de-tecnicos/08-04-SUMMARY.md`
</output>

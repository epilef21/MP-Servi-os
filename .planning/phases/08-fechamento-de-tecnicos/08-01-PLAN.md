---
phase: 08-fechamento-de-tecnicos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/utils/fechamentoTecnicos.js
  - mp-react/src/__tests__/fechamentoTecnicos.test.js
autonomous: true
requirements: [TEC-02]
must_haves:
  truths:
    - "Uma função pura agrupa as OS de um mês por técnico e devolve o total a pagar de cada um"
    - "OS com o mesmo técnico escrito de formas diferentes (maiúsculas/espaços) caem no mesmo grupo"
    - "O critério de 'OS do mês' é criado_em, idêntico ao calcularDRE do DRE"
  artifacts:
    - path: "mp-react/src/utils/fechamentoTecnicos.js"
      provides: "normNome, osDoMes, agruparPorTecnico, acharFechamento, statusFechamento (funções puras)"
      contains: "export function agruparPorTecnico"
    - path: "mp-react/src/__tests__/fechamentoTecnicos.test.js"
      provides: "Testes Vitest das funções puras de agrupamento"
      contains: "describe('agruparPorTecnico'"
  key_links:
    - from: "agruparPorTecnico"
      to: "reports (OS) via criado_em + valor_prestador"
      via: "filtro por mês + soma por técnico normalizado"
      pattern: "valor_prestador"
---

<objective>
Criar o módulo puro `utils/fechamentoTecnicos.js` com toda a lógica de agrupamento das OS do mês por técnico e do total a pagar, mais os testes Vitest. Esta é a fundação testável do fechamento de técnicos (TEC-02) — o componente de UI (plano 03/04) apenas consome estas funções.

Purpose: manter a regra de negócio (quais OS entram no mês, como casar o técnico da OS pelo nome livre, quanto somar) em funções puras verificáveis, exatamente como a Fase 7 fez com `utils/faturamento.js`.
Output: 1 módulo utilitário + 1 arquivo de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fechamento-de-tecnicos/08-CONTEXT.md
@.planning/phases/07-faturamento-e-contas-a-receber/07-PATTERNS.md

<interfaces>
<!-- Analog canônico: utils/faturamento.js (Fase 7) — funções puras, sem I/O, sem vi.mock. -->

Padrão de normalização já existente em mp-react/src/utils/faturamento.js:
```javascript
export function normSeguradora(s) {
  return String(s || '').trim().toLowerCase()
}
```

Critério "OS do mês" a REPLICAR de calcularDRE (FinanceiroEmpresaTab.jsx linhas 595-600):
```javascript
const osDoMes = reports.filter(r => {
  try {
    const d = r.criado_em?.toDate?.()
    return d && d.toISOString().slice(0, 7) === mesRef
  } catch { return false }
})
```

Campos da OS usados: `r.criado_em` (Firestore Timestamp com .toDate()), `r.tecnico` (nome livre, texto), `r.valor_prestador` (número/string, valor devido ao técnico), `r.id`, `r.nome_segurado`, `r.num_assist`, `r.cidade`.

Contrato do documento de fechamento (gravado no plano 04, lido aqui para derivar status):
```
empresas/{id}/fechamentosTecnicos/{docId} = {
  tecnico, tecnico_norm, mes_referencia, os_ids, total, qtd_os,
  status: 'pago', pago_em, criado_em
}
```
</interfaces>

Padrão de teste Vitest de função pura (analog: mp-react/src/__tests__/faturamento.test.js — sem vi.mock, só import + expect).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar utils/fechamentoTecnicos.js com as funções puras de agrupamento</name>
  <files>mp-react/src/utils/fechamentoTecnicos.js</files>
  <read_first>
    - mp-react/src/utils/faturamento.js (padrão de módulo puro + normSeguradora)
    - mp-react/src/components/admin/FinanceiroEmpresaTab.jsx linhas 593-613 (critério osDoMes por criado_em + soma valor_prestador)
  </read_first>
  <behavior>
    - normNome('  João  Silva ') === 'joão silva' (trim + lowercase + colapsa espaços internos)
    - normNome(undefined) === '' e normNome(null) === ''
    - osDoMes([{criado_em:{toDate:()=>new Date('2026-07-10')}}], '2026-07') retorna 1 item
    - osDoMes([{criado_em:{toDate:()=>new Date('2026-06-30')}}], '2026-07') retorna 0 itens
    - osDoMes ignora OS sem criado_em válido sem lançar erro
    - agruparPorTecnico junta 'JOÃO' e 'joão ' no mesmo grupo, somando valor_prestador
    - agruparPorTecnico devolve grupo com { tecnicoNome, tecnicoNorm, osList, total, count } ordenado por tecnicoNome
    - OS sem técnico caem no grupo com tecnicoNome '(sem técnico)'
    - acharFechamento acha o doc por tecnico_norm + mes_referencia; devolve null quando não existe
    - statusFechamento({status:'pago'}) === 'pago'; statusFechamento(null) === 'pendente'
  </behavior>
  <action>
Criar `mp-react/src/utils/fechamentoTecnicos.js` — módulo 100% puro (sem imports de firebase/react), seguindo o cabeçalho de comentário de `utils/faturamento.js`. Exportar EXATAMENTE estas funções:

```javascript
// Normaliza nome do técnico (OS.tecnico é texto livre dos portais) para casar
// com o técnico cadastrado. trim + lowercase + colapsa espaços internos.
export function normNome(s) {
  return String(s || '').trim().toLowerCase().replace(/\s+/g, ' ')
}

// OS pertencem ao mês pelo criado_em — MESMO critério do calcularDRE (consistência LOCKED no CONTEXT.md).
export function osDoMes(reports, mesRef) {
  return (reports || []).filter(r => {
    try {
      const d = r.criado_em?.toDate?.()
      return d && d.toISOString().slice(0, 7) === mesRef
    } catch { return false }
  })
}

// Agrupa as OS do mês por técnico (nome normalizado) somando valor_prestador.
// Retorna [{ tecnicoNome, tecnicoNorm, osList, total, count }] ordenado por tecnicoNome.
export function agruparPorTecnico(reports, mesRef) {
  const doMes = osDoMes(reports, mesRef)
  const grupos = {}
  for (const os of doMes) {
    const norm = normNome(os.tecnico)
    if (!grupos[norm]) {
      grupos[norm] = {
        tecnicoNome: (os.tecnico || '').trim() || '(sem técnico)',
        tecnicoNorm: norm,
        osList: [],
        total: 0,
        count: 0,
      }
    }
    grupos[norm].osList.push(os)
    grupos[norm].total += parseFloat(os.valor_prestador) || 0
    grupos[norm].count += 1
  }
  return Object.values(grupos).sort((a, b) => a.tecnicoNome.localeCompare(b.tecnicoNome))
}

// Acha o doc de fechamento (mesmo técnico normalizado + mês) numa lista já carregada do Firestore.
export function acharFechamento(fechamentos, tecnicoNorm, mesRef) {
  return (fechamentos || []).find(
    f => f.tecnico_norm === tecnicoNorm && f.mes_referencia === mesRef
  ) || null
}

// Status derivado: 'pago' só quando existe doc pago; 'pendente' NUNCA é gravado (igual às notas da Fase 7).
export function statusFechamento(fechamento) {
  return fechamento?.status === 'pago' ? 'pago' : 'pendente'
}
```

Arredondamento: NÃO arredondar `total` dentro do agrupamento (mantém float; a formatação BRL cuida da exibição), igual ao padrão de `calcularDRE`.
  </action>
  <acceptance_criteria>
    - `grep -q "export function normNome" mp-react/src/utils/fechamentoTecnicos.js`
    - `grep -q "export function agruparPorTecnico" mp-react/src/utils/fechamentoTecnicos.js`
    - `grep -q "export function osDoMes" mp-react/src/utils/fechamentoTecnicos.js`
    - `grep -q "export function acharFechamento" mp-react/src/utils/fechamentoTecnicos.js`
    - `grep -q "export function statusFechamento" mp-react/src/utils/fechamentoTecnicos.js`
    - `grep -q "valor_prestador" mp-react/src/utils/fechamentoTecnicos.js`
    - `grep -q "criado_em" mp-react/src/utils/fechamentoTecnicos.js`
    - Nenhum import de firebase/react: `grep -Eq "import .*firebase|import .*react" mp-react/src/utils/fechamentoTecnicos.js` retorna 1 (não encontra)
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npx vitest run src/__tests__/fechamentoTecnicos.test.js</automated>
  </verify>
  <done>Módulo criado com as 5 funções exportadas; funções puras sem I/O; testes da Task 2 passam.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever os testes Vitest de fechamentoTecnicos.js</name>
  <files>mp-react/src/__tests__/fechamentoTecnicos.test.js</files>
  <read_first>
    - mp-react/src/__tests__/faturamento.test.js (estilo de teste de função pura, sem vi.mock)
    - mp-react/src/utils/fechamentoTecnicos.js (recém criado na Task 1)
  </read_first>
  <action>
Criar `mp-react/src/__tests__/fechamentoTecnicos.test.js` importando de `../utils/fechamentoTecnicos`. Sem `vi.mock` (funções puras). Usar helper para simular o Timestamp do Firestore: `const ts = iso => ({ toDate: () => new Date(iso) })`.

Cobrir com `describe`/`it`/`expect`:

1. `describe('normNome')`:
   - `expect(normNome('  João  Silva ')).toBe('joão silva')`
   - `expect(normNome(undefined)).toBe('')`
   - `expect(normNome('MAPFRE')).toBe('mapfre')`

2. `describe('osDoMes')`:
   - inclui OS com `criado_em: ts('2026-07-10T12:00:00Z')` quando mesRef `'2026-07'`
   - exclui OS com `criado_em: ts('2026-06-30T12:00:00Z')` para `'2026-07'`
   - não lança e ignora OS `{ id: 'x' }` sem criado_em (retorna [] para essa entrada)

3. `describe('agruparPorTecnico')`:
   - dados: 3 OS no mês '2026-07', duas do "João" escritas 'JOÃO' e 'joão ' com valor_prestador 100 e 40, uma do "Maria" com 200
   - espera 2 grupos; grupo do João com `total === 140`, `count === 2`; ordenado por nome (João antes de Maria)
   - OS com `tecnico: ''` cria grupo `tecnicoNome === '(sem técnico)'`
   - OS de outro mês não entram no agrupamento

4. `describe('acharFechamento')`:
   - acha `{ tecnico_norm: 'joão silva', mes_referencia: '2026-07', status: 'pago' }`
   - devolve `null` quando técnico não bate

5. `describe('statusFechamento')`:
   - `expect(statusFechamento({ status: 'pago' })).toBe('pago')`
   - `expect(statusFechamento(null)).toBe('pendente')`
  </action>
  <acceptance_criteria>
    - `grep -q "describe('agruparPorTecnico'" mp-react/src/__tests__/fechamentoTecnicos.test.js`
    - `grep -q "toDate:" mp-react/src/__tests__/fechamentoTecnicos.test.js`
    - `grep -q "sem técnico" mp-react/src/__tests__/fechamentoTecnicos.test.js`
    - `grep -Fq "from '../utils/fechamentoTecnicos'" mp-react/src/__tests__/fechamentoTecnicos.test.js`
    - Sem vi.mock: `grep -q "vi.mock" mp-react/src/__tests__/fechamentoTecnicos.test.js` retorna 1 (não encontra)
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npx vitest run src/__tests__/fechamentoTecnicos.test.js</automated>
  </verify>
  <done>Arquivo de teste cobre as 5 funções; `npx vitest run src/__tests__/fechamentoTecnicos.test.js` passa 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhum | Módulo puro, sem I/O, sem entrada de rede/usuário direta — recebe apenas objetos JS já carregados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | agruparPorTecnico com valor_prestador não numérico | mitigate | `parseFloat(...) || 0` já cobre string/null/undefined; teste inclui caso. |
| T-08-02 | Denial | criado_em ausente/inválido lança e derruba a lista | mitigate | try/catch por item em osDoMes; teste cobre OS sem criado_em. |
</threat_model>

<verification>
- `cd mp-react && npx vitest run src/__tests__/fechamentoTecnicos.test.js` passa.
- Módulo não importa firebase nem react (é puro).
</verification>

<success_criteria>
- 5 funções exportadas e testadas.
- Agrupamento por técnico normalizado com total correto, critério de mês = criado_em.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fechamento-de-tecnicos/08-01-SUMMARY.md`
</output>

---
phase: 08-fechamento-de-tecnicos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mp-react/src/components/admin/TecnicosTab.jsx
autonomous: true
requirements: [TEC-01]
must_haves:
  truths:
    - "Admin cadastra chave PIX no formulário do técnico e ela é salva no Firestore"
    - "Admin escolhe a forma de pagamento do técnico (PIX, dinheiro, transferência) e ela é salva"
    - "Ao reabrir o técnico para editar, chave PIX e forma de pagamento aparecem preenchidas"
  artifacts:
    - path: "mp-react/src/components/admin/TecnicosTab.jsx"
      provides: "Campos chave_pix e forma_pagamento no form/modal de técnico"
      contains: "chave_pix"
  key_links:
    - from: "modal do técnico"
      to: "empresas/{id}/tecnicos/{id}"
      via: "payload de salvar (updateDoc/addDoc)"
      pattern: "chave_pix"
---

<objective>
Adicionar os campos **chave PIX** e **forma de pagamento** ao cadastro do técnico no `TecnicosTab.jsx` (TEC-01). Campos simples, gravados no doc do técnico, para ficarem à mão na hora do fechamento (plano 04 copia a chave PIX com um clique).

Purpose: hoje o pagamento é "de cabeça/WhatsApp"; ter a chave PIX no cadastro elimina a busca manual toda vez.
Output: `TecnicosTab.jsx` modificado (form + payload + render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fechamento-de-tecnicos/08-CONTEXT.md
@mp-react/src/components/admin/TecnicosTab.jsx

<interfaces>
<!-- TecnicosTab.jsx JÁ existe e é o alvo. Padrão do form (linhas 10-71): -->

Estado inicial (linha 10):
```javascript
const TECNICO_FORM_INITIAL = {
  nome: '', telefone: '', email: '', especialidade: '', ativo: true,
}
```

openModal (linhas 26-34) monta o form a partir do técnico existente com `tec.campo || ''`.
salvar() (linhas 36-71) monta `payload` e faz updateDoc (editar) ou addDoc (criar), atualizando `setTecnicos` local.
O modal renderiza campos com o padrão `<div className="field"> <label>...</label> <input .../> </div>` (linhas 162-219).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar chave_pix e forma_pagamento ao estado, openModal e payload</name>
  <files>mp-react/src/components/admin/TecnicosTab.jsx</files>
  <read_first>
    - mp-react/src/components/admin/TecnicosTab.jsx linhas 10-71 (form inicial, openModal, salvar/payload)
  </read_first>
  <action>
Em `mp-react/src/components/admin/TecnicosTab.jsx`:

1. Estender `TECNICO_FORM_INITIAL` (linha 10) para incluir os dois campos novos com defaults:
```javascript
const TECNICO_FORM_INITIAL = {
  nome: '', telefone: '', email: '', especialidade: '', ativo: true,
  chave_pix: '', forma_pagamento: 'pix',
}
```

2. Em `openModal` (linhas 26-34), no ramo que carrega o técnico existente, acrescentar os dois campos (mesmo padrão `tec.campo || default`):
```javascript
chave_pix:       tec.chave_pix || '',
forma_pagamento: tec.forma_pagamento || 'pix',
```

3. Em `salvar()`, no objeto `payload` (linhas 45-51), acrescentar:
```javascript
chave_pix:       formData.chave_pix.trim() || '',
forma_pagamento: formData.forma_pagamento || 'pix',
```

NÃO alterar a validação existente (chave PIX e forma de pagamento são opcionais — não adicionar em `errs`). Manter o resto do handler intacto (updateDoc/addDoc, setTecnicos, showToast).
  </action>
  <acceptance_criteria>
    - `grep -q "chave_pix: ''" mp-react/src/components/admin/TecnicosTab.jsx`
    - `grep -q "forma_pagamento: 'pix'" mp-react/src/components/admin/TecnicosTab.jsx`
    - `grep -c "chave_pix" mp-react/src/components/admin/TecnicosTab.jsx` retorna 3 ou mais (initial + openModal + payload)
    - `grep -q "forma_pagamento: formData.forma_pagamento" mp-react/src/components/admin/TecnicosTab.jsx`
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npx eslint src/components/admin/TecnicosTab.jsx</automated>
  </verify>
  <done>Form inicial, openModal e payload incluem chave_pix e forma_pagamento; validação inalterada; eslint sem erros no arquivo.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar os campos chave PIX e forma de pagamento no modal</name>
  <files>mp-react/src/components/admin/TecnicosTab.jsx</files>
  <read_first>
    - mp-react/src/components/admin/TecnicosTab.jsx linhas 162-219 (grid de campos do modal)
  </read_first>
  <action>
No corpo do modal (dentro do grid `gridTemplateColumns: '1fr 1fr'`, linhas 163-219), ANTES do bloco do toggle "Ativo" (linha 204), adicionar dois campos novos seguindo o padrão `<div className="field">`:

1. Chave PIX (largura total):
```jsx
<div className="field" style={{ gridColumn: '1/-1' }}>
  <label>Chave PIX (opcional)</label>
  <input
    value={formData.chave_pix}
    onChange={e => setFormData(p => ({ ...p, chave_pix: e.target.value }))}
    placeholder="CPF, telefone, e-mail ou chave aleatória"
  />
</div>
```

2. Forma de pagamento (select):
```jsx
<div className="field">
  <label>Forma de pagamento</label>
  <select
    value={formData.forma_pagamento}
    onChange={e => setFormData(p => ({ ...p, forma_pagamento: e.target.value }))}
  >
    <option value="pix">PIX</option>
    <option value="dinheiro">Dinheiro</option>
    <option value="transferencia">Transferência</option>
  </select>
</div>
```

Manter o restante do modal e do card de listagem intactos.
  </action>
  <acceptance_criteria>
    - `grep -q "Chave PIX" mp-react/src/components/admin/TecnicosTab.jsx`
    - `grep -q "Forma de pagamento" mp-react/src/components/admin/TecnicosTab.jsx`
    - `grep -q 'value="transferencia"' mp-react/src/components/admin/TecnicosTab.jsx`
    - `grep -q "p.chave_pix" mp-react/src/components/admin/TecnicosTab.jsx` (onChange do input) — via `p, chave_pix`
    - `grep -q "forma_pagamento: e.target.value" mp-react/src/components/admin/TecnicosTab.jsx`
  </acceptance_criteria>
  <verify>
    <automated>cd mp-react && npm run build</automated>
  </verify>
  <done>Modal renderiza input de chave PIX e select de forma de pagamento; `npm run build` compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (client) → Firestore tecnicos | Admin autenticado grava campos do técnico; regra `tecnicos` já exige isUserOfEmpresa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | chave PIX é dado sensível do técnico | accept | Coleção `tecnicos` já restrita a isUserOfEmpresa/superadmin (firestore.rules linhas 103-108); mesma sensibilidade de telefone/e-mail já ali. Sem novo boundary. |
| T-08-04 | Tampering | forma_pagamento com valor arbitrário | mitigate | Select fixo com 3 opções (pix/dinheiro/transferencia); default 'pix' no payload. |
</threat_model>

<verification>
- `cd mp-react && npm run build` compila.
- `grep -c "chave_pix"` >= 4 (initial, openModal, payload, input).
</verification>

<success_criteria>
- Admin cadastra/edita chave PIX e forma de pagamento; valores persistem em `empresas/{id}/tecnicos/{id}`.
- Editar um técnico reabre com os campos preenchidos.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fechamento-de-tecnicos/08-02-SUMMARY.md`
</output>
